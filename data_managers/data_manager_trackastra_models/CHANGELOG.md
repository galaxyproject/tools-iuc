# Changelog

## 0.1.1

- Fix command templating so Galaxy cannot render shell continuation
  backslashes as literal arguments to `fetch_models.py`.
- Use separate single-line commands for upstream and administrator-uploaded
  model sources.
- Make the functional-test assertions robust for nested data-manager JSON.

## 0.1.0

- Initial data-manager release.
