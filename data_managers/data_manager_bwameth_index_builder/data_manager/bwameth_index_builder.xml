<tool id="bwameth_index_builder_data_manager" name="bwa-meth index" tool_type="manage_data" version="1.0.0.3" profile="24.2">
    <description>builder</description>
    <macros>
        <token name="@TOOL_VERSION@">0.2.2</token>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">bwameth</requirement>
    </requirements>
    <command detect_errors="aggressive"><![CDATA[
        #import os.path
        #set basename=os.path.basename($all_fasta_source.fields.path)
        #set sym_linked_fasta_filename = os.path.join($out_file.extra_files_path, $basename)
        mkdir -p '$out_file.extra_files_path' &&
        ln -s '${all_fasta_source.fields.path}' '$sym_linked_fasta_filename' &&
        bwameth.py index '$sym_linked_fasta_filename' &&
        cp '$dmjson' '$out_file'
    ]]></command>
    <configfiles>
        <configfile name="dmjson"><![CDATA[#slurp
#import os.path
#set basename=os.path.basename($all_fasta_source.fields.path)
#set sym_linked_fasta_filename = os.path.join($out_file.extra_files_path, $basename)
#set $value = $sequence_id or $all_fasta_source.fields.dbkey
#set $name = $sequence_name or $all_fasta_source.fields.name
{
  "data_tables":{
    "bwameth_indexes":[
      {
        "value": "${value}",
        "dbkey": "${all_fasta_source.fields.dbkey}",
        "name": "${name}",
        "path": "$basename"
      }
    ]
  }
}
]]></configfile>
    </configfiles>
    <inputs>
        <param name="all_fasta_source" type="select" label="Source FASTA Sequence">
            <options from_data_table="all_fasta" />
        </param>
        <param name="sequence_id" type="text" value="" label="ID for index" help="If not specified, the value from the fasta file is used"/>
        <param name="sequence_name" type="text" value="" label="Displayed description for sequence" help="If not specified, the value from the fasta file is used"/>
    </inputs>
    <outputs>
        <data name="out_file" format="data_manager_json"/>
    </outputs>
    <tests>
        <test>
            <param name="all_fasta_source" value="phiX174"/>
            <output name="out_file">
                <assert_contents>
                    <has_text text="bwameth_indexes"/>
                    <has_json_property_with_text property="dbkey" text="phiX174"/>
                    <has_json_property_with_text property="name" text="phiX 174"/>
                    <has_json_property_with_text property="value" text="phiX174"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**Notice:** If you leave name, description, or id blank, it will be generated automatically.

What is BWA-meth?
-----------------

BWA-meth performs alignment of reads in a bisulfite-sequencing experiment (e.g., RRBS or WGBS) to a genome. The methodology employed for this is similar to bismark, where both the reads and the reference genome are *in silico* converted prior to alignment. Methylation extraction on the resulting BAM file can be done with the PileOMeth tool.

    ]]></help>
    <citations>
        <citation type="bibtex">@misc{1401.1129,
        Author = {Brent S. Pedersen and Kenneth Eyring and Subhajyoti De and Ivana V. Yang and David A. Schwartz},
        Title = {Fast and accurate alignment of long bisulfite-seq reads},
        Year = {2014},
        Eprint = {arXiv:1401.1129},
        }</citation>
    </citations>
</tool>
