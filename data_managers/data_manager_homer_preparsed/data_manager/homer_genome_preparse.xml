<tool id="homer_genome_preparse_data_manager" name="Homer genome preparse" tool_type="manage_data" version="@WRAPPER_VERSION@" profile="18.09">
    <requirements>
        <requirement type="package" version="@WRAPPER_VERSION@">homer</requirement>
    </requirements>
    <macros>
        <token name="@WRAPPER_VERSION@">4.11</token>
    </macros>
    <command detect_errors="exit_code"><![CDATA[
        python '$__tool_directory__/homer_genome_preparse.py'
        '${out_file}'
        --fasta_filename '${all_fasta_source.fields.path}'
        --fasta_dbkey '${all_fasta_source.fields.dbkey}'
        --fasta_description '${all_fasta_source.fields.name}'
        ${mask}
        --size '${size}'
        --data_table_name homer_preparse
    ]]></command>
    <inputs>
        <param name="all_fasta_source" type="select" label="Source FASTA Sequence">
            <options from_data_table="all_fasta"/>
        </param>
        <param name="sequence_name" type="text" value="" label="Name of sequence" />
        <param name="sequence_id" type="text" value="" label="ID for sequence" />
        <param name="mask" type="boolean" truevalue="--mask" falsevalue="" checked="False" label="mask repeats/lower case sequence" />
        <param name="size" type="integer" value="200" min="0" label="fragment size to use for motif finding" />
    </inputs>
    <outputs>
        <data name="out_file" format="data_manager_json"/>
    </outputs>
    <tests>
        <test>
            <param name="all_fasta_source" value="phiX174"/>
            <output name="out_file" value="homer_genome_preparse_data_manager.json"/>
        </test>
        <test>
            <param name="all_fasta_source" value="phiX174"/>
            <param name="mask" value="True"/>
            <param name="size" value="100"/>
            <output name="out_file" value="homer_genome_preparse_data_manager2.json"/>
        </test>
    </tests>

    <help>
.. class:: infomark

**Notice:** If you leave name, description, or id blank, it will be generated automatically.
    </help>
</tool>
