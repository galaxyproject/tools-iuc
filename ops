{"author":{"id":"783b610f274a0d93f6239db0fc75d0982606c877a4661d3aea84b42bc3a85cea"},"ops":[{"type":1,"timestamp":1550259843,"nonce":"tbQOvCLUSRwSxdwQ3RtskTC4bn4=","metadata":{"github-id":"MDU6SXNzdWU0MTA5MTY3NTM=","github-url":"https://github.com/galaxyproject/tools-iuc/issues/2300","origin":"github"},"title":"VCFfilter \"invert\" filter function not working (Galaxy Version 1.0.0_rc1+galaxy1 AND new 1.0.0_rc1+galaxy2)","message":"**Presents in:** Galaxy Version 1.0.0_rc1+galaxy1 AND new 1.0.0_rc1+galaxy2\n\nTest history: https://usegalaxy.org:/u/jen/h/test-history-vcffilter-100rc1galaxy1\n\nNew version did not address this problem (was not known as an issue). The new version does have some tool form rendering issues, so if that requires a wrapper update/re-publish to MTS, should probably do both at the same time.\n\n**Prior issue/fixes for 1.0.0_rc1+galaxy1 that resulted in 1.0.0_rc1+galaxy2**: \n - Issue 1 (closed) https://github.com/galaxyproject/tools-iuc/issues/1910\n - Issue 2 (could probably be closed if tool form doesn't need more changes for \"help\" rendering problems) https://github.com/galaxyproject/tools-iuc/issues/1917\n - Testing ticket for integration at https://usegalaxy.org (using new 19.01 pre-release): https://github.com/galaxyproject/usegalaxy-playbook/issues/182. See this part: https://github.com/galaxyproject/usegalaxy-playbook/issues/182#issuecomment-464163384\n\n**Copied over testing info from ticket re: invert functionality problems uncovered**:\n\nMissed that the \"invert\" (grep -v) functionality is not working (prior version and this one). \n\nSome terms will allow a `\u003c` operator that (sort of) gets around this. \nExamples: `DP \u003e 10` vs `DP \u003c 10` (both work)\n\nMixed operators are problematic:`!=` and `\u003c=` and `\u003e=` -- so no way to do this kind of filtering. Tried `ne` instead of `!=` but don't think that is expected to work with this tool. \nExamples: `type = ins` (works) versus `type != ins` (fails). `DP \u003e= 10`(fails). `DP != 10`(fails)","files":null}]}