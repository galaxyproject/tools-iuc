<tool id="orthologer" name="orthologer" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
  <description>Compute orthology</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <expand macro="version_command"/>
  <command detect_errors="exit_code"><![CDATA[
  #if $test == 'true'
  export MAP_ORTHODB_DATA='' &&
  #else
  export MAP_ORTHODB_DATA='$cached_ortodb_db.fields.path' &&
  export BUSCO_DATA='$cached_busco_db.fields.path' &&
  #end if
  ## creates environment and config file
  ## -L -> maximum nr of processes in parallel
  orthologer -f -c create -L "\${GALAXY_SLOTS}" &&

  ## copy input fasta
  touch orthology_import.txt &&
  
  #import os
  #for $fasta in $fsfiles:
  #set $label=os.path.basename($fasta.element_identifier)
  echo '+$label' '$fasta' >> orthology_import.txt &&
  #end for

  ## import fasta files and set project label
  ./orthologer.sh manage -f orthology_import.txt &&

  ## ensure the config file is conform
  ./orthologer.sh -t todo/orthology_import.todo -u &&
  
  ## run orthology
  echo "===> START RUN" &&
  ./orthologer.sh -r all &&
  echo "===> END OF RUN" &&
  
  ## create result
  grep -v '^#' Cluster/orthology_import.og_map > orthology.txt

  ]]></command>
  <inputs>
    <param name="test" type="hidden"/>
    <param name="cached_ortodb_db" label="Cached OrthoDB data" type="select">
      <options from_data_table="orthodb_data">
        <validator message="No OrthoDB data is available - please contact your Galaxy admin." type="no_options"/>
      </options>
    </param>
    <param name="cached_busco_db" label="Cached BUSCO database" type="select">
      <options from_data_table="busco_database">
        <validator message="No BUSCO database is available - please contact your Galaxy admin." type="no_options"/>
      </options>
    </param>
    <param name="fsfiles" type="data" multiple="true" format="fasta" label="Fasta file"/>
  </inputs>
  <outputs>
    <data name="orthology" label="${tool.name} on ${on_string}: computed orthology" format="txt" from_work_dir="orthology.txt"/>
  </outputs>
  <tests>
    <test expect_num_outputs="1">
      <param name="test" value="true"/>
      <param name="fsfiles" value="test-fasta/hsapi.fs,test-fasta/mputo.fs,test-fasta/mmula.fs"/>
      <output name="orthology">
        <assert_contents>
          <has_n_lines n="1030" delta="4"/>
        </assert_contents>
      </output>
    </test>
  </tests>
  <help><![CDATA[
  Orthologer computes orthology among a set of proteoms as given by fasta files.
  
  .. class:: warningmark
  
  For larger scale computations or if your computation requires fine tuning, the galaxy tool is not suitable.
  Please use the tool as described at https://orthologer.ezlab.org .

  **Input data**

  - A set of FASTA files with protein sequences. Each FASTA file represents one species.

  **Output**

  - The computed orthology.

  ]]></help>
  <expand macro="citations" />
</tool>
