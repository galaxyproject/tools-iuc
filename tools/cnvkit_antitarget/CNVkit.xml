<tool id="CNVkit antitarget" name="CNVkit antitarget" version="0.9.9" python_template_version="3.5">
    <description>Given a “target” BED file that lists the chromosomal coordinates of the tiled regions used for targeted resequencing, derive a BED file off-target/”antitarget” regions.</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[  
        ln -s '$targets_file' ./capture.bed &&
        cnvkit.py antitarget
            ./capture.bed
            --output '$output_setting.output'
    ]]></command>
        <configfiles>
        <configfile name="script_file"><![CDATA[
'$targets_file'
--access '$access'
--avg-size $avg_size
--min-size $min_size
--output '$output_setting.output'
        ]]></configfile>
    </configfiles>
    <inputs>
        <param name="targets_file" type="data" format="bed" multiple="false" label="input BED or interval file" help="input BED or interval file listing the targeted regions." />
        <param argument="--access" type="data" optional="true" format="bed" multiple="false" label=" Regions of accessible sequence on chromosomes" help="" />
        <param argument="--avg-size" optional="true" type="float" label=" Average size of antitarget bins." value="150000" help="(results are approximate)" />
        <param argument="--min-size" optional="true" type="float" label="Minimum size of antitarget bins." value="" help="(smaller regions are dropped)" />
        <section name="output_setting" title="Change output file name" expanded="false">
            <param argument="--output" optional="true" type="text" label="Output file name" value="capture.antitarget.bed" help="File_name.bed" />
        </section>
    </inputs>
    <outputs>
        <data name="out_capture_antitarget" format="bed" label="${tool.name} on ${on_string}: capture antitarget regions" from_work_dir="capture.antitarget.bed" />
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="targets_file" ftype="bed" value="capture.bed" />
            <param name="output" value="capture.antitarget.bed" />
            <output name="out_capture_antitarget" file="capture.antitarget.bed" /> 
        </test>
    </tests>
    <help><![CDATA[
    Given a “target” BED file that lists the chromosomal coordinates of the tiled regions used for targeted resequencing, derive a BED file off-target/”antitarget” regions.
    ]]></help>
    <citations>
        <citation type="doi">10.1371/journal.pcbi.1004873</citation>
    </citations>
</tool>
