<tool id="EMBOSS: wobble108" name="wobble"  version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
  <description>Wobble base plot</description>
  <macros>
    <token name="@COMMAND@">wobble</token>
    <import>macros.xml</import>
  </macros>
  <expand macro="bio_tools" />
  <expand macro="requirements" />
  <expand macro="version_command" />
  <command><![CDATA[
    @COMMAND@
      -sequence '$input1'
      -graph png
      -outfile '$ofile2'
      -goutfile 'output'
      -window '$window'
      -bases '$bases'
      -auto
  ]]></command>
  <inputs>
    <param name="input1" type="data" format="fasta" label="Sequence" />
    <param name="window" type="integer" value="30" label="Window size, in codons" />
    <param name="bases" type="text" value="GC" label="Bases used" />
  </inputs>
  <outputs>
    <data name="ofile1" format="png" from_work_dir="output.1.png" label="${tool.name} on ${on_string}: png"/>
    <data name="ofile2" format="wobble"/>
  </outputs>
  <tests>
    <test>
      <param name="input1" value="2.fasta"/>
      <param name="window" value="30"/>
      <param name="bases" value="GC"/>
      <output name="ofile1">
        <assert_contents>
          <has_size min="1"/>
        </assert_contents>
      </output>
      <output name="ofile2" file="emboss_wobble_out.wobble"/>
    </test>
  </tests>
  <help>
Plot third base position variability in a nucleotide sequence

    .. class:: warningmark

The input dataset needs to be sequences.

-----

@DOC_LINK@
  </help>
  <expand macro="citations" />
</tool>
