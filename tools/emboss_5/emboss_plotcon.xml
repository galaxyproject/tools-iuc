<tool id="EMBOSS: plotcon75" name="plotcon"  version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
  <!-- produces png file -->
  <description>Plot quality of conservation of a sequence alignment</description>
  <macros>
    <token name="@COMMAND@">plotcon</token>
    <import>macros.xml</import>
  </macros>
  <expand macro="bio_tools" />
  <expand macro="requirements" />
  <expand macro="version_command" />
  <command><![CDATA[
  @COMMAND@
    -sequences '$input1'
    -graph png
    -goutfile 'out_file1'
    -winsize $winsize
    -auto
  ]]></command>
  <inputs>
    <param name="input1" type="data" format="data" label="Sequence" />
    <param name="winsize" type="integer" value="4" label="Number of columns to average alignment quality over" />
  </inputs>
  <outputs>
    <data name="out_file1" format="png" from_work_dir="out_file1.1.png" />
  </outputs>
  <tests>
    <test>
      <!-- TODO msf is available in Galaxy, but not uploadable, so we use txt -->
      <param name="input1" value="globins.msf" ftype="txt"/>
      <output name="out_file1">
        <assert_contents>
          <has_size min="1"/>
        </assert_contents>
      </output>
    </test>
  </tests>
  <help>

@DOC_LINK@

  </help>
  <expand macro="citations" />
</tool>
