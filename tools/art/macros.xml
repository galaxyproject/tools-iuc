<macros>
    <token name="@WRAPPER_VERSION@">2014.11.03.0</token>

    <xml name="requirements">
      <requirements>
        <requirement type="package" version="2014_11_03">art</requirement>
        <requirement type="package" version="3.11.14">art</requirement><!-- conda dependency with the exact same version -->
        <yield/>
      </requirements>
    </xml>

    <xml name="stdio">
        <stdio>
            <!-- Anything other than zero is an error -->
            <exit_code range="1:" />
            <exit_code range=":-1" />
            <!-- In case the return code has not been set propery check stderr too -->
            <regex match="Error:" />
            <regex match="Exception:" />
        </stdio>
    </xml>
    <xml name="aln">
        <param name="aln" type="boolean" truevalue="-a" falsevalue=""
               label="output ALN alignment file" argument="-a" />
    </xml>
    <xml name="sam">
        <param name="sam" type="boolean" truevalue="-s" falsevalue=""
               label="output SAM alignment file" argument="-s" />
    </xml>

    <xml name="rndSeed">
        <param name="rndSeed" type="integer" value="-1" min="-1" optional="true"
               label="specify a fixed, random seed for the simulation"
               help="Use -1 to request a random seed" argument="-r/-rs" /><!-- art_SoLiD seems to use -r while the others expect -rs -->
    </xml>

    <xml name="citation">
        <citations>
            <citation type="doi">10.1093/bioinformatics/btr708</citation>
        </citations>
    </xml>

    <xml name="amplicon">
        <conditional name="amplicon">
            <param name="use_amplicon" type="boolean" label="Run Amplicon Sequencing Simulation" truevalue="amplicon_true" falsevalue="amplicon_false" />
            <when value="amplicon_true">
                <param label="number of reads per amplicon (for 5'end amplicon sequencing)" name="reads_per_amplicon" type="integer" value="0"/>
            </when>
            <when value="amplicon_false">
            </when>
        </conditional>
    </xml>

    <xml name="amplicon_pair">
        <conditional name="amplicon">
            <param name="use_amplicon" type="boolean" label="Run Amplicon Sequencing Simulation" truevalue="amplicon_true" falsevalue="amplicon_false" />
            <when value="amplicon_true">
                <param name="read_pairs_per_amplicon" type="integer" value="0"
                       label="number of read pairs per amplicon (for two-end amplicon sequencing)" />
            </when>
            <when value="amplicon_false"/>
        </conditional>
    </xml>

    <xml name="frag_len_sd">
        <param name="fragment_size" type="integer" value="200"
               label="the average DNA fragment size"/>
        <param name="fragment_sd" type="integer" value="0"
               label="the standard deviation of the DNA fragment size"/>
    </xml>
</macros>
