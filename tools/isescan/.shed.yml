name: "ISEScan"
owner: "iuc"
long_description: |
  "ISEScan is a pipeline to identify IS (Insertion Sequence) elements in genome and metagenome
  based on profile hidden Markov models constructed from manually curated IS elements."
categories:
- Sequence Analysis
remote_repository_url: "https://github.com/pimarin/tools-iuc/tree/isescan"
homepage_url: "https://github.com/xiezhq/ISEScan"
type: unrestricted
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "{{ tool_name }} tool for Insertion Sequence identification"
