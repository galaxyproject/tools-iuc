<?xml version="1.0" encoding="utf-8"?>
<tool id="sc3-sc3-calc-biology" name="SC3 calculate biology" version="@TOOL_VERSION@+galaxy0">
    <description>SC3 calculates biologically relevant clusters</description>
    <macros>
        <import>sc3-macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        sc3-sc3-calc-biology.R -i "$input1" -o "$output1"
    ]]></command>
    <inputs>
        <param type="data" name="input1" format="rds" label="File name in which a SC3 'SingleCellExperiment' object has been stored after kmeans clustering"/>
    </inputs>
    <outputs>
        <data name="output1" format="rds" label="File name for R object of type 'SingleCellExperiment' from SC3 in which to store the consensus matrix"/>
    </outputs>
    <tests>
        <test>
            <param name="input1" value="R_sc3_kmeans.rds"/>
            <output name="output1" file="R_sc3_kmeans_biology.rds"/>
        </test>
    </tests>
    <help><![CDATA[
    ]]></help>
    <expand macro="citations" />
</tool>