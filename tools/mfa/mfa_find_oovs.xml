<tool id="mfa_find_oovs" name="MFA Find OOVs" version="@TOOL_VERSION@+@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>find words in a corpus that are missing from the dictionary</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        @PREPARE_ENV@

        ## 1. Prepare Corpus
        mkdir -p corpus_dir &&
        #if $corpus_source.ext == 'zip':
            unzip -q '$corpus_source' -d corpus_dir &&
        #else:
            tar -xf '$corpus_source' -C corpus_dir &&
        #end if

        ## 2. Prepare Dictionary
        #if $dictionary_type.source == 'history':
            ln -s '$dictionary_type.dictionary_file' input_dictionary.txt &&
            #set $dict_arg = 'input_dictionary.txt'
        #else:
            #set $dict_arg = $dictionary_type.builtin_dictionary.fields.path
        #end if

        ## 3. Run Find OOVs
        ## Syntax: mfa find_oovs [OPTIONS] CORPUS_DIRECTORY DICTIONARY_PATH OUTPUT_DIRECTORY
        mfa find_oovs
            corpus_dir
            $dict_arg
            output_dir
            --job_name 'mfa_find_oovs_job'
            --clean
            --num_jobs "\${GALAXY_SLOTS:-4}"
            #if $speaker_characters:
                --speaker_characters '$speaker_characters'
            #end if

        ## 4. Package Output
        ## The tool creates 'oovs_found.txt' and 'oov_counts.txt' in output_dir
        ## We zip the entire directory for context, or we could extract specific files.
        &&
        cd output_dir &&
        zip -r -q output.zip . &&
        mv output.zip '$output_archive'
    ]]></command>

    <inputs>
        <param name="corpus_source" type="data" format="zip,tar,tar.gz" label="Corpus Archive" help="Archive containing audio and transcripts." />

        <conditional name="dictionary_type">
            <param name="source" type="select" label="Dictionary Source">
                <option value="builtin" selected="true">Use a built-in MFA Dictionary</option>
                <option value="history">Upload from History</option>
            </param>
            <when value="builtin">
                <param name="builtin_dictionary" type="select" label="Select Dictionary">
                    <options from_data_table="mfa_dictionary">
                        <column name="value" index="0"/>
                        <column name="dbkey" index="1"/>
                        <column name="name" index="2"/>
                        <column name="path" index="3"/>
                    </options>
                </param>
            </when>
            <when value="history">
                <param name="dictionary_file" type="data" format="txt" label="Dictionary File" />
            </when>
        </conditional>

        <param name="speaker_characters" type="integer" value="0" min="0" label="Speaker Characters" help="Number of characters at the start of filenames to use as speaker ID (0 = use directory structure)." />
        
    </inputs>

    <outputs>
        <data name="output_archive" format="zip" label="${tool.name} on ${on_string}: OOV Report" />
    </outputs>

    <tests>
        <test expect_num_outputs="1">
            <param name="corpus_source" value="test_corpus.zip" ftype="zip" />
            <conditional name="dictionary_type">
                <param name="source" value="builtin" />
                <param name="builtin_dictionary" value="english_us_arpa" />
            </conditional>
            <output name="output_archive" file="oov_report.zip" compare="sim_size" delta="5000" />
        </test>
    </tests>

    <help><![CDATA[
**Montreal Forced Aligner: Find OOVs**

This tool scans your corpus and compares the words in your transcripts against the provided pronunciation dictionary.

**What it does**

It identifies **Out-Of-Vocabulary (OOV)** items—words that exist in your text but missing from the dictionary. If a word is OOV, MFA cannot align it because it doesn't know its phonemes.

**Output**

A Zip archive containing:

* `oovs_found.txt`: A list of missing words.
* `oov_counts.txt`: How many times each missing word appears.
* `utterance_oovs.txt`: Which specific recording files contain the missing words.

**Next Steps**

You can take the `oovs_found.txt` file and run it through the **MFA G2P** tool to generate pronunciations for them!

    ]]></help>
    <expand macro="creators" />
    <expand macro="citations" />
</tool>