<tool id="mfa_remap_alignments" name="MFA Remap Alignments" version="@TOOL_VERSION@+@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>convert TextGrid transcriptions to a new phone set</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        @PREPARE_ENV@

        ## 1. Prepare Corpus
        mkdir -p corpus_dir &&
        #if $corpus_source.ext == 'zip':
            unzip -q '$corpus_source' -d corpus_dir &&
        #else:
            tar -xf '$corpus_source' -C corpus_dir &&
        #end if

        ## 2. Prepare Dictionary
        #if $dictionary_type.source == 'history':
            ln -s '$dictionary_type.dictionary_file' input_dictionary.txt &&
            #set $dict_arg = 'input_dictionary.txt'
        #else:
            #set $dict_arg = $dictionary_type.builtin_dictionary.fields.path
        #end if

        ## 3. Prepare Mapping File
        ln -s '$mapping_file' mapping_rules.yaml &&

        ## 4. Run Remap
        ## Syntax: mfa remap alignments [OPTIONS] CORPUS_DIRECTORY MAPPING_PATH OUTPUT_DIRECTORY
        mfa remap alignments
            corpus_dir
            mapping_rules.yaml
            output_dir
            --dictionary_path $dict_arg
            --job_name 'mfa_remap_align_job'
            --num_jobs "\${GALAXY_SLOTS:-4}"

        ## 5. Package Output
        &&
        cd output_dir &&
        zip -r -q output.zip . &&
        mv output.zip '$output_archive'
    ]]></command>

    <inputs>
        <param name="corpus_source" type="data" format="zip,tar,tar.gz" label="Corpus Archive" help="The corpus containing the TextGrids you want to remap." />

        <conditional name="dictionary_type">
            <param name="source" type="select" label="Source Dictionary" help="The dictionary that matches the CURRENT phone set of your corpus.">
                <option value="builtin" selected="true">Use a built-in MFA Dictionary</option>
                <option value="history">Upload from History</option>
            </param>
            <when value="builtin">
                <param name="builtin_dictionary" type="select" label="Select Dictionary">
                    <options from_data_table="mfa_dictionary">
                        <column name="value" index="0"/>
                        <column name="dbkey" index="1"/>
                        <column name="name" index="2"/>
                        <column name="path" index="3"/>
                    </options>
                </param>
            </when>
            <when value="history">
                <param name="dictionary_file" type="data" format="txt" label="Dictionary File" />
            </when>
        </conditional>

        <param name="mapping_file" type="data" format="yaml,json,txt" label="Mapping File" help="A file defining how to map old phones to new phones (e.g., Arpabet to IPA)." />
    </inputs>

    <outputs>
        <data name="output_archive" format="zip" label="${tool.name} on ${on_string}: Remapped TextGrids" />
    </outputs>

    <tests>
        <test expect_num_outputs="1">
            <param name="corpus_source" value="test_corpus_aligned.zip" ftype="zip" />
            <conditional name="dictionary_type">
                <param name="source" value="history" />
                <param name="dictionary_file" value="test_dict.txt" />
            </conditional>
            <param name="mapping_file" value="arpabet_to_ipa.yaml" />
            <output name="output_archive" file="remapped_output.zip" compare="sim_size" delta="5000" />
        </test>
    </tests>

    <help><![CDATA[
**Montreal Forced Aligner: Remap Alignments**

This tool updates the phone labels in your TextGrid files based on a mapping configuration.

**Use Case**
You have aligned a corpus using a standard dictionary (e.g., Arpabet), but you want the final TextGrids to use a different notation (e.g., IPA) without re-running the computationally expensive alignment process.

**Inputs**
1. **Corpus:** Zip archive containing audio and TextGrids.
2. **Dictionary:** The dictionary matching the *current* TextGrids.
3. **Mapping File:** A YAML/JSON file (e.g., `AA: ɑ`).

@HELP_FOOTER@
    ]]></help>
    <expand macro="creators" />
    <expand macro="citations" />
</tool>