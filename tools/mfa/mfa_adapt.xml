<tool id="mfa_adapt" name="MFA Adapt" version="@TOOL_VERSION@+@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>adapt a pretrained acoustic model to a new dataset</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[
        @PREPARE_ENV@

        ## 1. Prepare Corpus
        mkdir -p corpus_dir &&
        #if $corpus_source.ext == 'zip':
            unzip -q '$corpus_source' -d corpus_dir &&
        #else:
            tar -xf '$corpus_source' -C corpus_dir &&
        #end if

        ## 2. Prepare Dictionary
        #if $dictionary_type.source == 'history':
            ln -s '$dictionary_type.dictionary_file' input_dictionary.txt &&
            #set $dict_arg = 'input_dictionary.txt'
        #else:
            #set $dict_arg = $dictionary_type.builtin_dictionary.fields.path
        #end if

        ## 3. Prepare Input Acoustic Model
        #if $acoustic_type.source == 'history':
            ln -s '$acoustic_type.acoustic_file' input_acoustic.zip &&
            #set $ac_arg = 'input_acoustic.zip'
        #else:
            #set $ac_arg = $acoustic_type.builtin_acoustic.fields.path
        #end if

        ## 4. Run Adaptation
        ## Syntax: mfa adapt [OPTIONS] CORPUS_DIRECTORY DICTIONARY_PATH ACOUSTIC_MODEL_PATH OUTPUT_MODEL_PATH
        mfa adapt
            corpus_dir
            $dict_arg
            $ac_arg
            '$output_model'
            --job_name 'mfa_adapt_job'
            #if $speaker_characters:
                --speaker_characters '$speaker_characters'
            #end if
    ]]></command>

    <inputs>
        <param name="corpus_source" type="data" format="zip,tar,tar.gz" label="Corpus Archive" help="Archive containing audio and transcripts to adapt to." />

        <conditional name="dictionary_type">
            <param name="source" type="select" label="Dictionary Source">
                <option value="builtin" selected="true">Use a built-in MFA Dictionary</option>
                <option value="history">Upload from History</option>
            </param>
            <when value="builtin">
                <param name="builtin_dictionary" type="select" label="Select Dictionary">
                    <options from_data_table="mfa_dictionary" >
                        <column name="value" index="0"/>
                        <column name="dbkey" index="1"/>
                        <column name="name" index="2"/>
                        <column name="path" index="3"/>
                    </options>
                </param>
            </when>
            <when value="history">
                <param name="dictionary_file" type="data" format="txt" label="Dictionary File" />
            </when>
        </conditional>

        <conditional name="acoustic_type">
            <param name="source" type="select" label="Input Acoustic Model Source">
                <option value="builtin" selected="true">Use a built-in MFA Model</option>
                <option value="history">Upload from History</option>
            </param>
            <when value="builtin">
                <param name="builtin_acoustic" type="select" label="Select Acoustic Model to Adapt">
                    <options from_data_table="mfa_acoustic">
                        <column name="value" index="0"/>
                        <column name="dbkey" index="1"/>
                        <column name="name" index="2"/>
                        <column name="path" index="3"/>
                    </options>
                </param>
            </when>
            <when value="history">
                <param name="acoustic_file" type="data" format="zip" label="Acoustic Model File" />
            </when>
        </conditional>

        <param name="speaker_characters" type="integer" value="0" min="0" label="Speaker Characters" help="Number of characters at the start of filenames to use as speaker ID (0 = use directory structure). Adaptation works best with clear speaker separation." />
        
    </inputs>

    <outputs>
        <data name="output_model" format="zip" label="${tool.name} on ${on_string}: Adapted Acoustic Model" />
    </outputs>

    <tests>
        <test expect_num_outputs="1">
            <param name="corpus_source" value="test_corpus.zip" ftype="zip" />
            <conditional name="dictionary_type">
                <param name="source" value="builtin" />
                <param name="builtin_dictionary" value="english_us_arpa" />
            </conditional>
            <conditional name="acoustic_type">
                <param name="source" value="builtin" />
                <param name="builtin_acoustic" value="english_us_arpa" />
            </conditional>
            <output name="output_model" file="adapted_model.zip" compare="sim_size" delta="10000" />
        </test>
    </tests>

    <help><![CDATA[
**Montreal Forced Aligner: Adapt**

This tool fine-tunes a pre-trained acoustic model using a specific dataset. 

**Why use this?**

Standard models (like "English US") are trained on generic data (e.g., LibriSpeech). If your audio contains:

* Strong accents
* Children's speech
* Noisy environments
* Elderly speech

Running `Adapt` will create a **new** acoustic model specifically tailored to your data. You can then use this new model in the **MFA Align** tool (by selecting "Upload from History").

**Inputs**

1.  **Corpus:** Zip/Tar archive of Audio + Transcripts.
2.  **Dictionary:** Standard pronunciation dictionary.
3.  **Acoustic Model:** The "Starting point" model you want to improve.

**Outputs**

* **Adapted Acoustic Model:** A `.zip` file containing the new model.

    ]]></help>
    <expand macro="creators" />
    <expand macro="citations" />
</tool>