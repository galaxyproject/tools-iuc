<tool id="samestr_compare" name="SameStr Compare" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>performs pairwise clade-specific comparison of metagenomic alignments</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[

    ln -s '$input_files' '${input_files.element_identifier}.${input_files.ext}' &&
    ln -s '$input_names' '${input_names.element_identifier}.names.txt' &&

    samestr compare
    --marker-dir '$database.fields.path'
    --input-files '${input_files.element_identifier}.${input_files.ext}'
    --input-names '${input_names.element_identifier}.names.txt'
    --output-dir out_compare/
    $variant_comparison_settings.dominant_variants
    $variant_comparison_settings.dominant_variants_added
    $variant_comparison_settings.dominant_variants_msa
    ]]></command>
    <inputs>
        <param name="input_files" type="data" format="npz,npy" label="Input SNV profiles" help="NPZ/NPY files from samestr merge."/>
        <param name="input_names" type="data" format="txt" label="Sample identifiers" help="Text files listing the sample/gene identifiers."/>

        <param name="database" type="select" label="SameStr database" help="SameStr database built from MetaPhlAn or mOTUS markers.">
            <options from_data_table="samestr_db">
                <filter type="sort_by" column="2" />
                <filter type="static_value" column="4" value="1" />
            </options>
        </param>


        <section name="variant_comparison_settings" title="Variant comparison settings">
            <param argument="--dominant-variants" type="boolean" truevalue="--dominant-variants" falsevalue="" label="Compare only dominant variants" help="Compare only dominant variants as obtained from consensus call (default: False)." />
            <param argument="--dominant-variants-added" type="boolean" truevalue="--dominant-variants-added" falsevalue="" label="Include dominant variants as additional entries" help="Add dominant variants as additional entries to data (default: False)." />
            <param argument="--dominant-variants-msa" type="boolean" truevalue="--dominant-variants-msa" falsevalue="" label="Output dominant variant alignments" help="Generate FASTA alignments of dominant variants for each clade (default: False)." />
        </section>
    </inputs>
    <outputs>
        <data name="overlap_file" format="tabular" from_work_dir="out_compare/*.overlap.txt" label="${tool.name} on ${on_string}: Coverage overlap"/>
        <data name="close_file" format="tabular" from_work_dir="out_compare/*.closest.txt" label="${tool.name} on ${on_string}: Closest distance"/>
        <data name="fraction_file" format="tabular" from_work_dir="out_compare/*.fraction.txt" label="${tool.name} on ${on_string}: Similarity fraction"/>
        <data name="msa_file" format="fasta" from_work_dir="out_compare/*.msa.fa" label="${tool.name} on ${on_string}: Dominant variant alignments">
            <filter>variant_comparison_settings.dominant_variants_msa</filter>
        </data>

    </outputs>
    <tests>
        <test expect_num_outputs="4">
            <param name="input_files" value="t__Propionibacterium_phage_PAS50.npz" ftype="npz" />
            <param name="input_names" value="t__Propionibacterium_phage_PAS50.names.txt" ftype="txt" />
            <param name="database" value="mpa_test-db-without-one-marker"/>

            <section name="variant_comparison_settings">
                <param name="dominant_variants" value="true" />
                <param name="dominant_variants_added" value="true" />
                <param name="dominant_variants_msa" value="true" />
            </section>

            <output name="overlap_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0.dom" />
                    <has_text text="phage_padded.dom" />
                    <has_text_matching expression="genome_0\.dom\s+0\s+0" />
                    <has_text_matching expression="phage_padded\.dom\s+0\s+66" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="close_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0.dom" />
                    <has_text text="phage_padded.dom" />
                    <has_text_matching expression="genome_0\.dom\s+0\s+0" />
                    <has_text_matching expression="phage_padded\.dom\s+0\s+66" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="fraction_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0.dom" />
                    <has_text text="phage_padded.dom" />
                    <has_text_matching expression="genome_0\.dom\s+0\.0\s+0\.0" />
                    <has_text_matching expression="phage_padded\.dom\s+0\.0\s+1\.0" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="msa_file" ftype="fasta">
                <assert_contents>
                    <has_text text=">genome_0.dom" />
                    <has_text text=">phage_padded.dom" />
                    <has_size value="1373" delta="500"/>
                </assert_contents>
            </output>
        </test>

        <test expect_num_outputs="4">
            <param name="input_files" value="t__Propionibacterium_phage_PAS50.npz" ftype="npz" />
            <param name="input_names" value="t__Propionibacterium_phage_PAS50.names.txt" ftype="txt" />
            <param name="database" value="mpa_test-db-without-one-marker"/>

            <section name="variant_comparison_settings">
                <param name="dominant_variants" value="false" />
                <param name="dominant_variants_added" value="false" />
                <param name="dominant_variants_msa" value="false" />
            </section>

            <output name="overlap_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0" />
                    <has_text text="phage_padded" />
                    <has_text_matching expression="genome_0\s+0\s+0" />
                    <has_text_matching expression="phage_padded\s+0\s+66" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="close_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0" />
                    <has_text text="phage_padded" />
                    <has_text_matching expression="genome_0\s+0\s+0" />
                    <has_text_matching expression="phage_padded\s+0\s+66" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="fraction_file" ftype="tabular">
                <assert_contents>
                    <has_text text="Sample" />
                    <has_text text="genome_0" />
                    <has_text text="phage_padded" />
                    <has_text_matching expression="genome_0\s+0\.0\s+0\.0" />
                    <has_text_matching expression="phage_padded\s+0\.0\s+1\.0" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>

            <output name="msa_file" ftype="fasta">
                <assert_contents>
                    <has_size value="0" />
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
@SAMESTR_INTRO@

SameStr Compare
...............

SameStr Compare performs pairwise clade-specific alignment comparisons to identify shared strains between samples.
It calculates similarity metrics between variant profiles to detect when the same bacterial strain is present
across different samples.

**Input Requirements**

SameStr Compare analyzes filtered SNV profiles from samestr filter. For each analysis, the following inputs must be provided:

- A SNV profile file in NPZ/NPY format (.npz, .npy, or .npy.gz).
- Corresponding sample identifier files in text format (.txt).
- The MetaPhlAn or mOTUS clade marker database that matches the database used throughout the workflow.

**Parameters**

- Compare only dominant variants: Focus on dominant variants only, ignoring minority variants.
- Include dominant variants as additional entries: Add dominant variant comparisons alongside all-variant comparisons.
- Output dominant variant alignments: Generate FASTA alignments of dominant variants for each clade.

**Results and Outputs**

The tool generates the following outputs per clade:

- Coverage overlap. Number of overlapping marker positions between sample pairs (.overlap.txt) .
- Clostest distance. Distance metric based on variant differences between samples (.closest.txt) .
- Similarity fraction. Normalized similarity scores (.fraction.txt) .
- Dominant variant alignments in FASTA format (.msa.fasta). Multiple sequence alignments of dominant variants. This output is only generated when the "Output dominant variant alignments" is enabled.
    ]]></help>
    <expand macro="citations" />
</tool>
