<tool id="samestr_summarize" name="SameStr Summarize" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>identfies and reports shared bacterial strains across samples using variant profile similarity</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
        mkdir tax_profiles out_compare &&

        #for $file in $tax_profiles:
            ## Naming the taxonomic profile datasets correctly is crucial and error-prone.
            ## The parameter help explains that the names need to match sample names,
            ## but command line users might not strip the file suffix they are used to,
            ## so we do it here just in case. (This is also important for testing.)
            #set $tax_basename = $file.element_identifier.split('.profile.txt')[0]
            ln -s '$file' 'tax_profiles/${tax_basename}.profile.txt' &&
        #end for
        #for $file in $overlaps:
            ln -s '$file' 'out_compare/${file.element_identifier}.overlap.txt' &&
        #end for

        #for $file in $similarities:
            ln -s '$file' 'out_compare/${file.element_identifier}.fraction.txt' &&
        #end for

        samestr summarize
        --marker-dir '$database.fields.path'
        --input-dir out_compare/
        --tax-profiles-dir tax_profiles/
        --aln-pair-min-overlap $aln_pair_min_overlap
        --aln-pair-min-similarity $aln_pair_min_similarity
    ]]></command>
    <inputs>
        <param name="database" type="select" label="SameStr database" help="SameStr database built from MetaPhlAn or mOTUS markers">
            <options from_data_table="samestr_db">
                <filter type="sort_by" column="2" />
                <filter type="static_value" column="4" value="1" />
            </options>
        </param>
        <param name="overlaps" type="data_collection" collection_type="list" format="tabular" label="Coverage Overlap results produced by SameStr compare"/>
        <param name="similarities" type="data_collection" collection_type="list" format="tabular" label="Similarity Fraction results produced by SameStr compare"/>
        <param name="tax_profiles" type="data" multiple="true" format="txt" label="Taxonomic profiles"
        help="Taxonomic profiles generated with MetaPhlAn or mOTUS for each of the samples in the SameStr compare outputs. Note: To obtain nicely formatted reports the dataset names here should match the sample names mentioned in the overlap and similarity inputs!"/>
        <section name="arguments" title="Summary threshold arguments">
            <param argument="--aln-pair-min-overlap" type="integer" value="5000" min="0" label="Minimum overlap for comparison" help="Minimum number of overlapping positions required in both alignments to evaluate similarity (default: 5000)." />
            <param argument="--aln-pair-min-similarity" type="float" value="0.999" min="0" max="1" label="Minimum similarity for shared strains" help="Minimum pairwise alignment similarity to call shared strains (default: 0.999)." />
        </section>
    </inputs>
    <outputs>
        <data name="cooccurrences" format="tabular" from_work_dir="out_summarize/sstr_cooccurrences.tsv" label="${tool.name} on ${on_string}: Strain co-occurrences"/>
        <data name="strain_events" format="tabular" from_work_dir="out_summarize/sstr_strain_events.tsv" label="${tool.name} on ${on_string}: Strain events"/>
        <data name="taxon_counts" format="tabular" from_work_dir="out_summarize/taxon_counts.tsv" label="${tool.name} on ${on_string}: Taxon counts"/>
    </outputs>
    <tests>
       <test expect_num_outputs="3">
            <param name="database" value="mpa_test-db-without-one-marker"/>
            <param name="overlaps">
                <collection type="list">
                    <element name="t__Propionibacterium_phage_PAS50" value="t__Propionibacterium_phage_PAS50.overlap.txt" ftype="tabular"/>
                </collection>
            </param>
            <param name="similarities">
                <collection type="list">
                    <element name="t__Propionibacterium_phage_PAS50" value="t__Propionibacterium_phage_PAS50.fraction.txt" ftype="tabular"/>
                </collection>
            </param>
            <param name="tax_profiles" value="phage_padded.profile.txt,PAS50.profile.txt" ftype="txt"/>
            <section name="arguments">
                <param name="aln_pair_min_overlap" value="10"/>
                <param name="aln_pair_min_similarity" value="0.9"/>
            </section>
            <output name="cooccurrences" ftype="tabular">
                <assert_contents>
                    <has_text_matching expression="row\s+col\s+shared_kingdom\s+shared_phylum\s+shared_class\s+shared_order\s+shared_family\s+shared_genus\s+shared_species" />
                    <has_text_matching expression="PAS50\s+phage_padded\s+1\s+1\s+1\s+1\s+1\s+1\s+1\s+1\s+0\s+0" />
                    <has_text_matching expression="PAS50\s+file_0\s+\t+\t+\t+\t+\t+\t+\t+\t+0\s+0" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>
            <output name="strain_events" ftype="tabular">
                <assert_contents>
                    <has_text_matching expression="row\s+col\s+clade\s+similarity\s+overlap\s+analysis_level\s+analyzed_strain\s+shared_strain\s+event" />
                    <has_text_matching expression="PAS50\s+file_0\s+t__Propionibacterium_phage_PAS50\s+0\.0\s+0\s+clade-level\s+False\s+False\s+same_clade" />
                    <has_n_lines n="2" />
                </assert_contents>
            </output>
            <output name="taxon_counts" ftype="tabular">
                <assert_contents>
                    <has_text_matching expression="Name\s+count_kingdom\s+count_phylum\s+count_class\s+count_order\s+count_family\s+count_genus\s+count_species" />
                    <has_text_matching expression="PAS50\s+1\s+1\s+1\s+1\s+1\s+1\s+1" />
                    <has_text_matching expression="phage_padded\s+1\s+1\s+1\s+1\s+1\s+1\s+1" />
                    <has_n_lines n="3" />
                </assert_contents>
            </output>
        </test>
    </tests>

    <help><![CDATA[
@SAMESTR_INTRO@

SameStr Summarize
.................

SameStr Summarize identifies and summarizes shared bacterial strains across samples based on variant profile similarity.
This module generates taxonomic co-occurrence tables at multiple taxonomic levels and provides strain sharing events.

**Input Requirements**

The following inputs must be provided:

- Samestr compare outputs (Similarity fractions and Coverage overlaps) containing pairwise similarity matrices.
- Taxonomic profiles from MetaPhlAn or mOTUS containing relative abundance data for each sample.
- The MetaPhlAn or mOTUS clade marker database that matches the database used throughout the workflow.

**Parameters**

- Minimum overlap for comparison. Minimum number of overlapping positions which have to be covered in both alignments to evaluate alignment similarity.
- Minimum similarity for shared strains. Minimum pairwise alignment similarity required to call strains as shared.

**Results and Outputs**

- Strain co-occurrences (TSV format): Taxonomic co-occurrence counts across different taxonomic levels. Shows how many taxonomic groups are shared between each sample pair.
- Strain events (TSV format): Detailed strain sharing events between sample pairs.
- Taxon counts (TSV format): Taxon counts per sample at different taxonomic levels.
    ]]></help>
   <expand macro="citations" />
</tool>
