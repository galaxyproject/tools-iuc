<tool id="samestr_summarize" name="SameStr Summarize" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Identify and summarize shared bacterial strains across samples using variant profile similarity</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
    mkdir -p out_summarize/ tax_profiles/ out_compare/ &&

    #for $file in $tax_profiles_dir:
        cp '$file' tax_profiles/'$file.element_identifier' &&
    #end for

    #for $file in $overlap_files:
        ln -s '$file' out_compare/'$file.element_identifier' &&
    #end for

    #for $file in $close_files:
        ln -s '$file' out_compare/'$file.element_identifier' &&
    #end for

    #for $file in $fraction_files:
        ln -s '$file' out_compare/'$file.element_identifier' &&
    #end for

    samestr summarize
    --marker-dir '$database.fields.path'
    --input-dir out_compare/
    --output-dir out_summarize/
    --tax-profiles-dir tax_profiles/
    #if $tax_profiles_extension
        --tax-profiles-extension '$tax_profiles_extension'
    #end if
    --aln-pair-min-overlap '$aln_pair_min_overlap'
    --aln-pair-min-similarity '$aln_pair_min_similarity'
    ]]></command>
    <inputs>
        <param name="database" type="select" label="SameStr database" help="SameStr database built from MetaPhlAn or mOTUS markers">
            <options from_data_table="samestr_db">
                <filter type="sort_by" column="2" />
            </options>
        </param>

        <param name="overlap_files" type="data_collection" collection_type="list" format="tabular" label="Overlap files"/>
        <param name="close_files" type="data_collection" collection_type="list" format="tabular" label="Closest files"/>
        <param name="fraction_files" type="data_collection" collection_type="list" format="tabular" label="Fraction files"/>

        <param argument="--tax-profiles-dir" type="data_collection" collection_type="list" format="txt" label="Taxonomic profiles" help="Collection of MetaPhlAn or mOTUS .profile.txt files"/>
        <param argument="--tax-profiles-extension" type="text" value=".profile.txt" optional="true" label="Taxonomic profile extension" help="File extension of taxonomic profiles (default: .profile.txt)"/>

        <section name="arguments" title="Summary threshold arguments">
            <param argument="--aln-pair-min-overlap" type="integer" value="5000" min="0" label="Minimum overlap for comparison" help="Minimum number of overlapping positions required in both alignments to evaluate similarity (default: 5000)." />
            <param argument="--aln-pair-min-similarity" type="float" value="0.999" min="0" max="1" label="Minimum similarity for shared strains" help="Minimum pairwise alignment similarity to call shared strains (default: 0.999)." />
        </section>

    </inputs>
    <outputs>
        <data name="command_log" format="json" from_work_dir="out_summarize/command_log.json" label="${tool.name} on ${on_string}: Command log"/>
        <data name="cooccurrences" format="tsv" from_work_dir="out_summarize/sstr_cooccurrences.tsv" label="${tool.name} on ${on_string}: Strain co-occurrences"/>
        <data name="strain_events" format="tsv" from_work_dir="out_summarize/sstr_strain_events.tsv" label="${tool.name} on ${on_string}: Strain events"/>
        <data name="taxon_counts" format="tsv" from_work_dir="out_summarize/taxon_counts.tsv" label="${tool.name} on ${on_string}: Taxon counts"/>
    </outputs>
    <tests>
       <test expect_num_outputs="4">
            <param name="database" value="mpa_test-db-without-one-marker"/>
            <param name="overlap_files">
                <collection type="list">
                    <element name="t__Propionibacterium_phage_PAS50.overlap.txt" value="t__Propionibacterium_phage_PAS50.overlap.txt" ftype="tabular"/>
                </collection>
            </param>
            
            <param name="close_files">
                <collection type="list">
                    <element name="t__Propionibacterium_phage_PAS50.closest.txt" value="t__Propionibacterium_phage_PAS50.closest.txt" ftype="tabular"/>
                </collection>
            </param>
            
            <param name="fraction_files">
                <collection type="list">
                    <element name="t__Propionibacterium_phage_PAS50.fraction.txt" value="t__Propionibacterium_phage_PAS50.fraction.txt" ftype="tabular"/>
                </collection>
            </param>
            
            <param name="tax_profiles_dir">
                <collection type="list">
                    <element name="phage_padded.profile.txt" value="phage_padded.profile.txt" ftype="txt"/>
                    <element name="PAS50.profile.txt" value="PAS50.profile.txt" ftype="txt"/>
                </collection>
            </param>
            
            <param name="tax_profiles_extension" value=".profile.txt"/>
    
            <section name="arguments">
                <param name="aln_pair_min_overlap" value="10"/>
                <param name="aln_pair_min_similarity" value="0.9"/>
            </section>

            <output name="command_log" ftype="json">
                <assert_contents>
                    <has_text text="command"/>
                    <has_text text="date-start"/>
                    <has_text text="date-end"/>
                </assert_contents>
            </output>

            <output name="cooccurrences" ftype="tsv">
                <assert_contents>
                    <has_text text="row"/>
                    <has_text text="col"/>
                    <has_text text="shared_kingdom"/>
                    <has_text text="shared_phylum"/>
                    <has_text text="shared_class"/>
                    <has_text text="shared_order"/>
                    <has_text text="shared_family"/>
                    <has_text text="shared_genus"/>
                    <has_text text="shared_species"/>
                    <has_text text="phage_padded"/>
                    <has_text text="PAS50"/>
                </assert_contents>
            </output>

            <output name="strain_events" ftype="tsv">
                <assert_contents>
                    <has_text text="row"/>
                    <has_text text="col"/>
                    <has_text text="clade"/>
                    <has_text text="similarity"/>
                    <has_text text="overlap"/>
                    <has_size min="5" />
                </assert_contents>
            </output>

            <output name="taxon_counts" ftype="tsv">
                <assert_contents>
                    <has_text text="Name"/>
                    <has_text text="count_kingdom"/>
                    <has_text text="count_phylum"/>
                    <has_text text="count_class"/>
                    <has_text text="count_order"/>
                    <has_text text="count_family"/>
                    <has_text text="count_genus"/>
                    <has_text text="count_species"/>
                    <has_text text="phage_padded"/>
                    <has_text text="PAS50"/>
                </assert_contents>
            </output>
        </test>
    </tests>

    <help><![CDATA[
    **SameStr Summarize**

    SameStr Summarize identifies and summarizes shared bacterial strains across samples based on variant profile similarity. 
    This module generates taxonomic co-occurrence tables at multiple taxonomic levels and provides strain sharing events.

    **Input Requirements**

    The following inputs must be provided:
    - Samestr compare outputs (.fraction.txt, .overlap.txt, .closest.txt) generated by samestr compare, containing pairwise similarity matrices.
    - Taxonomic profile files (.profile.txt) from MetaPhlAn or mOTUS containing relative abundance data for each sample.
    - The MetaPhlAn or mOTUS clade marker database that matches the database used throughout the workflow.

    **Parameters**

    - Minimum overlap for comparison. Minimum number of overlapping positions which have to be covered in both alignments to evaluate alignment similarity.
    - Minimum similarity for shared strains. Minimum pairwise alignment similarity required to call strains as shared.
    - Taxonomic profile extension (default: .profile.txt). File extension for taxonomic profiles.

    **Results and Outputs**

    - Command log (JSON format). A detailed execution log of the summarize process.
    - Strain co-occurrences (TSV format). Taxonomic co-occurrence counts across different taxonomic levels. Shows how many taxonomic groups are shared between each sample pair.
    - Strain events (TSV format). Detailed strain sharing events between sample pairs.
    - Taxon counts (TSV format). Taxon counts per sample at different taxonomic levels. 
    ]]></help>
   <expand macro="citations" />
</tool>