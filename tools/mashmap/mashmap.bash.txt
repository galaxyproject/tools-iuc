#raw
rm -rf 'reflist'
#for i, mash in enumerate($reflist):
   #if i == 0:
       echo $mash > 'reflist'
   #else:
       echo $mash >> 'reflist'
   #end if
#end for
mashmap -rl 'reflist' -q '$query' > $mashout
#end raw
