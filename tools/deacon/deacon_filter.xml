<tool id="deacon_filter" name="Deacon Filter" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>TODO</description>
    <macros>
        <import>macros.xml</import>
    </macros>^
    <expand macro="requirements"/>
    <command detect_errors="exit_code">
        <![CDATA[

            #import re

            #if $index_type.is_select == "own"
                #set $identifier = re.sub("[^\s\w\-]", "_", str($index_file.element_identifier))
                ln -s "$index_file" "${identifier}.deacon.idx" &&
            #end if

            #if $fastx_input.is_select == "single"
                #set $identifier = re.sub("[^\s\w\-]", "_", str($file.element_identifier))
                ln -s "$file" "${identifier}.${file.ext}" &&
            #else
                #set $identifier = re.sub("[^\s\w\-]", "_", str($first_file.element_identifier))
                ln -s "$first_file" "${identifier}.${first_file.ext}" &&
                #set $identifier = re.sub("[^\s\w\-]", "_", str($second_file.element_identifier))
                ln -s "$second_file" "${identifier}.${second_file.ext}" &&
            #end if

            deacon filter
            #if $index_type.is_select == "own"
                #set $identifier = re.sub("[^\s\w\-]", "_", str($index_file.element_identifier))
                "${identifier}.deacon.idx"
            #else
                '$from_dm.fields.path'
            #end if
            #if $fastx_input.is_select == "single"
                #set $identifier = re.sub("[^\s\w\-]", "_", str($file.element_identifier))
                "${identifier}.${file.ext}"
                -o "output_single.${file.ext}"
            #else
                #set $identifier = re.sub("[^\s\w\-]", "_", str($first_file.element_identifier))
                "${identifier}.${first_file.ext}"
                -o "output_first_pair.${first_file.ext}"
                #set $identifier = re.sub("[^\s\w\-]", "_", str($second_file.element_identifier))
                "${identifier}.${second_file.ext}"
                -O "output_second_pair.${second_file.ext}"
            #end if
            -a ${abs_threshold}
            -r ${rel_threshold}
            -p ${prefix_threshold}
            $deplete
            $rename
            $summary
            -t "\${GALAXY_SLOTS:-8}"
            --compression-level ${compression_level}

            &&

            #if $fastx_input.is_select == "single"
                mv "output_single.${file.ext}" "$file_single"
            #else
                mv "output_first_pair.${first_file.ext}" "$pair_first_file" &&
                mv "output_second_pair.${second_file.ext}" "$pair_second_file"
            #end if

        ]]>
    </command>
    <inputs>
        <conditional name="index_type">
            <param name="is_select" type="select" label="Select if index file will be used from DM or own index file">
                <option value="own">Use own index file</option>
                <option value="dm">Use index file from DM</option>
            </param>
            <when value="own">
                <param name="index_file" type="data" format="deacon.idx" label="Input own index file"/>
            </when>
            <when value="dm">
                <expand macro="dm_ref"/>
            </when>
        </conditional>
        <conditional name="fastx_input">
            <param name="is_select" type="select" label="Single or paired input">
                <option value="single">Single input</option>
                <option value="paired">Paired with 2 single files</option>
            </param>
            <when value="single">
                <param name="file" type="data" format="fasta,fastq,fasta.gz,fastq.gz" label="Input file to be filtered"/>
            </when>
            <when value="paired">
                <param name="first_file" type="data" format="fasta,fastq,fasta.gz,fastq.gz" label="Input first file here from pair"/>
                <param name="second_file" type="data" format="fasta,fastq,fasta.gz,fastq.gz" label="Input second file here from pair"/>
            </when>
        </conditional>
        <param argument="--abs-threshold" type="integer" value="2" label="Set absolute number threshold" help="Minimum absolute number of minimizer hits for a match"/>
        <param argument="--rel-threshold" type="float" min="0.0" value="0.01" max="1.0" label="Set relative proportion threshold" help="Minimum relative proportion of minimizer hits for a match"/>
        <param argument="--prefix-length" type="integer" value="0" label="Set prefix length" help=" Search only the first N nucleotides per sequence (0 = entire sequence)"/>
        <param argument="--deplete" type="boolean" falsevalue="" truevalue="--deplete" checked="false" label="Invert filtering" help="The output will contained the non-matching sequences instead of the matching sequences when this flag is set"/>
        <param argument="--rename" type="boolean" falsevalue="" truevalue="--rename" checked="false" label="Rename headers" help="Replace sequence headers with incrementing numbers"/>
        <param argument="--summary" type="boolean" falsevalue="" truevalue="--summary '$json'" checked="false" label="Output a JSON summary file"/>
        <param argument="--compression-level" type="integer" value="2" min="1" max="9" label="Set compression level if input is gz compressed" help="this only affects if input files are .gz files. When non .gz files are inputed they are not effected by it."/> 
    </inputs>
    <outputs>
        <data name="json" format="json" label="${tool.name} on ${on_string}: JSON summary file">
            <filter>summary</filter>
        </data>
        <data name="file_single" auto_format="true" label="${tool.name} on ${on_string}: Single file">
            <filter>fastx_input['is_select'] == single</filter>
        </data>
        <data name="pair_first_file" auto_format="true" label="${tool.name} on ${on_string}: First pair file">
            <filter>fastx_input['is_select'] == paired</filter>
        </data>
        <data name="pair_second_file" auto_format="true" label="${tool.name} on ${on_string}: Second pair file">
            <filter>fastx_input['is_select'] == paired</filter>
        </data>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <conditional name="index_type">
                <param name="is_select" value="own"/>
                <param name="index_file" value="A1.deacon.idx" ftype="deacon.idx"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="single"/>
                <param name="file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <output name="file_single" file="filter_single.fasta.gz" ftype="fasta.gz"/>
        </test>
        <test expect_num_outputs="1">
            <conditional name="index_type">
                <param name="is_select" value="dm"/>
                <param name="from_dm" value="test_index"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="single"/>
                <param name="file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <output name="file_single" file="filter_single.fasta.gz" ftype="fasta.gz"/>
        </test>
        <test expect_num_outputs="1">
            <conditional name="index_type">
                <param name="is_select" value="own"/>
                <param name="index_file" value="A1.deacon.idx" ftype="deacon.idx"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="single"/>
                <param name="file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <param name="deplete" value="true"/>
            <output name="file_single" file="filter_single_inverted.fasta.gz" ftype="fasta.gz"/>
        </test>
        <test expect_num_outputs="1">
            <conditional name="index_type">
                <param name="is_select" value="own"/>
                <param name="index_file" value="A1.deacon.idx" ftype="deacon.idx"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="single"/>
                <param name="file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <param name="rename" value="true"/>
            <output name="file_single" file="filter_single_rename.fasta.gz" ftype="fasta.gz"/>
        </test>
        <test expect_num_outputs="2">
            <conditional name="index_type">
                <param name="is_select" value="own"/>
                <param name="index_file" value="A1.deacon.idx" ftype="deacon.idx"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="single"/>
                <param name="file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <param name="summary" value="true"/>
            <param name="abs_threshold" value="5"/>
            <param name="rel_threshold" value="0.5"/>
            <param name="prefix_length" value="3"/>
            <param name="compression_level" value="8"/>
            <output name="file_single" file="filter_single_setting.fasta.gz" ftype="fasta.gz"/>
            <output name="json" file="test.json" ftype="json"/>
        </test>
        <test expect_num_outputs="2">
            <conditional name="index_type">
                <param name="is_select" value="own"/>
                <param name="index_file" value="test_fastq_gz.deacon.idx" ftype="deacon.idx"/>
            </conditional>
            <conditional name="fastx_input">
                <param name="is_select" value="paired"/>
                <param name="first_file" value="A1_forward.fastq.gz" ftype="fasta.gz"/>
                <param name="second_file" value="A1_reverse.fastq.gz" ftype="fasta.gz"/>
            </conditional>
            <param name="rename" value="true"/>
            <output name="pair_first_file" file="filter_pair_first.fasta.gz" ftype="fasta.gz"/>
            <output name="pair_second_file" file="filter_pair_second.fasta.gz" ftype="fasta.gz"/>
        </test>
    </tests>
    <help>
        <![CDATA[
        
            **WHAT DOES THIS TOOL**

            This tool filter out sequence data from fasta/fastq file base on the given index file

            **Input**

            - Index file which is used as base to filter out all sequences not contain in this file (Own file or a file from DM can be used)
            - A fasta/fastq file (gzip supported)
            - Optional: A second fasta/fastq file when paired input is chosen 
            
            **Output**

            - A fasta/fastq file (gzip or not depend on the input) where matching sequences are stored. NOTE: when deplete option is used then this file contained all non-matching sequences
            - Optional: When paired input is chosen then there will be a second fasta/fastq output
            - Optional: A summary file in JSON format            
        
        ]]>
    </help>
    <expand macro="citation"/>
</tool>