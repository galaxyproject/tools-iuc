<?xml version="1.0"?>
<macros>
    <xml name="requirements">
        <requirements>
            <requirement type="package" version="@TOOL_VERSION@">vg</requirement>
        </requirements>
    </xml>
    <token name="@TOOL_VERSION@">1.23.0</token>  
    <token name="@TOOL_VERSION_GIRAFFE@">1.73.0</token>  
    <token name="@VERSION_SUFFIX_GIRAFFE@">0</token>
    <token name="@PROFILE@">26.1</token>
    <xml name="param_input_xg_for_htslib">
        <param name="input_xg" argument="--xg-name" type="data" format="xg" optional="true" label="XG Graph" help="Writing the alignments in SAM, BAM, or CRAM format can have poor performance. Specify a XG graph that is compatible with the input graph to avoid that. This is mandatory if your input GBZ or GBWTGraph is missing reference paths for surjection (e.g., if it was created with a vg version older than 1.37.0). This option is ignored if the input graph is already an XG graph"/>
    </xml>
    <xml name="param_input_gbwt">
        <param name="input_gbwt" argument="--gbwt-name" type="data" format="gbwt" label="GBWT Index" help="Use this GBWT index while mapping"/>
    </xml>
    <xml name="param_interleaved">
        <param argument="--interleaved" type="boolean" truevalue="--interleaved" falsevalue="" checked="false" label="Interleaved Pairs" help="Check this if the input file contains interleaved paired-end reads"/>
    </xml>
    <xml name="param_comments_as_tags">
        <param name="comments_as_tags" type="boolean" truevalue="--comments-as-tags" falsevalue="" checked="false" label="Comments as Tags" help="Treat comments in FASTQ/FASTA name lines as SAM-style tags and annotate alignments with them (--comments-as-tags)"/>   
    </xml>
    <xml name="param_named_coordinates">
        <param name="named_coordinates" type="boolean" truevalue="--named-coordinates" falsevalue="" checked="false" label="Named Coordinates" help="Output coordinates named instead of node IDs. This only works if the input graph supports it (e.g., was created from a GFA file with segment names); otherwise, tool execution will fail (--named-coordinates)"/>
    </xml>
    <xml name="param_ref_name">
        <param argument="--ref_name" type="text" optional="true" label="Reference Name" help="Name of the reference assembly to use as the target for surjection"/>                 
    </xml>
    <xml name="param_ref_paths">
        <param argument="--ref_paths" type="data" format="txt" optional="true" label="Reference Paths File" help="A text file (one path name per line) to define the target sequences for surjection and their order for the @SQ header"/>
    </xml>
    <xml name="param_add_graph_aln">
        <param name="add_graph_aln" type="boolean" truevalue="--add-graph-aln" falsevalue="" checked="false" label="Add Graph Alignment" help="Annotate records with a cs-style difference string of the pre-surjected graph alignment in the GR tag (--add_graph_aln)"/>
    </xml>
    <xml name="param_sample">
        <param argument="--sample" type="text" optional="true" label="Sample Name" help="Add the sample name to the output"/>
    </xml>
    <xml name="param_read_group">
        <param name="read_group" type="text" optional="true" label="Read Group" help="Add the read group to the output (--read-group)"/>
    </xml>
    <xml name="param_set_refpos">
        <param name="set_refpos" type="boolean" truevalue="--set-refpos" falsevalue="" checked="false" label="Add Reference Offset" help="Add the reference offset for reads to the output (--set-refpos)"/>   
    </xml>
    <xml name="citations">
        <citations>
            <yield />
        </citations>
    </xml>
</macros>
