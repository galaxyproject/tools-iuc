<tool id="vg_convert" name="vg convert" version="@TOOL_VERSION@">
    <description></description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
ln -s '$infile' ./infile.gfa &&

vg convert
$output_format
./infile.gfa
> '$output'
    ]]></command>
    <inputs>
        <param name="infile" type="data" format="gfa1" label="Input file in Graphical Fragment Assembly (gfa)" />
        <param name="output_format" type="select" label="Choose your output format">
            <option value="--xg-out">XG format (--xg-out)</option>
            <option value="--odgi-out">odgi format (--odgi-out)</option>
        </param>
    </inputs>
    <outputs>
        <data name="output" format="odgi">
            <change_format>
                <when input="output_format" value="--xg-out" format="xg"/>
            </change_format>
        </data>
    </outputs>
    <tests>
        <test>
            <param name="infile" value="x.gfa" ftype="gfa1" />
            <param name="output_format" value="--odgi-out" />
            <output name="output" file="t.odgi" ftype="odgi" />
        </test>
    </tests>
    <help><![CDATA[

variation graph (vg) convert module
-----------------------------------

This module is converting Graphical Fragment Assembly (gfa) files into the XG or ODGI format.

    ]]></help>
    <expand macro="citations" />
</tool>
