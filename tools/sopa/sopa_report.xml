<tool id="sopa_report" name="SOPA Report" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Generate HTML quality control report</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="bio_tools"/>
    <expand macro="requirements"/>
    <expand macro="creators"/>
    <command detect_errors="exit_code"><![CDATA[
        @CMD_UNZIP_ZARR@

        ## Run report generation
        sopa report
            input/spatialdata.zarr
            report.html
            --table-key '$table_key'
    ]]></command>
    <inputs>
        <expand macro="input_sdata"/>
        <param argument="--table-key" type="text" value="table" label="Key in SpatialData object containing the table to be used for report generation">
            <expand macro="sanitize_query"/>
        </param>
    </inputs>
    <outputs>
        <data name="output_report" format="html" label="${tool.name} on ${on_string}: QC Report"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input_sdata" value="toy_dataset_aggregated.spatialdata.zip"/>
            <output name="output_report">
                <assert_contents>
                    <has_text text="SOPA"/>
                    <has_text text="Quality Control"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
**What it does**

SOPA Report generates an interactive HTML quality control report for spatial omics data. The report provides comprehensive visualizations and statistics to assess data quality and processing results.

**Requirements**

- Input should be a processed SpatialData object (ideally after aggregation)
- Works best with aggregated data but can generate reports at any pipeline stage

**More Information**

See the `SOPA report documentation <https://prism-oncology.github.io/sopa/tutorials/cli_usage/#pipeline-report>`_.
    ]]></help>
    <expand macro="citations"/>
</tool>
