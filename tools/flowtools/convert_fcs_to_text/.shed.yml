owner: iuc
name: convert_fcs_to_text
description: converts FCS files to text format with no tranformation
long_description: |
    **Input**
    This tool requires valid FCS files as input. Files are processed serially. Applying compensation is an option for FCS files including a compensation matrix.

    **Output file**
    The output is tab-separated text, containing the fluorescence intensity values for each marker
    
    https://www.immportgalaxy.org
categories:
    - Flow Cytometry Analysis
    - Convert Formats
remote_repository_url: https://github.com/ImmPortDB/immport-galaxy-tools/tree/master/flowtools/convert_fcs_to_text
type: unrestricted
