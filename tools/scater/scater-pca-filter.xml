<tool id="scater_pca_filter" name="Scater: PCA filter" version="@TOOL_VERSION@">
    <description>Remove outlier cells from SCE with PCA</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements">
        <requirement type="package" version="2.0.9">r-mvoutlier</requirement>
    </expand>
    <command detect_errors="exit_code"><![CDATA[
Rscript '$__tool_directory__/scater-pca-filter.R'
-i '$input_loom'
-o '$output_loom'
    ]]></command>
    <inputs>
        <param name="input_loom" type="data" format="loom" label="Input SingleCellLoomExperiment dataset to filter" />
    </inputs>
    <outputs>
        <data name="output_loom" format="loom" label="${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <test>
            <param name="input_loom" value="scater_qcready.loom" ftype="loom" />
            <output name="output_loom" file="scater_pca_filtered.loom" compare="sim_size" />
        </test>
    </tests>
    <help><![CDATA[
Use PCA to automatically removes outliers from a SingleCellLoomExperiment object using Scater.
    ]]></help>
    <expand macro="citations" />
</tool>
