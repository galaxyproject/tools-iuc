name: pe_histogram
owner: iuc
description: Contains a tool that produces an insert size histogram for a paired-end BAM file.
homepage_url: https://github.com/seqcode/cegr-tools/tree/master/src/org/seqcode/cegrtools/pehistogram
long_description: |
  Contains a tool that produces an insert size histogram and basic statistics for a paired-end
  BAM file.  The tool generates 2 outputs: a png image consisting of the histogram of the insert
  size frequency and a tabular file containing the alignment statistics.
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/tools/pe_histogram
type: unrestricted
categories:
- Graphics
