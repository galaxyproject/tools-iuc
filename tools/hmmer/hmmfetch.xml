<?xml version="1.0"?>
<tool id="hmmer.hmmfetch" name="hmmfetch" version="@WRAPPER_VERSION@.0">
  <description>retrieve profile HMM(s) from a file</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <expand macro="stdio"/>
  <command><![CDATA[
hmmfetch -f $hmmfile $keyfile > $output;
      ]]></command>
  <inputs>
    <param name="hmmfile" type="data" label="HMM model"/>
    <param name="keyfile" type="data" label="List of HMM names to extract"/>
  </inputs>
  <outputs>
    <data format="txt" name="output" label="Selected HMM Models from $hmmfile.name">
    </data>
  </outputs>
  <tests>
    <test>
      <param name="hmmfile" value="fn3.hmm"/>
      <param name="keyfile" value="fn3.keys"/>
      <output name="output" file="fn3.hmm" lines_diff="4"/>
    </test>
  </tests>
<!-- TODO: implement auto-indexing -->
  <help><![CDATA[
@HELP_PRE@

Quickly retrieves one or more profile HMMs from an <hmmfile> (a large Pfam database, for example).

For maximum speed, the <hmmfile> should be indexed first, using hmmfetch --index. The index is a
binary file named <hmmfile>.ssi. However, this is optional, and retrieval will still work from unindexed files,
albeit much more slowly.

The default mode is to retrieve a single profile by name or accession, called the <key>. For example:

    % hmmfetch Pfam-A.hmm Caudal act
    % hmmfetch Pfam-A.hmm PF00045

By default, fetched HMMs are printed to standard output in HMMER3 format.

@ATTRIBUTION@
]]></help>
  <expand macro="citation"/>
</tool>
