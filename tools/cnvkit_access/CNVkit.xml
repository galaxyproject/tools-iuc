<tool id="CNVkit access" name="CNVkit access" version="0.9.9" python_template_version="3.5">
    <description>Calculate the sequence-accessible coordinates in chromosomes from the given reference genome, output as a BED file.</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[  
        ln -s '$fa_fname' ./genome.fasta &&
        cnvkit.py access
            ./genome.fasta
            --output '$output_setting.output'
    ]]></command>
        <configfiles>
        <configfile name="script_file"><![CDATA[
'$fa_fname'
--min-gap-size $min_gap_size
--exclude '$exclude'
--output '$output_setting.output'
        ]]></configfile>
    </configfiles>
    <inputs>
        <param name="fa_fname" type="data" format="fasta" multiple="false" label="Target coverage file" help="(.targetcoverage.cnn)." />
        <param argument="--min-gap-size" optional="true" type="float" label="Minimum gap size between accessible sequence regions." value="5000" help="Regions separated by less than this distance will be joined together." />
        <param argument="--exclude" type="data" optional="true" format="bed" multiple="false" label="EXCLUDE" help="Additional regions to exclude, in BED format. Can be used multiple times." />
        <section name="output_setting" title="Change output file name" expanded="false">
            <param argument="--output" optional="true" type="text" label="Output file name" value="access-excludes.bed" help="File_name.bed" />
        </section>
    </inputs>
    <outputs>
        <data name="out_sample_access" format="bed" label="${tool.name} on ${on_string}: Sequence-accessible coordinates" from_work_dir="access-excludes.bed" />
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="fa_fname" ftype="fasta" value="genome.fasta" />
            <param name="output" value="access-excludes.bed" />
            <output name="out_sample_access" file="access-excludes.bed" /> 
        </test>
    </tests>
    <help><![CDATA[
    Calculate the sequence-accessible coordinates in chromosomes from the given reference genome, output as a BED file.
    ]]></help>
    <citations>
        <citation type="doi">10.1371/journal.pcbi.1004873</citation>
    </citations>
</tool>
