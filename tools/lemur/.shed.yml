name: lemur
owner: iuc
description: "LEMUR - Latent Embedding Multivariate Regression"
long_description: |
  LEMUR is a framework for analyzing multi-condition single-cell RNA-seq data.
  This tool fits an interpretable latent embedding model of the data and tests for
  differential expression between two conditions, identifying neighborhoods of cells
  that share consistent differential expression patterns.

  The method combines matrix factorization and regression via geodesic regression
  on Grassmann manifolds—referred to as latent embedding multivariate regression.
  It disentangles the effects of known covariates, latent states, and their interactions,
  and validates differential expression using pseudo-bulk testing on held-out data
  via glmGamPoi, edgeR, or limma — the first two on raw counts, limma on the continuous assay.

  LEMUR simplifies the interpretation of complex, multi-condition single-cell datasets
  through a unified latent space representation and robust statistical testing.
homepage_url: https://github.com/const-ae/lemur
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/tools/lemur
type: unrestricted
categories:
- Single Cell
- Transcriptomics
- Statistics
