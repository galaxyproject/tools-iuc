<tool id="mags_visualization_drep_cluster_annot" name="MAGs-visualization drep-cluster-annot" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="22.05">
    <description>Create a dRep cluster plot with optional annotation</description>
    
    <macros>
        <import>macros.xml</import>
    </macros>
    
    <expand macro="requirements"/>

    <expand macro="version_command"/>

    <command detect_errors="exit_code"><![CDATA[
        mags-visualization drep-cluster-annot
            --drep '$drep'
            --gtdb '$gtdb'
            --top_n '$top_n'
            --tax_levels_space '$tax_levels_space'
            --format '$format'
            -o outputs

        #if $checkm2
            --checkm2 '$checkm2'
        #end if

        #if $quast
            --quast '$quast'
        #end if

        #if $bakta
            --bakta '$bakta'
        #end if

        #if len($tax_levels) > 0
            --tax_levels
            #for $tl in $tax_levels
                '$tl.level'
            #end for
        #end if

        #if str($fig_size.width) != "" and str($fig_size.height) != ""
            --fig_size '$fig_size.width' '$fig_size.height'
        #end if
    ]]></command>

    <inputs>
        <param name="drep" type="data" format="csv,tabular,tsv" label="dRep cluster table" />
        <param name="gtdb" type="data" format="tabular,tsv" label="GTDB annotation table" />

        <param name="checkm2" type="data" format="tabular,tsv" optional="true" label="CheckM2 table (optional)" />
        <param name="quast" type="data" format="tabular,tsv" optional="true" label="QUAST table (optional)" />
        <param name="bakta" type="data" format="tabular,tsv" optional="true" label="Bakta table (optional)" />

        <repeat name="tax_levels" title="Taxonomy levels" min="2">
            <param name="level" type="select" label="Taxonomic level">
                <option value="domain">domain</option>
                <option value="phylum" selected="true">phylum</option>
                <option value="class">class</option>
                <option value="order">order</option>
                <option value="family">family</option>
                <option value="genus" selected="true">genus</option>
                <option value="species">species</option>
            </param>
        </repeat>

        <param name="top_n" type="integer" value="30" min="1" label="Top N clusters" />
        <param name="tax_levels_space" type="float" value="0.3" label="Spacing between taxonomy columns" />

        <param name="format" type="select" label="Output format">
            <option value="png" selected="true">png</option>
            <option value="pdf">pdf</option>
            <option value="svg">svg</option>
        </param>

        <section name="fig_size" title="Figure size" expanded="false">
            <param name="width" type="float" optional="true" label="Figure width" />
            <param name="height" type="float" optional="true" label="Figure height" />
        </section>
    </inputs>

    <outputs>
        <collection name="plots" type="list" label="${tool.name} outputs">
            <discover_datasets directory="outputs" pattern="(?P&lt;designation&gt;.+)\.(?P&lt;ext&gt;png|pdf|svg)$" visible="true" />
        </collection>
    </outputs>

    <tests>
        <test>
            <param name="drep" value="drep.csv" />
            <param name="gtdb" value="gtdb.tsv" />
            <param name="format" value="png" />
            <param name="top_n" value="30" />
            <param name="tax_levels_0|level" value="phylum" />
            <param name="tax_levels_1|level" value="genus" />
            <output_collection name="plots" type="list">
                <element name="drep_cluster_top30_phylum-genus">
                    <assert_contents>
                        <has_size min="1" />
                    </assert_contents>
                </element>
            </output_collection>
        </test>

        <test>
            <param name="drep" value="drep.csv" />
            <param name="gtdb" value="gtdb.tsv" />
            <param name="checkm2" value="checkm2.tsv" />
            <param name="quast" value="quast.tsv" />
            <param name="bakta" value="bakta.tsv" />
            <param name="format" value="png" />
            <param name="top_n" value="30" />
            <param name="tax_levels_0|level" value="phylum" />
            <param name="tax_levels_1|level" value="genus" />
            <output_collection name="plots" type="list">
                <element name="drep_cluster_top30_phylum-genus">
                    <assert_contents>
                        <has_size min="1" />
                    </assert_contents>
                </element>
            </output_collection>
        </test>
    </tests>

    <help><![CDATA[
**MAGs-visualization: drep-cluster-annot**

Generates dRep cluster plots based on clustering results and GTDB taxonomy assignments.
Optional annotations from CheckM2, QUAST, and Bakta can be integrated into the plot.

Example output plots are available in the MAGs-visualization use cases:
https://github.com/usegalaxy-eu/MAGs-visualization/tree/main/use-cases

For a direct example (bee microbiome dataset):
https://github.com/usegalaxy-eu/MAGs-visualization/tree/main/use-cases/bee-use-case/plots   
    ]]></help>

    <expand macro="citation"/>
</tool>