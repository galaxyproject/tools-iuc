<tool id="ivar_trim" name="ivar trim" version="@VERSION@+galaxy0">
    <description>Trim reads in aligned BAM</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <expand macro="version_command" />
    <command><![CDATA[
        ln -s '$input_bed' bed.bed &&
        ln -s '$input_bam' sorted.bam &&
        ivar trim
        -i sorted.bam
        -b bed.bed
        -m $min_len
        -q $min_qual
        -s $window_width
        $inc_primers
        -p trimmed

    ]]>    </command>
    <inputs>
        <param name="input_bam" type="data" format="bam" label="Bam file" help="Aligned reads, to trim primers and quality"/>
        <param name="input_bed" type="data" format="bed" label="BED file with primer sequences and positions"/>
        <param name="min_len" type="integer" min="0" value="30" label="Minimum length of read to retain after trimming"/>
        <param name="min_qual" type="integer" min="0" value="20" label="Minimum quality threshold for sliding window to pass"/>
        <param name="window_width" type="integer" min="0" value="4" label="Width of sliding window"/>
        <param name="inc_primers" type="boolean" truevalue="-e" falsevalue="" checked="false" label="Include reads with no primers"/>
    </inputs>
    <outputs>
        <data name="output_bam" format="bam" label="${tool.name} on ${on_string} Trimmed bam" from_work_dir="trimmed.bam"/>
    </outputs>
    <tests>
        <!-- #1: SARS-Cov data-->
        <test>
            <param name="input_bam" value="covid19/PC00101P_sub.sorted.bam" />
            <param name="input_bed" value="covid19/ARTIC-V1.bed" />
            <param name="inc_primers" value="true" />
            <output name="output_bam" file="covid19/PC00101P_sub.sorted.bam" compare="sim_size" delta="300000"/>
        </test>
        <!-- #1: Zika data-->
        <test>
            <param name="input_bam" value="zika/Z52_a.sorted.bam" />
            <param name="input_bed" value="zika/db/zika_primers.bed" />
            <output name="output_bam" file="zika/Z52_a.trimmed.sorted.bam" compare="sim_size" delta="100000"/>
        </test>
        <test>
            <param name="input_bam" value="zika/Z52_b.sorted.bam" />
            <param name="input_bed" value="zika/db/zika_primers.bed" />
            <output name="output_bam" file="zika/Z52_b.trimmed.sorted.bam" compare="sim_size" delta="100000"/>
        </test>
    </tests>
    <help><![CDATA[

    ]]>    </help>
    <expand macro="citations" />
</tool>
