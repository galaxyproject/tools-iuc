<tool id="odgi_overlap" name="odgi overlap" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>find paths overlapped by query paths</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
odgi overlap
--input '$input'
#if $subset_paths
    --subset-paths '$subset_paths'
#end if
#if str($path)
    --path '$path'
#end if
#if $paths
    --paths '$paths'
#end if
#if $bed_input
    --bed-input '$bed_input'
#end if
@THREADS@
> '$output'
    ]]></command>
    <inputs>
        <expand macro="graph_input_param_input"/>
        <param argument="--subset-paths" type="data" format="txt" optional="true" label="Subset of paths considered" help="Text file with one path per line."/>
        <param argument="--path" type="text" optional="true" label="Query path name"/>
        <param argument="--paths" type="data" format="txt" optional="true" label="Paths to report overlaps for"/>
        <param argument="--bed-input" type="data" format="bed" optional="true" label="BED intervals on query paths"/>
    </inputs>
    <outputs>
        <expand macro="standard_tabular_output"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="note5_out.og" ftype="odgi"/>
            <param name="path" value="x"/>
            <output name="output" ftype="tabular">
                <assert_contents>
                    <has_size min="1"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help format="markdown"><![CDATA[
@HELP_HEADER@

`odgi overlap` lists paths touched by the queried paths or intervals.

### Inputs

- **Input odgi graph**: Graph to query.
- **Subset/path/path lists**: Limit overlap detection to selected paths.
- **BED intervals**: Query path ranges.

### Outputs

- **Tabular**: Overlap results in tab-delimited format.
    ]]></help>
    <expand macro="citations"/>
</tool>
