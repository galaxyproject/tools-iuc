name: chopin2
owner: iuc
categories:
  - Machine Learning
description: Domain-Agnostic Supervised Learning with Hyperdimensional Computing
long_description: |
  A Python tool for building classification models based on a supervised leraning 
  techniques which follows the hyperdimensional computing principles.
  It also implements a feature selection method based on the backward variable
  elimination strategy.
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/tools/chopin2
homepage_url: https://github.com/cumbof/chopin2
type: unrestricted
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  descriptor_template: "Galaxy wrapper for {{ tool_name }}"