<tool id="compleasm_miniprot" name="compleasm miniprot" profile="20.01" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@">
    <description></description>
    <macros>
        <import>macros.xml</import>
    </macros>

    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">compleasm</requirement>
    </requirements>
    <command><![CDATA[
    compleasm miniprot
    -a '$input'
    -p '$input_prot'
    -o galaxy



]]>    </command>
    <inputs>
        <param argument="-a" name="input" type="data" format="fasta" label="Input genome file"/>
        <param argument="-p" name="input_prot" type="data" format="faa" label="Input protein file"/>

    </inputs>

    <outputs>
          
    </outputs>
    
    <tests>
    </tests>
    <help><![CDATA[


    compleasm_ Using miniprot submodule to run miniprot alignment

    .. _compleasm: https://github.com/huangnengCSU/compleasm

    ]]>    </help>
</tool>