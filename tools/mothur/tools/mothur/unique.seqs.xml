<tool id="mothur_unique_seqs" name="Unique.seqs" version="@WRAPPER_VERSION@.0">
    <description>Return unique sequences</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />

    <command><![CDATA[
        echo "unique.seqs(
            #if $names:
                name="$names",
            #end if
            fasta="$fasta"
            )"
        | sed 's/ //g'  ## mothur trips over whitespace
        | mothur;

        ## move output files to correct destination
        prefix="$fasta";
        mv mothur.*.logfile "$logfile";
        mv \${prefix%.dat}*.unique.* "$out_fasta";
        mv \${prefix%.dat}*.names "$out_names";
    ]]></command>
    <inputs>
        <param name="fasta" type="data" format="fasta" label="fasta - Sequences to filter"/>
        <param name="names" type="data" format="names" optional="true" label="names - Sequences Names"/>
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="${tool.name} on ${on_string}: logfile" />
        <data format="fasta" name="out_fasta" label="${tool.name} on ${on_string}: fasta" />
        <data format="names" name="out_names" label="${tool.name} on ${on_string}: names" />
    </outputs>
    <tests>
        <test>
            <param name="fasta" value="amazon.fasta" />
            <param name="names" value="amazon.names" /> 
            <output name="out_fasta" file="amazon.unique.fasta" ftype="fasta" />
            <output name="out_names" file="amazon.unique.names" ftype="names" />
            <expand macro="logfile-test" />
        </test>
    </tests>
    <help>
<![CDATA[

@MOTHUR_OVERVIEW@

**Command Documenation**

The unique.seqs_ command returns only the unique sequences found in a fasta-formatted sequence file and a name_ file that indicates those sequences that are identical to the reference sequence. 

.. _name: http://www.mothur.org/wiki/Name_file
.. _unique.seqs: http://www.mothur.org/wiki/Unique.seqs

]]>
    </help>
    <expand macro="citations" />
</tool>
