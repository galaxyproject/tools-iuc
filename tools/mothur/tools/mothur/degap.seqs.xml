<tool id="mothur_degap_seqs" name="Degap.seqs" version="@WRAPPER_VERSION@.0">
    <description>Remove gap characters from sequences</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />

    <command><![CDATA[
        echo "degap.seqs(
            fasta="$fasta"
            )"
        | sed 's/ //g'  ## mothur trips over whitespace
        | mothur;

        ## move output files to correct destination
        prefix="$fasta";
        mv mothur.*.logfile "$logfile";
        mv \${prefix%.dat}*.ng.fasta "$out_fasta";
    ]]></command>
    <inputs>
        <param name="fasta" type="data" format="fasta" label="fasta - Dataset"/>
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="${tool.name} on ${on_string}: logfile" />
        <data format_source="fasta" name="out_fasta" label="${tool.name} on ${on_string}: fasta" />
    </outputs>
    <tests>
        <test>
            <param name="fasta" value="Mock_S280_L001_R1_001_small.trim.contigs.good.align_head" />
            <output name="out_fasta" file="Mock_S280_L001_R1_001_small.trim.contigs.good.ng.fasta"  />
            <expand macro="logfile-test" />
        </test>
    </tests>
    <help>
<![CDATA[

@MOTHUR_OVERVIEW@

**Command Documenation**

The degap.seqs_ command reads a fasta file and outputs a fasta containing the sequences after all gap characters are removed.

.. _degap.seqs: http://www.mothur.org/wiki/Degap.seqs

v1.21.0: Updated to Mothur 1.33

]]>
    </help>
    <expand macro="citations" />
</tool>
