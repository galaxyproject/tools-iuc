<tool id="mothur_get_group" name="Get.group" version="@WRAPPER_VERSION@.0">
    <description>group names from shared or from list and group</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />

    <command><![CDATA[
        echo "get.group(
            shared=$otu
            )"
        | sed 's/ //g'  ## mothur trips over whitespace
        | mothur
        | head -n-5
        | sed '1,29d'
        > "$bootgroups"; ## groups are printed to stdout, put in file

        ## move output files to correct destination
        cat "$bootgroups";
        mv mothur.*.logfile "$logfile";
    ]]></command>
    <inputs>
        <param name="otu" type="data" format="mothur.shared" label="shared - OTU Shared"/>
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="${tool.name} on ${on_string}: logfile" />
        <data format="mothur.groups" name="bootgroups" label="${tool.name} on ${on_string}: bootGroups" />
    </outputs>
    <tests>
        <test>
            <param name="otu" value="amazon.an.shared"  ftype="mothur.shared"/>
            <output name="bootgroups" md5="93b9f7ead2ee04f056e77b5bb94ea0dd" />
            <expand macro="logfile-test" />
        </test>
    </tests>
    <help>
<![CDATA[

@MOTHUR_OVERVIEW@

**Command Documenation**

The get.group_ command generate principle components plot data.

.. _get.group: http://www.mothur.org/wiki/Get.group

v.1.20.0: Updated to Mothur 1.33
]]>
    </help>
    <expand macro="citations" />
</tool>
