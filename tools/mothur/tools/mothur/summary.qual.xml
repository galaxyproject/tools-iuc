<tool id="mothur_summary_qual" name="Summary.qual" version="@WRAPPER_VERSION@.0">
    <description>Summarize the quality scores</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />

    <command ><![CDATA[
        echo "summary.qual(
            qfile=$qfile
            #if $name:
                ,name=$name
            #end if
            #if $count:
                ,count=$count
            #end if
            )"
        | sed 's/ //g'  ## mothur trips over whitespace
        | mothur;

        ## move output files to correct destination
        prefix="$qfile";
        mv mothur.*.logfile "$logfile";
        mv \${prefix%.dat}*.summary "$out_summary";
    ]]></command>
    <inputs>
        <param name="qfile" type="data" format="qual" label="qfile - Sequence Quality file"/>
        <param name="name" type="data" format="mothur.names" optional="true" label="name - Names"/>
        <param name="count" type="data" format="mothur.count_table" optional="true" label="count - a count_table" help="generated by count.seqs"/>
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="${tool.name} on ${on_string}: logfile" />
        <data format="mothur.summary" name="out_summary" label="${tool.name} on ${on_string}: summary" />
    </outputs>
    <tests>
        <test>
            <param name="qfile" value="Mock_S280_L001_R1_001_small.trim.contigs.qual" />
            <output name="out_summary" md5="04de7c9d0f3889d5e121db9d9070c935" />
            <expand macro="logfile-test" />
        </test>
    </tests>
    <help>
<![CDATA[

@MOTHUR_OVERVIEW@

**Command Documenation**

The summary.qual_ command reads a quality file and an optional name, and summarizes the quality information.

.. _summary.qual: http://www.mothur.org/wiki/Summary.qual
]]>
    </help>
    <expand macro="citations" />
</tool>
