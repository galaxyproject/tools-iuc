<tool id="mothur_reverse_seqs" name="Reverse.seqs" version="@WRAPPER_VERSION@.0">
    <description>Reverse complement the sequences</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />

    <command><![CDATA[
        echo "reverse.seqs(
            fasta="$fasta"
            )" 
        | sed 's/ //g'  ## mothur trips over whitespace
        | mothur;

        ## move output files to correct destination
        prefix="$fasta";
        mv mothur.*.logfile "$logfile";
        mv \${prefix%.dat}*.rc.* "$out_fasta";
    ]]></command>
    <inputs>
        <param name="fasta" type="data" format="fasta,align" label="fasta - Sequences to reverse complement"/>
    </inputs>
    <outputs>
        <data format="txt" name="logfile" label="${tool.name} on ${on_string}: logfile" />
        <data format_source="fasta" name="out_fasta" label="${tool.name} on ${on_string}: rc.fasta" />
    </outputs>
    <tests>
        <test>
            <param name="fasta" value="Mock_S280_L001_R1_001_small.trim.contigs.good.align_head" />
            <output name="out_fasta" file="Mock_S280_L001_R1_001_small.trim.contigs.good.rc.align_head"  />
            <expand macro="logfile-test" />
        </test>
    </tests>
    <help>
<![CDATA[

@MOTHUR_OVERVIEW@

**Command Documenation**

The reverse.seqs_ command will generate a fasta containing the reverse complement of each sequence in the input fasta.

.. _reverse.seqs: http://www.mothur.org/wiki/Reverse.seqs
]]>
    </help>
    <expand macro="citations" />
</tool>
