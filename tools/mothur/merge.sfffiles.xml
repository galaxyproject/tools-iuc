<tool profile="20.05" id="mothur_merge_sfffiles" name="Merge.sfffiles" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@">
    <description>Merge SFF files</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="stdio"/>
    <expand macro="version_command"/>
    <command><![CDATA[
@SHELL_OPTIONS@

## create symlinks to input datasets
#for $i in $sff:
    ln -s '$i' input${sff.index($i)}.dat &&
#end for

echo 'merge.sfffiles(
    sff=#for $i in $sff#input${sff.index($i)}.dat-#end for#,
    keytrim=$keytrim,
    output=$output
)'
| sed 's/ //g'  ## mothur trips over whitespace
| sed 's/-,/,/g'
| mothur
| tee mothur.out.log
    ]]></command>
    <inputs>
        <param name="sff" type="data" format="sff" multiple="true" label="input - sff files to merge"/>
        <param argument="keytrim" type="boolean" truevalue="true" falsevalue="false" checked="false" label="Keytrim" help="It allows 
            you to mergesff files with different keysequence by trimming them to the first 4 characters. Provided the first 4 match." />
        <expand macro="param-savelog"/>
    </inputs>
    <outputs>
        <expand macro="logfile-output"/>
        <data name="output" format="sff" label="${tool.name} on ${on_string}: merged sff"/>
    </outputs>
    <tests>
        <test>
            <param name="sff" value="Fasting_Example1.sff,Fasting_Example2.sff"/>
            <output name="output" ftype="sff">
                <assert_contents>
                    <has_text text="FLP3FBN01ELBSX"/>
                </assert_contents>
            </output>
            <param name="savelog" value="true"/>
            <expand macro="logfile-test"/>
        </test>
        <!-- Test keytrim parameter -->
        <test>
            <param name="sff" value="Fasting_Example1.sff,Fasting_Example2.sff"/>
            <param name="keytrim" value="true"/>
            <output name="output" ftype="sff">
                <assert_contents>
                    <has_text text="FLP3FBN01ELBSX"/>
                </assert_contents>
            </output>
            <param name="savelog" value="true"/>
            <expand macro="logfile-test"/>
        </test>
    </tests>
    <help><![CDATA[

@MOTHUR_OVERVIEW@

.. _Mothur: https://www.mothur.org/wiki/Main_Page

**Command Documentation**

The merge.sfffiles_ command merge inputs into a single output.

.. _merge.sfffiles: https://www.mothur.org/wiki/Merge.sfffiles

    ]]></help>
    <expand macro="citations"/>
</tool>
