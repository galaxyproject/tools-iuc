 strandedCounts
 filterWindows

 result2 <- consolidateSizes(dataList, resultList, equiweight=equiweight, merge.args= merge_args, region=regions, overlap.args=overlap)

 ##lapply(result[[1]], function(x) write.table(x, file="$id_output", quote = FALSE, row.names = FALSE, col.names = TRUE, append=TRUE))
##write.table(result[[2]], file="$region_output", quote = FALSE, row.names = FALSE)
##write.table(result[[3]], file="$table_output", row.names = FALSE)