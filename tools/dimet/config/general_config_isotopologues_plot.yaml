hydra:
  job:
    name: isotopologue_stacked_bar
  run:
    dir: outputs/${analysis.dataset.label}-${analysis.method.label}

# check https://hydra.cc/docs/patterns/specializing_config/ for more info about this
defaults:
  - analysis: isotopologues_stackedbars_example

figure_path: figures
table_path: tables



#palette: pastel # TODO : vverify, only used in abundance bars? or also in lineplots? 
#x_text: ''  # TODO : x_text param is only used in abundance bars, move there
