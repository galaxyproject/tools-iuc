<tool id="hyphy_strike_ambigs" name="Replace ambiguous codons" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>in a multiple alignment using HyPhy</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
        hyphy '$__tool_directory__/scripts/strike-ambigs.bf'
            --alignment '$alignment' --code '$gencodeid' --output '$output'
            > strike_ambigs_stdout.md
        @ERRORS@
        ]]></command>
    <inputs>
        <param  argument="--alignment" type="data" format="fasta" label="Input alignment" help="An in-frame codon alignment in FASTA format."/>
        <expand macro="gencode"/>
    </inputs>
    <outputs>
        <data name="output" format="fasta" label="Ambiguous codons replaced alignment on ${on_string}" />
        <data name="strike_ambigs_md_report" format="markdown" from_work_dir="strike_ambigs_stdout.md" label="Strike Ambigs Report (Markdown) for ${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="alignment" value="strike-ambigs-in1.fa"/>
            <param name="gencodeid" value="Universal"/>
            <output name="output">
                <assert_contents>
                    <has_text text="---"/>
                </assert_contents>
            </output>
            <output name="strike_ambigs_md_report">
                <assert_contents>
                    <has_text text="Striking 37 codons"/>
                </assert_contents>
            </output>
        </test>
        <test expect_num_outputs="2">
            <param name="alignment" value="strike-ambigs-in2.fa"/>
            <param name="gencodeid" value="Universal"/>
            <output name="output">
                <assert_contents>
                    <has_text text="---"/>
                </assert_contents>
            </output>
            <output name="strike_ambigs_md_report">
                <assert_contents>
                    <has_text text="Striking 15 codons"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
HyPhy Strike-Ambigs
===================

**What does this tool do?**

This tool reads an alignment of coding sequences and replaces any ambiguous codons with '---' (three hyphens). Ambiguous codons are those that cannot be unambiguously translated into a single amino acid, often due to sequencing errors or unresolved polymorphisms.

**Input**

- **Input alignment:** An in-frame codon alignment in FASTA format. It is crucial that the input alignment is correctly aligned along codon boundaries for accurate processing.

**Tool Options**

- **Genetic code:** Specifies which genetic code should be used for translation and codon ambiguity resolution.

**Output**

- **Ambiguous codons replaced alignment:** A FASTA format alignment where ambiguous codons have been replaced with '---'.
- **Strike Ambigs Report:** A Markdown report summarizing the analysis, including details on how many ambiguous codons were replaced.

**Further reading**

- https://www.ncbi.nlm.nih.gov/pubmed/15604363
    ]]></help>
    <expand macro="citations">
        <citation type="doi">10.1093/bioinformatics/bti079</citation>
    </expand>
</tool>
