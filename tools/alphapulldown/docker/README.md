# What is this?

AlphaPullDown docker is built from a modified version of the Alphafold docker created by [Cameron Hyde](https://github.com/usegalaxy-au/tools-au/tree/master/tools/alphafold/docker).

Build:

```sh
cd alphafold
docker build -f ../Dockerfile -t alphapulldown .
```
