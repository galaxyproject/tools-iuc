<tool id="alphapulldown" name="AlphaPullDown" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="20.01">
    <description>protein-protein interaction screens and high-throughput modelling of higher-order oligomers using AlphaFold-Multimer</description>
    <macros>
      <import>macros.xml</import>
    </macros>
    <xrefs>
      <xref type="bio.tools">alphapulldown</xref>
    </xrefs>
    <command detect_errors="exit_code"><![CDATA[
    mkdir -p './output_fies' &&
    create_individual_features.py
      --fasta_paths=$fasta_paths
      --data_dir=\${ALPHAFOLD_DB:-/data}/@TOOL_MINOR_VERSION@/
      --use_mmseqs2=$use_mmseqs2
      --save_msa_files=$save_msa_files
      --output_dir='./output_files'


    ]]></command>
    <inputs>
      <param argument="--fasta_paths" type="data" format="fasta" multiple="true" label="FASTA files" 
        help="FASTA files, each containing a prediction target that will be folded one after another. If a 
        FASTA file contains multiple sequences, then it will be folded as a multimer." />
        <param argument="--use_mmseqs2" type="boolean" truevalue="True" falsevalue="False" checked="false" label="Use MMSeqs2"/>
      <param argument="--save_msa_files" type="boolean" truevalue="True" falsevalue="False" checked="false" 
        label="Save MSA" help="Save MSA alignment outputs" />
      
    </inputs>

    <outputs>
    </outputs>

    <tests>
    </tests>
    <help><![CDATA[

.. class:: infomark

**Purpose**

AlphaPulldown is a Python package that streamlines protein-protein interaction screens and high-throughput modelling of higher-order oligomers using AlphaFold-Multimer:

- Provides a convenient command line interface to screen a bait protein against many candidates, calculate all-versus-all pairwise comparisons, test alternative homo-oligomeric states, and model various parts of a larger complex.
- Separates the CPU stages (MSA and template feature generation) from GPU stages (the actual modeling).
- Allows modeling fragments of proteins without recalculation of MSAs and keeping the original full-length residue numbering in the models.
- Summarizes the results in a CSV table with AlphaFold scores, pDockQ and mpDockQ, PI-score, and various physical parameters of the interface.
- Provides a Jupyter notebook for an interactive analysis of PAE plots and models.

    ]]></help>
    <expand macro="citations"/>
</tool>
