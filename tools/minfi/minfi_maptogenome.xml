<tool id="minfi_maptogenome" name="Minfi map to genome" version="@MINFI_VERSION@">
    <description>mapping Ilumina methylation array data to the genome</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code">
    <![CDATA[
     Rscript '$minfi_mtg_script'
    ]]>
    </command>
    <configfiles>
    <configfile name="minfi_mtg_script"><![CDATA[ 
require("minfi", quietly = TRUE)
require(IlluminaHumanMethylation450kanno.ilmn12.hg19, quietly = TRUE)

RSet <- get(load('$input1'))

GRSet <- mapToGenome(RSet)

save(GRSet,file = '$output1')
    ]]>
    </configfile>
    </configfiles>
    <inputs>
        <param type="data" name="input1" format="rdata" label= "Methylation Data to Map" />
    </inputs>
    <outputs>
        <data name="output1" format="rdata" from_work_dir="GRSet.rdata" label= "Data Mapped To The Genome" />
    </outputs>
    <tests>
        <test>
            <param name="input1" value="RSet.rdata"/>
            <output name="output1" file="GRSet.rdata"/>
        </test>
    </tests>
    <help><![CDATA[
        Mapping Ilumina methylation array data to the genome using an annotation package. Depending on the genome, not all methylation loci may have a genomic position.
    ]]></help>
    <expand macro="citations" />
</tool>
