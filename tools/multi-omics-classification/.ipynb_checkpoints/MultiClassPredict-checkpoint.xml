<tool id="MultiClassPredict" name="MultiClassPredict" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>compares OVO vs. OVR for multi-class classification and use AUC scores to measure model performance</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    
    <command detect_errors="exit_code"><![CDATA[
multiclasspredict.py
X = pd.DataFrame($X)
y = pd.Series($y, name="target")
seed_results = {}     

#for seed in $seeds:
    ks_results = {}
    #for k in $ks:

        print(f"CV for seed {seed} and {k} features")

        ## Define the classifier
        classifier = RandomForestClassifier(random_state=seed)

        ## Create a Random Forest Classifier
        rf = RandomForestClassifier(n_estimators=100, random_state=seed)

        ## Create a SelectFromModel using the Random Forest Classifier
        selector = SelectFromModel(rf, max_features = k)

        ##Create a pipeline with feature selection and classification
        pipeline = Pipeline(steps=[
            ('feature_selection', selector),
            ('classification', rf)
        ])

        ## Parameter grid for RandomForestClassifier
        p_grid = {
            "estimator__classification__n_estimators": [$p_grid.n_estimators],          # Number of trees in the forest
            "estimator__classification__max_features": ["$p_grid.max_features"],       # Feature selection strategy
            "estimator__classification__criterion": ["$p_grid.criteria"],       # Split criterion
            "estimator__classification__min_samples_leaf": [$p_grid.min_samples],        # Minimum samples per leaf
        }
            
        results = ovo_and_ova_multiclass_auc(X,y,pipeline, p_grid, random_state=seed)

        print(results)
        

        ks_results[k] = results

    seed_results[seed] = copy.copy(ks_results)
 
## Flatten the nested dictionary into a DataFrame
df = pd.DataFrame(
    {(outer_key, inner_key): values for outer_key, inner_dict in seed_results.items() for inner_key, values in inner_dict.items()}
).T

## Set multi-level index names for clarity
df.index.names = ['Seed', 'Features (k)']

## Display the DataFrame
print(df)
cp df $output

      

    ]]></command>
    <inputs>
        <param name="X" type="data" format="tabular" label="X: Input features" help="The input features (also called independent variables) that the model will use to make predictions. It can be DataFrame containing all the features (variables) you're training the model on."/>
        <param name="y" type="data" format="tabular" label="Y: target values" help="The output labels (also called dependent variable or target variable) that the model will try to predict. Usually a Series of the actual values you're predicting."/>
        <param argument="seeds" type="text" format="csv" label="list of seeds"/>
        <param argument="ks"   type="text" format="csv" label="feature counts" help="array of number of features you want to select for each run of the model. "/>
        <section name="p_grid" title="HyperParameter Grid" expanded="true">
            <param argument="n_estimators" type="integer" value="100"  label="Number of trees in the forest" />
            <param argument="max_features" type="text" value="sqrt"  label="Feature selection strategy" />
            <param argument="criteria" type="text" value="entropy"  label="Split criteria" />
            <param argument="min_samples" type="integer" value="3"  label="Minimum samples per leaf" />
        </section>   
    </inputs>
    <outputs>
        <data name="output" format="tabular"  label="dataframe of AUC results"/>
        
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="X" value="X.tsv"/>
            <param name="y" value="y.tsv"/>
            <param name="seeds" value="1,2,3"/> 
            <param name="ks" value="10,100"/> 
            <section name="p_grid">
                <param name="n_estimators" value="100"/>
                <param name="max_feautres" value="sqrt"/>
                <param name="criteria" value="entropy"/>
                <param name="min_samples" value="3"/>
                
            </section>
            <output name="output">
                <assert_contents>
                    <has_size value="1000" />
                </assert_contents>
            </output>
                  
        </test>
     </tests>   
    <help><![CDATA[
    
    ]]></help>
</tool>
