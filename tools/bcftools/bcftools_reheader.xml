<?xml version='1.0' encoding='utf-8'?>
<tool name="bcftools @EXECUTABLE@" id="bcftools_@EXECUTABLE@" version="@VERSION@.0">
  <description>Modify header of VCF/BCF files, change sample names.</description>
  <macros>
    <token name="@EXECUTABLE@">reheader</token>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements" />
  <expand macro="stdio" />
  <expand macro="version_command" />
  <command>bcftools @EXECUTABLE@

## Default section
#if $sec_default.header:
  --header "${sec_default.header}"
#end if
#if $sec_default.output:
  --output "${sec_default.output}"
#end if
#if $sec_default.samples:
  --samples "${sec_default.samples}"
#end if

## Primary Input/Outputs

$input_file
&gt;
$output_file</command>
  <inputs>
    <param name="input_file" label="VCF/BCF Data" type="data" format="vcf,bcf" />
    <section name="sec_default" expanded="true" title="Default Options">
      <param name="header" label="Header" type="data" format="data" optional="True" help="new header" />
      <param name="samples" label="Samples" type="data" format="data" optional="True" help="new sample names" />
    </section>
  </inputs>
  <outputs />
  <tests />
  <help>Modify header of VCF/BCF files, change sample names.</help>
  <expand macro="citations" />
</tool>