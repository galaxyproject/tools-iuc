<tool id="input_collection_list" name="input_collection_list" version="0.1.0" profile="22.01">
    <command><![CDATA[

    #for $sample in $hybpiper_results
        #set sample_name = $sample.element_identifier
        printf 'sample_name %s\n'
            '${sample_name}'
            >> '${out1}'
        &&
    #end for

    wait

 ]]></command>
    <inputs>
        <param name="hybpiper_results" format="txt" type="data_collection" collection_type="list" label="Unpaired reads" help="Why don't you update" />
    </inputs>
    <outputs>
        <data name="out1" label="${hybpiper_results.element_identifier}" format="txt"/>
    </outputs>
  </tool>