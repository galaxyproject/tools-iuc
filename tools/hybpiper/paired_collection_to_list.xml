<tool id="paired_collection_to_list" name="paired_collection_to_list" version="0.1.0" profile="22.01">
    <command><![CDATA[

    #set paired_collection_sample_name = $paired_input.element_identifier
    printf 'paired_collection_sample_name %s\n'
        '${paired_collection_sample_name}'
        >> '${out1}'
    &&

    ## #set unpaired_collection_sample_name = $unpaired_input.designation
    ## printf 'unpaired_collection_sample_name %s\n'
    ##     '${unpaired_collection_sample_name}'
    ##     >> '${out1}'
    ## &&

    wait

 ]]></command>
    <inputs>
        <param name="paired_input" format="fastqsanger" type="data_collection" collection_type="paired" label="Paired reads" help="Why don't you update" />
        <!-- <param name="unpaired_input" format="fastqsanger" type="data_collection" collection_type="list" label="Unpaired reads" help="Why don't you update" /> -->
    </inputs>
    <outputs>
        <data name="out1" format="txt"/>
    </outputs>
  </tool>