<tool id="obi_clean" name="obiclean" version="@TOOL_VERSION_OBI4@" profile="@PROFILE_OBI4@">
    <description>tags a set of sequences for PCR/sequencing errors identification</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements_obi4"/>
    <expand macro="stdio"/>

    <command><![CDATA[
        #if $save_graph or $save_ratio
            mkdir save_collection &&
        #end if

        obiclean
        --no-progressbar
        #if $distance
            -d '$distance'
        #end if
        #if $key
            -s '$key'
        #end if
        #if $ratio
            -r '$ratio'
        #end if
        ${head}
        $compress_bool
        #if $save_graph
            --save-graph save_collection/
        #end if
        #if $save_ratio
            --save-ratio save_collection/obiclean_ratio.csv.gz
            --min-eval-rate $min_eval_rate
        #end if
        @INPUT_FORMAT_OBI4@
        @OUT_FORMAT@
        $input 
        >'$output' 

        #if $save_ratio and $compress_bool != "--compressed"
            && gzip -d save_collection/obiclean_ratio.csv.gz
        #end if

        @GENERATE_GALAXY_JSON_OBI4@
    ]]></command>
    <inputs>
        <param name="input" type="data" format="@INPUT_FORMATS@" label="Input sequences file"/>
        <param name="distance" type="integer" value="1" optional="true" label="Maximum numbers of differences between two variant sequences (default: 1)"/>
        <param name="key" type="text" optional="true" label="Specify an attribute containing sample definition"/>
        <param name="ratio" optional="true" type="float" value="1" label="Threshold ratio between counts (rare/abundant counts) of two sequence records so that the less abundant one is a variant of the more abundant (default: 1, i.e. all less abundant sequences are variants)"/>
        <param name="head" type="boolean" checked="false" truevalue="-H" falsevalue="" label="Use this option to actually discarded sequence instead of annotate"/>
        <param name="detect_chimera" type="boolean" checked="false" truevalue="--detect-chimera" falsevalue="" label="enable chimera detection"/>
        <param name="min_sample_count" type="integer" value="1" label="minimum sample count" help="minimum number of samples a sequence must be present in to be considered in the analysis."/>
        <param name="save_graph" type="boolean" checked="false" label="Do you want to save the clustering graph ?" help=" save the clustering graph for each sample (PCR) in a GML file in the directory precised as parameter of the option"/>
        <section name="save_ratio_section" title="save ratio ?">
            <param name="save_ratio" type="boolean" checked="false" label="Do you want to save the abundance ratio statistics ?" help="create a CSV file named ratio.csv containing abundance ratio statistics for the edges of the clustering graphs above the min-eval-rate threshold."/>
            <param name="min_eval_rate" type="integer" optional="true" value="1000" label="the minimum abundance of the destination sequence of an edge to be stored in the CSV file produced by the --save-ratio option." />
        </section>
        <expand macro="compress_bool_macro"/>
        <expand macro="input_format_options_macro"/>
        <expand macro="out_format_macro"/>
    </inputs>
    <outputs>
        <data format="auto" name="output" label="output_obiclean"/>
        <collection name="output_collection_save" type="list" >
            <discover_datasets pattern="__name_and_ext__" visible="false" directory="save_collection"/>
        </collection>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input" value="output_obiannotate.fasta" />
            <param name="head" value="True"/>
            <param name="save_graph" value="True"/>
            <section name="save_ratio_section">
                <param name="save_ratio" value="True"/>
                <param name="min_eval_rate" value="75"/>
            </section>
            <output name="output" file="output_obiclean_simple.fasta" ftype="fasta" compare="sim_size"/>
            <output_collection name="output_collection_save" type="list" count="5"/>
        </test>
        <test expect_num_outputs="2">
            <param name="input" value="output_obiannotate.fasta.gz" />
            <param name="key" value="merged_sample"/>
            <param name="ratio" value="0.05"/>
            <param name="head" value="False"/>
            <section name="save_ratio_section">
                <param name="save_ratio" value="False"/>
            </section>
            <param name="compress_bool" value="True"/>
            <output name="output" file="output_obiclean_advanced.fasta.gz" ftype="fasta.gz" compare="sim_size"/>
        </test>
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

Obiclean implements the denoising algorithms provided by OBITools4.

The original obiclean algorithm is a denoising (clustering) algorithm designed to filter out potential PCR-generated spurious sequences.

This new version of obiclean adds two additional filters:

- A filter to set a threshold for the minimum number of samples (PCRs) a sequence must be present to be retained (default: 1, can be changed using the --min-sample-count option).
- A naive chimera detection algorithm. This is an experimental feature. It is not run by default. It can be enabled with the --dectect-chimera option.

Obiclean can run in two modes:

- A tagging mode where no sequences are actually removed from the data set, they are just tagged. It is your responsibility to remove the sequences you do not want based on these tags and your filter rules, using obigrep.

- A filter mode in which sequences that are considered to be artifactual sequences by obiclean are removed from the data set.

Considering S a sequence record, the following properties hold for S tagged as:

Obiclean relies on per-sample (PCR) sequence abundance information to apply its algorithms. Therefore, the input data set must first be dereplicated using the obiuniq command with the -m sample option.

By default, tagging is done once for the whole dataset, but it can also be done sample by sample by specifying the -s option. In such a case, the counts are extracted from the sample information.

Finally, each sequence record is annotated with three new attributes head, internal and singleton. The attribute values are the numbers of samples in which the sequence record has been classified in this manner.

@OBITOOLS4_IMAGE@

@OBITOOLS_LINK_OBI4@

        ]]>
    </help>
    <expand macro="citation"/>
    </tool>
