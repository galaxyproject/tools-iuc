<tool id="obiclean" name="obiclean" version="1.0.010">
    <description>tags a set of sequences for PCR/sequencing errors identification</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="stdio"/>

    <command>

        <![CDATA[
        obiclean

        #if str( '$options_dist.options_dist_selector' ) == "yes"
            -d '$options_dist.distance'
        #end if
        #if str( '$options_sample.options_sample_selector' ) == "yes"
            -s '$options_sample.key'
        #end if
        #if str( '$options_ratio.options_ratio_selector') == "yes"
            -r '$options_ratio.ratio'
        #end if
        ${head}

        '$input'>'$output'


        ]]>

    </command>

    <inputs>
        <param name="input" type="data" format="fasta" label="Input sequences file" />
        <conditional name="options_dist">
            <param name="options_dist_selector" type="select" label="Did you want to use the distance option?"  help="Use this option if you want to specify a maximum number of differences between two variant sequences" >
                <option value="yes">yes</option>
                <option value="no" selected="true">no</option>
            </param>
            <when value="yes">
                <param name="distance" type="integer" value="1" label="Maximum numbers of differences between two variant sequences (default: 1)" />
            </when>
            <when value="no"></when>
        </conditional>
        <conditional name="options_sample">
            <param name="options_sample_selector" type="select" label="Did you want to use the sample option?"  help="Use this option if you want to specify an attribute containing sample definition" >
                <option value="yes">yes</option>
                <option value="no" selected="true">no</option>
            </param>
            <when value="yes">
                <param name="key" type="text" label="key" />
            </when>
            <when value="no"></when>
        </conditional>
        <conditional name="options_ratio">
            <param name="options_ratio_selector" type="select" label="Did you want to use the ratio option?"  help="Use this option if you want to specify a threshold ratio between counts of two sequence records" >
                <option value="yes">yes</option>
                <option value="no" selected="true">no</option>
            </param>
            <when value="yes">
                <param name="ratio" type="float" value="1" label="Threshold ratio between counts (rare/abundant counts) of two sequence records so that the less abundant one is a variant of the more abundant (default: 1, i.e. all less abundant sequences are variants)" />
            </when>
            <when value="no"></when>
        </conditional>
        <param name="head" type="boolean" checked="false" truevalue="-H" falsevalue="" label="Did you want to select only sequences with the head status in a least one sample?" />
    </inputs>
    <outputs>
        <data format="fasta" name="output" label="output with ${tool.name} on ${on_string}" />
    </outputs>
    <tests>
        <test>
            <param name="input" value="output_obiannotate.fasta" />
            <conditional name="options_dist">
                <param name="options_dist_selector" value="no"/>
            </conditional>
            <conditional name="options_sample">
                <param name="options_sample_selector" value="no"/>
            </conditional>
            <conditional name="options_ratio">
                <param name="options_ratio_selector" value="no"/>
            </conditional>
            <param name="head" value="True"/>
            <output name="output" file="output_obiclean_simple.fasta" ftype="fasta"/>
        </test>
        <test>
            <param name="input" value="output_obiannotate.fasta" />
            <conditional name="options_dist">
                <param name="options_sample_selector" value="no"/>
            </conditional>
            <conditional name="options_sample">
                <param name="options_dist_selector" value="yes"/>
                <param name="key" value="merged_sample"/>
            </conditional>
            <conditional name="options_ratio">
                <param name="options_ratio_selector" value="yes"/>
                <param name="ratio" value="0.05"/>
            </conditional>
            <param name="head" value="False"/>
            <output name="output" file="output_obiclean_advanced.fasta" ftype="fasta"/>
        </test>
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

obijoinpairedend aims at joining the two reads of a paired-end library.

For this purpose, it concatenates sequence merging the forward read and the reversed-complemented reverse read.

The program uses as input one or two sequences reads files.

If two files are used one of them must be specified using the -r option. Sequence records corresponding to the same read pair must be in the same order in the two files.
If just one file is provided, sequence records are supposed to be all of the same length. The first half of the sequence is used as forward read, the second half is used as the reverse read.

@OBITOOLS_LINK@

        ]]>
    </help>
    <expand macro="citation" />
    </tool>
