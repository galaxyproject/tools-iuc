<tool id="obi_complement" name="obicomplement" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>
        Compute the reverse complement of the sequence entries.
    </description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="stdio"/>
    <command><![CDATA[
        obicomplement
        --no-progressbar
        @INPUT_FORMAT@
        @OUT_FORMAT@
        '$input'
        > '$output'
    ]]></command>
    <inputs>
        <param name="input" type="data" format="@INPUT_FORMATS@" label="Input sequences file"/>
        <expand macro="input_format_options_macro"/>
        <expand macro="out_format_macro"/>
    </inputs>
    <outputs>
        <data name="output" format_source="input">
            <expand macro="change_format_macro"/>
        </data>
    </outputs>
    <tests>
        <test>
            <param name="input" value="output_obipairing.fastqsanger" ftype="fastqsanger"/>
            <param name="out_format" value="fastq.gz"/>
            <output name="output" value="output_obicomplement.fastq.gz" ftype="fastq.gz"/>
        </test>
    </tests>
    <help><![CDATA[

    .. class:: infomark

    **What it does**

    Compute the reverse complement of the sequence entries.

    @OBITOOLS_LINK_OBI4@

    ]]>
    </help>
    <expand macro="citation" />

</tool>
