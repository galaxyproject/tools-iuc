<tool id="obi_convert" name="obiconvert" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>converts sequence files to different output formats</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="stdio"/>
    <command><![CDATA[ 
        obiconvert
        --no-progressbar
        @INPUT_FORMAT@
        @OUT_FORMAT@
        $input 
        > '${output}'
    ]]></command>
    <inputs>
        <param name="input" type="data" format="@INPUT_FORMATS@,txt,tabular" label="Input sequences file" help="database to convert"/>
        <expand macro="input_format_options_macro"/>
        <expand macro="out_format_macro"/>
    </inputs>
    <outputs>
        <data name="output" format_source="input">
            <expand macro="change_format_macro"/>
        </data>
    </outputs>
    <tests>
        <test>
            <param name="input" value="output_obimultiplex_error_3.fastqsolexa.gz" ftype="fastqsolexa.gz"/>
            <param name="out_format" value="fasta"/>
            <output name="output" file="output_obiconvert.fasta" ftype="fasta"/>
        </test>
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

obiconvert converts sequence files to different output formats. See the documentation for more details on the different formats.

Input files can be in :

fasta format

extended OBITools fasta format

fastq format

Solexa fastq format

ecoPCR format

ecoPCR database format

GenBank format

EMBL format

obiconvert converts those files to the :

extended OBITools fasta format

fastq format

ecoPCR database format

If no file name is specified, data is read from standard input.

@OBITOOLS_LINK_OBI4@

    ]]>

    </help>
    <expand macro="citation" />
</tool>
