owner: bgruening
description: Glimmer makes gene predictions.
homepage_url: https://ccb.jhu.edu/software/glimmer/
long_description: Glimmer makes gene predictions based on an interpolated context model (ICM).
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/tools/glimmer
type: unrestricted
categories:
- Sequence Analysis
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "{{ tool_name }} (from the Glimmer tool suite)"
suite:
  name: "suite_glimmer"
  description: Glimmer makes gene predictions based on an interpolated context model (ICM).
