<tool id="utils.subsample_n" name="utils.subsample_n" version="1.3.1+galaxy1">
  <description>Subsample n samples from rows of array.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc



sc.utils.subsample_n(
   X = '$X',
   seed = '$seed',)
]]></configfile>
  </configfiles>
  <inputs>
    <param name="X" type="data" format="np.ndarray" value="" label="X" help="    Data array."/>
    <param name="seed" type="data" format="int" value="" label="seed" help="    Seed for sampling."/>
    </inputs>
  <outputs>
    <data name="Xsampled" type="data" format="np.ndarray" label="${tool.name} on ${on_string}: Xsampled"/>
    <data name="rows" type="data" format="np.ndarray" label="${tool.name} on ${on_string}: rows"/>
    </outputs>
  <tests>
    <test>
      <param name="X" value=""/>
      <param name="seed" value=""/>
      <output name="Xsampled" file=""/>
      <output name="rows" file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Subsample n samples from rows of array.

Parameters
----------
X : np.ndarray
    Data array.
seed : int
    Seed for sampling.

Returns
-------
Xsampled : np.ndarray
    Subsampled X.
rows : np.ndarray
    Indices of rows that are stored in Xsampled.
    ]]></help>
  <expand macro="citations"/>
</tool>
