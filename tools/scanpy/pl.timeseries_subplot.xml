<tool id="pl.timeseries_subplot" name="pl.timeseries_subplot" version="1.3.1+galaxy1">
  <description>Plot X.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc
@CMD_set_figure_params


sc.pl.timeseries_subplot(
   X = '$X',)
]]></configfile>
  </configfiles>
  <inputs>
    <param name="X" type="data" format="np.ndarray" value="" label="X" help="    Call this with:    X with one column, color categorical.    X with one column, color continuous.    X with n columns, color is of length n."/>
    <section name="pl_settings" title="Plot settings" expanded="false"><expand macro="inputs_set_figure_params"/></section></inputs>
  <outputs>
    </outputs>
  <tests>
    <test>
      <param name="X" value=""/>
      <expand macro="tests_set_figure_params"/>
    </test>
  </tests>
  <help><![CDATA[
        Plot X.

Parameters
----------
X : np.ndarray
    Call this with:
    X with one column, color categorical.
    X with one column, color continuous.
    X with n columns, color is of length n.
    ]]></help>
  <expand macro="citations"/>
</tool>
