<tool id="datasets.blobs" name="datasets.blobs" version="1.3.1+galaxy1">
  <description>Gaussian Blobs.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc


sc.datasets.blobs(
   n_variables = '$n_variables',
   n_centers = '$n_centers',
   cluster_std = '$cluster_std',
   n_observations = '$n_observations',)

adata.write_loom($csv_output)
adata.write_csv($loom_output)
]]></configfile>
  </configfiles>
  <inputs>
    <param name="n_variables" type="int" value="" optional="true" label="n_variables" help="    Dimension of feature space."/>
    <param name="n_centers" type="int" value="" optional="true" label="n_centers" help="    Number of cluster centers."/>
    <param name="cluster_std" type="float" value="" optional="true" label="cluster_std" help="    Standard deviation of clusters."/>
    <param name="n_observations" type="int" value="" optional="true" label="n_observations" help="    Number of observations. By default, this is the same observation number as in    ``sc.examples.krumsiek11()``."/>
    </inputs>
  <outputs>
    <data name="csv_output" type="data" format="csv" label="${tool.name} on ${on_string}: Annotated matrix (csv)"/>
    <data name="loom_output" type="data" format="loom" label="${tool.name} on ${on_string}: Annotated matrix (loom)"/>
    </outputs>
  <tests>
    <test>
      <param name="n_variables" value=""/>
      <param name="n_centers" value=""/>
      <param name="cluster_std" value=""/>
      <param name="n_observations" value=""/>
      <output name="loom_output" file=""/>
      <output name="csv_output" file=""/>
      <output name="loom_output" file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Gaussian Blobs.

Parameters
----------
n_variables : `int`, optional (default: 11)
    Dimension of feature space.
n_centers : `int`, optional (default: 5)
    Number of cluster centers.
cluster_std : `float`, optional (default: 1.0)
    Standard deviation of clusters.
n_observations : `int`, optional (default: 640)
    Number of observations. By default, this is the same observation number as in
    ``sc.examples.krumsiek11()``.

Returns
-------
adata : :class:`~anndata.AnnData`
    Annotated data matrix containing a observation annotation 'blobs' that
    indicates cluster identity.
    ]]></help>
  <expand macro="citations"/>
</tool>
