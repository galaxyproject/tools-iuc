<tool id="tl.paga_degrees" name="tl.paga_degrees" version="1.3.1+galaxy1">
  <description>Compute the degree of each node in the abstracted graph.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc

@CMD_read_inputs

sc.tl.paga_degrees(
   adata = '$adata',)
]]></configfile>
  </configfiles>
  <inputs>
    <expand macro="inputs_anndata"/>
    </inputs>
  <outputs>
    <data name="degrees" type="data" format="list" label="${tool.name} on ${on_string}: degrees"/>
    </outputs>
  <tests>
    <test>
      <output name="degrees" file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Compute the degree of each node in the abstracted graph.

Parameters
----------
adata : AnnData
    Annotated data matrix.

Returns
-------
degrees : list
    List of degrees for each node.
    ]]></help>
  <expand macro="citations"/>
</tool>
