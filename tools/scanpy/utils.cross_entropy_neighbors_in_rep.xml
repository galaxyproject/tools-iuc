<tool id="utils.cross_entropy_neighbors_in_rep" name="utils.cross_entropy_neighbors_in_rep" version="1.3.1+galaxy1">
  <description>Compare neighborhood graph of representation based on cross entropy.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc



sc.utils.cross_entropy_neighbors_in_rep()

adata.write_loom($csv_output)
adata.write_csv($loom_output)
]]></configfile>
  </configfiles>
  <inputs>
    </inputs>
  <outputs>
    <data name="The cross entropy and the geodesic-distance-weighted cross entropy as" type="data" format="" label="${tool.name} on ${on_string}: The cross entropy and the geodesic-distance-weighted cross entropy as"/>
    <data name="``entropy, geo_entropy_d, geo_entropy_o``." type="data" format="" label="${tool.name} on ${on_string}: ``entropy, geo_entropy_d, geo_entropy_o``."/>
    <data name="csv_output" type="data" format="csv" label="${tool.name} on ${on_string}: Annotated matrix (csv)"/>
    <data name="loom_output" type="data" format="loom" label="${tool.name} on ${on_string}: Annotated matrix (loom)"/>
    </outputs>
  <tests>
    <test>
      <output name="The cross entropy and the geodesic-distance-weighted cross entropy as" file=""/>
      <output name="``entropy, geo_entropy_d, geo_entropy_o``." file=""/>
      <output name="loom_output" file=""/>
      <output name="csv_output" file=""/>
      <output name="loom_output" file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Compare neighborhood graph of representation based on cross entropy.

`n_points` denotes the number of points to add as highlight annotation.

Returns
-------
The cross entropy and the geodesic-distance-weighted cross entropy as
``entropy, geo_entropy_d, geo_entropy_o``.

Adds the most overlapping or disconnected points as annotation to `adata`.
    ]]></help>
  <expand macro="citations"/>
</tool>
