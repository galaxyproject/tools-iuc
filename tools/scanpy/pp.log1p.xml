<tool id="pp.log1p" name="pp.log1p" version="1.3.1+galaxy1">
  <description>Logarithmize the data matrix.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc

@CMD_read_inputs

sc.pp.log1p(
   data = '$adata',
   copy = '$copy',)
]]></configfile>
  </configfiles>
  <inputs>
    <expand macro="inputs_anndata"/>
    <param name="copy" type="bool" value="False" optional="true" label="copy" help="    If an :class:`~anndata.AnnData` is passed, determines whether a copy    is returned."/>
    </inputs>
  <outputs>
    <data name="Returns or updates `data`, depending on `copy`." type="data" format="" label="${tool.name} on ${on_string}: Returns or updates `data`, depending on `copy`."/>
    </outputs>
  <tests>
    <test>
      <param name="copy" value=""/>
      <output name="Returns or updates `data`, depending on `copy`." file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Logarithmize the data matrix.

Computes `X = log(X + 1)`, where `log` denotes the natural logarithm.

Parameters
----------
data : :class:`~anndata.AnnData`, `np.ndarray`, `sp.sparse`
    The (annotated) data matrix of shape `n_obs` × `n_vars`. Rows correspond
    to cells and columns to genes.
copy : `bool`, optional (default: `False`)
    If an :class:`~anndata.AnnData` is passed, determines whether a copy
    is returned.

Returns
-------
Returns or updates `data`, depending on `copy`.
    ]]></help>
  <expand macro="citations"/>
</tool>
