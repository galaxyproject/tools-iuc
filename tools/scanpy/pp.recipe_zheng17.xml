<tool id="pp.recipe_zheng17" name="pp.recipe_zheng17" version="1.3.1+galaxy1">
    <description>Normalization and filtering as of Zheng et al. (2017)</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
            python $script_file
        ]]></command>
    <configfiles>
        <configfile name="script_file"><![CDATA[
@CMD_imports@
@CMD_read_inputs@

sc.pp.recipe_zheng17(
   adata=adata,
   n_top_genes=$n_top_genes,
   log=$log,
   plot=False,
   copy=False)

@CMD_anndata_write_outputs@
]]></configfile>
    </configfiles>
    <inputs>
        <expand macro="inputs_anndata"/>
        <param name="n_top_genes" type="integer" value="1000" label="Number of genes to keep" help=""/>
        <param name="log" type="boolean" truevalue="True" falsevalue="False" checked="true" label="Take logarithm?" help=""/>
        <expand macro="anndata_output_format"/>
    </inputs>
    <outputs>
        <expand macro="anndata_outputs"/>
    </outputs>
    <tests>
        <test>
            <conditional name="input">
                <param name="format" value="h5ad" />
                <param name="adata" value="random-randint.h5ad" />
            </conditional>
            <param name="n_top_genes" value="1000"/>
            <param name="log" value="True"/>
            <param name="anndata_output_format" value="h5ad" />
            <output name="anndata_out_h5ad" file="pp.recipe_zheng17.random-randint.h5ad" ftype="h5" compare="sim_size"/>
        </test>
    </tests>
    <help><![CDATA[
Normalization and filtering as of Zheng et al. (2017) - the Cell Ranger R Kit of 10x
Genomics.

Expects non-logarithmized data. If using logarithmized data, pass `log=False`.

The recipe runs the following steps:

- only consider genes with more than 1 count
- normalize with total UMI count per cell
- select highly-variable genes
- subset the genes
- renormalize after filtering
- log transform (if needed)
- scale to unit variance and shift to zero mean

More details on the `scanpy documentation
<https://scanpy.readthedocs.io/en/latest/api/scanpy.api.pp.recipe_zheng17.html#scanpy.api.pp.recipe_zheng17>`__

    ]]></help>
    <expand macro="citations"/>
</tool>
