<tool id="pp.recipe_weinreb17" name="pp.recipe_weinreb17" version="1.3.1+galaxy1">
  <description>Normalization and filtering as of [Weinreb17]_.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc

@CMD_read_inputs

sc.pp.recipe_weinreb17(
   adata = '$adata',
   copy = '$copy',)
]]></configfile>
  </configfiles>
  <inputs>
    <expand macro="inputs_anndata"/>
    <param name="copy" type="data" format="bool " value="" label="copy" help="    Return a copy if true."/>
    </inputs>
  <outputs>
    </outputs>
  <tests>
    <test>
      <param name="copy" value=""/>
    </test>
  </tests>
  <help><![CDATA[
        Normalization and filtering as of [Weinreb17]_.

Expects non-logarithmized data. If using logarithmized data, pass `log=False`.

Parameters
----------
adata : :class:`~anndata.AnnData`
    Annotated data matrix.
copy : bool (default: False)
    Return a copy if true.
    ]]></help>
  <expand macro="citations"/>
</tool>
