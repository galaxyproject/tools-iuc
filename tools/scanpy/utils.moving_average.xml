<tool id="utils.moving_average" name="utils.moving_average" version="1.3.1+galaxy1">
  <description>Moving average over one-dimensional array.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc



sc.utils.moving_average(
   a = '$a',
   n = '$n',)
]]></configfile>
  </configfiles>
  <inputs>
    <param name="a" type="data" format="np.ndarray" value="" label="a" help="    One-dimensional array."/>
    <param name="n" type="data" format="int" value="" label="n" help="    Number of entries to average over. n=2 means averaging over the currrent    the previous entry."/>
    </inputs>
  <outputs>
    <data name="An array view storing the moving average." type="data" format="" label="${tool.name} on ${on_string}: An array view storing the moving average."/>
    </outputs>
  <tests>
    <test>
      <param name="a" value=""/>
      <param name="n" value=""/>
      <output name="An array view storing the moving average." file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Moving average over one-dimensional array.

Parameters
----------
a : np.ndarray
    One-dimensional array.
n : int
    Number of entries to average over. n=2 means averaging over the currrent
    the previous entry.

Returns
-------
An array view storing the moving average.
    ]]></help>
  <expand macro="citations"/>
</tool>
