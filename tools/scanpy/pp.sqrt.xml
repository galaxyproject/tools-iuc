<tool id="pp.sqrt" name="pp.sqrt" version="1.3.1+galaxy1">
  <description>Square root the data matrix.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc

@CMD_read_inputs

sc.pp.sqrt(
   data = '$adata',
   copy = '$copy',)
]]></configfile>
  </configfiles>
  <inputs>
    <expand macro="inputs_anndata"/>
    <param name="copy" type="bool" value="False" optional="true" label="copy" help="    If an :class:`~scanpy.api.AnnData` is passed, determines whether a copy    is returned."/>
    </inputs>
  <outputs>
    <data name="Returns or updates `data`, depending on `copy`." type="data" format="" label="${tool.name} on ${on_string}: Returns or updates `data`, depending on `copy`."/>
    </outputs>
  <tests>
    <test>
      <param name="copy" value=""/>
      <output name="Returns or updates `data`, depending on `copy`." file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Square root the data matrix.

Computes `X = sqrt(X)`.

Parameters
----------
data : :class:`~scanpy.api.AnnData`, `np.ndarray`, `sp.sparse`
    The (annotated) data matrix of shape `n_obs` × `n_vars`. Rows correspond
    to cells and columns to genes.
copy : `bool`, optional (default: `False`)
    If an :class:`~scanpy.api.AnnData` is passed, determines whether a copy
    is returned.

Returns
-------
Returns or updates `data`, depending on `copy`.
    ]]></help>
  <expand macro="citations"/>
</tool>
