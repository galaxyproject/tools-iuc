<tool id="tl.paga_expression_entropies" name="tl.paga_expression_entropies" version="1.3.1+galaxy1">
  <description>Compute the median expression entropy for each node-group.</description>
  <macros>
    <import>macros.xml</import>
  </macros>
  <expand macro="requirements"/>
  <command detect_errors="exit_code"><![CDATA[
        python $script_file
    ]]></command>
  <configfiles>
    <configfile name="script_file"><![CDATA[
import scanpy.api as sc

@CMD_read_inputs

sc.tl.paga_expression_entropies(
   adata = '$adata',)
]]></configfile>
  </configfiles>
  <inputs>
    <expand macro="inputs_anndata"/>
    </inputs>
  <outputs>
    <data name="entropies" type="data" format="list" label="${tool.name} on ${on_string}: entropies"/>
    </outputs>
  <tests>
    <test>
      <output name="entropies" file=""/>
    </test>
  </tests>
  <help><![CDATA[
        Compute the median expression entropy for each node-group.

Parameters
----------
adata : AnnData
    Annotated data matrix.

Returns
-------
entropies : list
    Entropies of median expressions for each node.
    ]]></help>
  <expand macro="citations"/>
</tool>
