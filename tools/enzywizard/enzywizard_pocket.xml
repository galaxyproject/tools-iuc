<tool id="enzywizard_pocket" name="EnzyWizard Pocket" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Detect and characterize binding pockets from a cleaned protein structure</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">enzywizard-pocket</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        exec 3>&1 4>&2;
        exec > enzywizard.log 2>&1;

        MSMS_EXE=\$(command -v msms || command -v msms.x86_64Linux2.2.6.1 || true);
        if [ -n "\$MSMS_EXE" ]; then mkdir -p "\$HOME"; echo "set pyvol_msms_exe, \$MSMS_EXE" > "\$HOME/.pymolrc"; fi;

        #set $input_name = $input_path.element_identifier
        ln -s '$input_path' '$input_name' &&
        enzywizard-pocket
        -i '$input_name'
        --min_rad '$advanced.min_rad'
        --max_rad '$advanced.max_rad'
        --min_volume '$advanced.min_volume'
        -o ./
        ;
        status=\$?;
        echo "---- files in Galaxy job working directory ----" >&4;
        find . -maxdepth 4 -type f | sort >&4;
        if [ "\$status" -ne 0 ]; then cat enzywizard.log >&4; exit "\$status"; fi;
        if ! find . -maxdepth 2 -type f -name 'pocket_report*.json' | grep -q .; then echo "No EnzyWizard Pocket output file was produced." >&4; cat enzywizard.log >&4; [ -f log.txt ] && cat log.txt >&4; exit 1; fi;

    ]]></command>
    <inputs>
        <expand macro="protein_input"/>
        <section name="advanced" title="Advanced options" expanded="false">
            <expand macro="pocket_params"/>
            <expand macro="show_log_param"/>
        </section>
    </inputs>
    <outputs>
        <expand macro="output_collection" name="output_files" label="${tool.name} outputs" pattern="(?P&lt;designation&gt;.+\.json)"/>
    
        <expand macro="log_output"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input_path" value="cleaned_1HVR.cif"/>
            <output_collection name="output_files" type="list" count="1"/>
        </test>
    </tests>
    <help><![CDATA[
Detect and characterize binding pockets from a cleaned protein structure. The wrapper symlinks Galaxy file inputs into the job working directory and captures the tool-created output directory/files as a Galaxy discovered collection.
    ]]></help>
    <expand macro="citations" repo_name="enzywizard-pocket"/>
    <expand macro="creator"/>
</tool>
