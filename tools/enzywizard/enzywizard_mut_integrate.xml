<tool id="enzywizard_mut_integrate" name="EnzyWizard Mut Integrate" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Integrate wild-type and mutant EnzyWizard JSON report directories</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">enzywizard-mut-integrate</requirement>
        <requirement type="package">unzip</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        ln -s '$mut_clean_report_path' mut_clean_report.json && mkdir -p wt_input mut_input && unzip -q '$wt_input_zip' -d wt_input && unzip -q '$mut_input_zip' -d mut_input &&
        enzywizard-mut-integrate
        -i mut_clean_report.json
        -w wt_input
        -m mut_input
        -wo wt_output
        -mo mut_output
        $strict
    ]]></command>
    <inputs>
        <param name="mut_clean_report_path" type="data" format="json" label="Mutation clean report"/>
        <expand macro="zip_input_dir" name="wt_input_zip" label="Wild-type report directory (zip)" help="Zip archive containing wild-type JSON reports." optional="false"/>
        <expand macro="zip_input_dir" name="mut_input_zip" label="Mutant report directory (zip)" help="Zip archive containing mutant JSON reports." optional="false"/>
        <expand macro="strict"/>
    </inputs>
    <outputs>
        <expand macro="output_collection_json" name="wt_output_files" label="${tool.name} wild-type outputs" directory="wt_output"/>
        <expand macro="output_collection_json" name="mut_output_files" label="${tool.name} mutant outputs" directory="mut_output"/>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="mut_clean_report_path" value="mut_clean_report_1ZG4_WT_to_1ZG6_S70G.json"/>
            <param name="wt_input_zip" value="wt_input.zip"/>
            <param name="mut_input_zip" value="mut_input.zip"/>
            <output_collection name="wt_output_files" type="list" min="1"/>
            <output_collection name="mut_output_files" type="list" min="1"/>
        </test>
    </tests>
    <help><![CDATA[
Integrate wild-type and mutant EnzyWizard JSON report directories. Directory inputs are represented as zip datasets and unpacked explicitly inside this wrapper command in the Galaxy job working directory. Outputs are exposed as discovered dataset collections.
    ]]></help>
    <expand macro="creator"/>
</tool>
