<tool id="enzywizard_embedding" name="EnzyWizard Embedding" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Generate protein sequence embeddings using ESM-2</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <requirements>
        <requirement type="package" version="@TOOL_VERSION@">enzywizard-embedding</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        @CMD_LINK_INPUT_FASTA@
        enzywizard-embedding
        @CMD_INPUT_FASTA@
        --model_name '$advanced.embedding_model_name'
        @CMD_OUTPUT_DIR@
        @CMD_LOG@
    ]]></command>
    <inputs>
        <expand macro="fasta_input"/>
        <section name="advanced" title="Advanced options" expanded="false">
            <expand macro="embedding_model_name"/>
            <expand macro="show_log_param"/>
        </section>
    </inputs>
    <outputs>
        <data name="out_json" format="json" label="${tool.name} JSON report"  from_work_dir="embedding_report_*.json" />
        <expand macro="log_output"/>
    </outputs>
    <tests>
        <test expect_num_outputs="2">
            <param name="input_fasta" value="cleaned_1HVR.fasta"/>
            <section name="advanced">
                <param name="show_log" value="true"/>
            </section>
            <output name="out_json">
                <assert_contents>
                    <has_text text="enzywizard_embedding" />
                    <has_text text="sequence_embeddings" />
                    <has_text text="residue_index" />
                    <has_text text="residue_name" />
                    <has_text text="residue_embedding" />
                    <!-- Check that the first residue is P (as in the example) -->
                    <has_text text="&quot;P&quot;" />
                </assert_contents>
            </output>
            <output name="log">
                <assert_contents>
                    <has_text text="Embedding processing started" />
                        <has_text text="Embedding processing finished" />
                    <not_has_text text="[ERROR]" />
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
**What it does**

EnzyWizard Embedding generates per-residue protein sequence embeddings from a cleaned FASTA sequence using an ESM-2 protein language model. The JSON report contains the raw floating-point embedding vector for each residue.

**Inputs**

* **Cleaned protein sequence**: FASTA sequence from EnzyWizard Clean.
* **ESM-2 model**: model size used for embedding generation. Larger models produce larger embeddings and generally need more memory and runtime.

**Outputs**

* JSON report named like ``embedding_report_*.json`` with residue embeddings.
* Optional log file.

**Downstream use**

Include the embedding report in EnzyWizard Integrate or Batch to add sequence-derived residue features to graph nodes.
    ]]></help>
    <expand macro="citations" repo_name="enzywizard-embedding"/>
    <expand macro="creator"/>
</tool>
