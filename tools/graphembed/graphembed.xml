<tool id="graphembed" name="GraphEmbed" version="@VERSION@.0" >
    <description>Compute a 2D embedding of a data matrix given supervised class information.</description>
    <macros>
        <token name="@VERSION@">2.4</token>
    </macros>
    <requirements>
        <requirement type="package" version="@VERSION@">graph_embed</requirement>
    </requirements>
    <version_command><![CDATA[graphembed --version]]></version_command>
    <command detect_errors='exit_code'><![CDATA[
    graphembed
 -i '${inp_csv}'
 -t '${inp_class}'
 -c ${class_conf}
 -k ${num_links_nnsame}
 -d ${num_links_dn_diffclass}
 -z ${horizon}
 -l ${num_mutual_nn}
#if '$advanced.option' == 'Yes'
 '$advanced.normalisation'
 '$advanced.feature_selection'
 '$advanced.correlation_transformation'
#end if
 --min_threshold='$advanced.min_thresh'
 --max_threshold='$advanced.max_thresh'
 --random_state='$advanced.rand_state'
## --figure_size='$advanced.fig_size'
 --cmap_name='$advanced.cmap_name'
 --verbose &&

 mv 2D_coords.txt '${out_coords}' &&
 mv target.txt '${out_target}' &&
 mv img_1_clean.pdf '${out_clean}' &&
 mv img_2_edges.pdf '${out_target}' &&
 mv img_3_edge.pdf '${out_edge}' &&
 mv img_4_hull.pdf '${out_hull}' &&
 mv img_5_hull_link.pdf '${out_hulllink}'

 ]]>
    </command>

    <inputs>
        <param name="inp_csv" type="data" format="csv" label="Count Matrix" />
        <param name="inp_class" type="data" format="csv" label="Class Matrix" />

        <param name="class_conf" type="float" value="1.0" label="Confidence bias for clustering" />
        <param name="num_links_nnsame" type="integer" value="3" label="Number of links towards closest neighbour with the same class" />
        <param name="num_links_dn_diffclass" type="integer" value="1" label="Number of links towards denser neighbours with a different class" />
        <param name="horizon" type="integer" value="5" label="Number of nearest neighbours to limit the horizon of where to search for denser neighbours of a different class" />
        <param name="num_mutual_nn" type="integer" value="3" label="Number of mutual nearest neighbours that define outlier instances." />

        <conditional name="advanced" >
            <param name="option" type="select" label="Use Default Parameters?">
                <option value="Yes" selected="true" >Yes</option>
                <option value="No" >No</option>
            </param>

            <when value="No" ></when>
            <when value="Yes" >
                <param argument="normalisation" type="boolean" optional="true" label="Convert data matrix to normalized matrix" />
                <param argument="feature_selection" type="boolean" optional="true" label="Select most discriminative features" />
                <param argument="correlation_transformation" type="boolean" optional="true" label="Convert data matrix to corr coeff matrix" />
                <param name="min_thresh" type="integer" value="5" label="Min num instances per class" />
                <param name="max_thresh" type="integer" value="300" label="Max num instances per class" />
                <param name="rand_state" type="integer" value="1" label="Random seed" />
                <param name="cmap_name" type="select" label="Color map" >
                    <option value="cubehelix" />
                    <option value="terrain" />
                    <option value="nipy_spectral" />
                    <option value="gist_stern" />
                    <option value="gist_ncar" selected="true" />
                </param>
            </when>
        </conditional>
    </inputs>

    <outputs>
        <data name="out_clean" format="pdf" label="${tool.name} on ${on_string}: Embed." />
        <data name="out_edges" format="pdf" label="${tool.name} on ${on_string}: Embed. + knn + k-shift" />
        <data name="out_edge" format="pdf" label="${tool.name} on ${on_string}: Embed + 1st shift" />
        <data name="out_hull" format="pdf" label="${tool.name} on ${on_string}: Embed + hulls + edges" />
        <data name="out_hulllink" format="pdf" label="${tool.name} on ${on_string}: Embed + hulls + 1st shift" />
        <data name="out_coords" format="tabular" label="${tool.name} on ${on_string}: 2D coords" />
        <data name="out_target" format="tabular" label="${tool.name} on ${on_string}: Target" />
    </outputs>

    <tests>
        <test>
            <param name="inp_csv" value="target.csv" />
            <param name="inp_class" value="prot_expression.csv" />
            <output name="out_clean" value="embed.pdf" />
            <output name="out_edges" value="embed_knn_k-shift.pdf" />
            <output name="out_edge" value="embed_1st_shift" />
            <output name="out_hull" value="embed_hulls_edges" />
            <output name="out_hulllink" value="embed_hulls_1st_shift" />
            <output name="out_coords" value="2d_coords.txt" />
            <output name="out_target" value="target.txt" />
        </test>
    </tests>
    <help>
============
GraphEmbed
============
*Compute a 2D embedding of a data matrix given supervised class information.*

Input: A discrete label for each instance is expected.

A graph is built where nodes are instances and there exist two types of edges:

 * 'knn' edges
    An edge to the k-th nearest instance that has the same label.
 * 'k_shift' edges
    An edge to the k-th nearest instance that is denser and has a different label

Density is defined as the sum of the pairwise cosine similarity between an instance and all the other instances. The desired edge length is the euclidean distance between the instances. If the endpoints of an edge have the same label then the desired distance is divided by 1 + class_confidence. A k-shift edge is deleted if at least one of the endpoints is an outlier. Outlier nodes are defined as those instances that have no mutual k neighbors.

Finally the embedding is computed as the 2D coordinates of the corresponding graph embedding using the force layout algorithm from Tomihisa Kamada, and Satoru Kawai. "An algorithm for drawing general undirected graphs.", Information processing letters 31, no. 1 (1989): 7-15.    </help>
    <citations>
        <citation type="doi">10.5281/zenodo.825832</citation>
    </citations>
</tool>
