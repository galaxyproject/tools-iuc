<?xml version="1.0"?>
<tool id="medaka_tool-vcf2tsv" name="medaka: VCF to TSV" version="@TOOL_VERSION@+@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>converter</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        ## initialize
        ln -s '$vcf' 'sample.vcf' &&

        ## run
        medaka tools vcf2tsv
        ## optional
        --debug
        ## required
        'sample.vcf'
        ]]></command>
    <inputs>
        <expand macro="vcf"/>
    </inputs>
    <outputs>
        <data name="out_result" format="tabular" from_work_dir="sample.vcf.tsv" label="${tool.name} on ${on_string}: Result"/>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <param name="vcf" value="sample.vcf"/>
            <output name="out_result">
                <assert_contents>
                    <has_n_lines n="201"/>
                    <has_line line="alt&#009;chrom&#009;pos&#009;qual&#009;ident&#009;filt&#009;ref&#009;GT"/>
                    <has_line line="A&#009;contig1&#009;1987&#009;8.0&#009;.&#009;.&#009;AACA&#009;1/1"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module converts a VCF to a tabular file.

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>