<?xml version="1.0"?>
<tool id="medaka_tool-resolve-model" name="medaka: Resolve model" version="@TOOL_VERSION@+@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>and print full file path</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        medaka tools resolve_model
        ## optional
        --debug
        @MODEL@
        $op.disable_cudnn
        ## required
        |& tee result.txt
        ]]></command>
    <inputs>
        <section name="op" title="Optional parameters" expanded="true">
            <expand macro="disable_cudnn"/>
            <expand macro="model"/>
        </section>
    </inputs>
    <outputs>
        <data name="out_result" format="txt" from_work_dir="result.txt" label="${tool.name} on ${on_string}: Result"/>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <output name="out_result">
                <assert_contents>
                    <has_n_lines n="1"/>
                    <!-- file path -->
                    <has_text_matching expression=".+"/>
                </assert_contents>
            </output>
        </test>
        <!-- #2 -->
        <test expect_num_outputs="1">
            <section name="op">
                <param name="disable_cudnn" value="true"/>
                <param name="model" value="r941_prom_snp_g303"/>
            </section>
            <output name="out_result">
                <assert_contents>
                    <has_n_lines n="1"/>
                    <!-- file path -->
                    <has_text_matching expression=".+r941_prom_snp_g303"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module resolves a model and prints the full file path.

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>