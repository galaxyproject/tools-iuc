<?xml version="1.0"?>
<tool id="medaka_variant" name="medaka: Variant" version="@TOOL_VERSION@+@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>to decode probabilities to VCF</description>    
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        medaka variant
        ## optional
        --debug
        #if $region
            --region '${region}'
        #end if
        $verbose
        ## required
        '$ref_fasta'
        #for $current in $inputs
            '$current'
        #end for
        'result.vcf' ## output
        |& tee log.txt &&
        ls -lisa
        ]]></command>
    <inputs>
        <param argument="ref_fasta" type="data" format="fasta" label="Select reference sequence"/>
        <param argument="inputs" type="data" format="hdf" multiple="true" label="Select consensus file(s)"/>
        <param argument="--region" type="text" value="" optional="true" label="Set reference names" help="Separated by ','."/>
        <param argument="--verbose" type="boolean" truevalue="--verbose" falsevalue="" label="Populate VCF info fields?"/>            
        <param name="out" type="select" multiple="true" optional="false" label="Select output file(s)">
            <option value="result" selected="true">Result</option>
            <option value="log">Log</option>
        </param>
    </inputs>
    <outputs>
        <data name="out_result" format="vcf" from_work_dir="result.vcf" label="${tool.name} on ${on_string}: Result">
            <filter>'result' in out</filter>
        </data>
        <data name="out_log" format="tabular" from_work_dir="log.txt" label="${tool.name} on ${on_string}: Log">
            <filter>'log' in out</filter>
        </data>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <param name="ref_fasta" value="reference.fasta"/>  
            <param name="inputs" value="consensus.hdf"/>         
            <output name="out_result">
                <assert_contents>
                    <has_n_lines n="6"/>
                    <has_line line="##fileformat=VCFv4.1"/>
                </assert_contents>
            </output>            
        </test>    
        <!-- #2 -->
        <test expect_num_outputs="2">
            <param name="ref_fasta" value="reference.fasta"/>
            <param name="inputs" value="consensus.hdf"/>
            <param name="verbose" value="true"/>
            <param name="out" value="result,log"/>
            <output name="out_result">
                <assert_contents>
                    <has_n_lines n="13"/>
                    <has_line line="##fileformat=VCFv4.1"/>
                </assert_contents>
            </output>   
            <output name="out_log">
                <assert_contents>
                    <has_n_lines n="17"/>
                    <has_text_matching expression=".+Loaded.+"/>
                </assert_contents>
            </output>            
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module decodes probabilities to VCF.

**Input**

- reference sequence (FASTA)
- (several) consensus files (H5)

**Output**

- decoded probabilities (VCF)

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>