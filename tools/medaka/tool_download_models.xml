<?xml version="1.0"?>
<tool id="medaka_tool-download-models" name="medaka: Download models" version="@TOOL_VERSION@+@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>that are not already installed</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        medaka tools download_models
        ## optional
        --debug
        ## required        
        |& tee log.txt
        ]]></command>
    <inputs>
        <section name="ap" title="Advanced parameters" expanded="true">
            <param name="out" type="select" multiple="true" optional="false" label="Select output file(s)" help="">
                <option value="log" selected="true">Log</option>
            </param>
        </section>
    </inputs>
    <outputs>
        <data name="out_log" format="tabular" from_work_dir="log.txt" label="${tool.name} on ${on_string}: Log">
            <filter>'log' in ap['out']</filter>
        </data>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <param name="out" value="log"/>
            <output name="out_log">
                <assert_contents>
                    <has_size value="502"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module download model files for any models not already installed.

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>