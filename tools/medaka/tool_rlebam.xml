<?xml version="1.0"?>
<tool id="medaka_tool-rlebam" name="medaka: rlebam" version="@TOOL_VERSION@+@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>adds RLE tags from HDF to BAM</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        medaka tools rlebam
        ## optional
        --workers \${GALAXY_SLOTS:-4}
        ## required
        '$read_index'
        |& tee result.txt
        ]]></command>
    <inputs>
        <param argument="read_index" type="data" format="tabular" label="Select input file"/>
    </inputs>
    <outputs>
        <data name="out_result" format="tabular" from_work_dir="result.txt" label="${tool.name} on ${on_string}: Result"/>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="1">
            <param name="read_index" value=""/>
            <output name="out_result">
                <assert_contents>
                    <has_text_matching expression=".+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module adds RLE tags from HDF to BAM.

**Input**

Requires a two column tabular file with mapping of read_ids to HDF filepaths."

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>