<?xml version="1.0"?>
<tool id="medeka_compressbam" name="medeka: Compress BAM" version="@TOOL_VERSION@+@WRAPPER_VERSION@">
    <description> into an RLE system of reference</description>    
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>                                
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        medaka compress_bam
        --debug
        --threads \${GALAXY_SLOTS:-4}
        --regions '$ap.regions_cond.custom'
        '$bam_input'
        'result.bam' ## bam_output
        '$ref_fname'
        |& tee log.txt
        ]]></command>
    <inputs>
        <param argument="bam_input" type="data" format="bam" label="Select file to compress"/>
        <param argument="ref_fname" type="data" format="fasta" label="Select reference file"/>

        <section name="ap" title="Advanced parameters">
            <conditional name="regions_cond" label="Genomic regions to analyse">
                <param name="regions_sel" type="select" label="Choose input type">
                    <option value="custom">Custom input</option>
                    <option value="bed">BED file</option>             
                </param>
                <when value="custom">
                    <param argument="custom" type="text" value="" optional="true" label="Set genomic regions to analyse"/>
                </when>
                <when value="bed">
                    <param argument="bed" type="data" format="bed" optional="true" multiple="true" label="Select file with genomic regions to analyse"/>
                </when>
            </conditional>
            <param name="out" type="select" multiple="true" optional="false" label="Select output file(s)" help="">
                <option value="result" selected="true">Result</option>
                <option value="log">Log</option>
            </param>
            <repeat name="fast5" title="Fast5 Info">
                <param argument="--use_fast5_info" type="data" format="fast5" label="Select fast5 file"/>
                <param argument="--use_fast5_info" type="data" format="tabular" label="Select file with columns filename and read_id"/>
            </repeat>
        </section>
    </inputs>
    <outputs>                                                                                          
        <data name="out_log" format="tabular" from_work_dir="log.txt" label="${tool.name} on ${on_string}: Log">
            <filter>'log' in ap['out']</filter>
        </data>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="2">
            <param name="bam_input" value=""/>
            <param name="ref_fname" value=""/>        
            <output name="out_log">
                <assert_contents>
                    <has_n_lines n=""/>
                    <has_line line=""/>
                    <has_text_matching expression=""/>
                    <has_size value="" delta=""/>
                </assert_contents>
            </output>            
        </test>                                                   
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

This module can be used to compress Bam into an RLE system of references.

**Input**

...


**Output**

...

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>