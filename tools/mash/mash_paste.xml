<tool id="mash_paste" name="mash paste" version="@TOOL_VERSION@+galaxy1" profile="@PROFILE@">
    <description>Create a single sketch file from multiple sketch files.</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="xref"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
        @SAFE_NAME_HELPERS@
        #set $list_inputs = []
        #for $i, $file in enumerate($msh_files)
            #set $safe_identifier = $safe_name($file.element_identifier) + '_' + str($i)
            ln -s '$file' '${safe_identifier}.${file.ext}' &&
            #silent $list_inputs.append($safe_identifier + '.' + str($file.ext))
        #end for
        mash paste result.msh
        #for $name in $list_inputs
            '$name'
        #end for
        && mv result.msh '$output'
            ]]></command>
    <inputs>
        <param name="msh_files" type="data" format='msh' multiple="true" label="Sketch files to merge"/>
    </inputs>
    <outputs>
        <data name="output" format="msh" label="${tool.name} on ${on_string}: Output Sketch File"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="msh_files" value="test_01_mash_sketch.msh,test_02_mash_sketch.msh"/>
            <output name="output" file="res_paste.msh" compare="sim_size" />
        </test>
        <test expect_num_outputs="1">
            <param name="msh_files">
                <collection type="list">
                    <element name="-weird name (v2)" value="test_01_mash_sketch.msh" ftype="msh"/>
                    <element name="-weird+name (v2)" value="test_02_mash_sketch.msh" ftype="msh"/>
                </collection>
            </param>
            <output name="output" file="res_paste.msh" compare="sim_size" />
        </test>
    </tests>
    <help><![CDATA[
	Create a single sketch file from multiple sketch files.
  ]]></help>
    <expand macro="citations"/>
</tool>