<tool id="onto_tk_obo2tran" name="Convert OBO to RDF" version="@VERSION@.0">
  <description>with transitive closure</description>
    <expand macro="requirements" />
    <expand macro="stdio" />
    <macros>
        <import>onto_macros.xml</import>
    </macros>
  <command>
    obo2tran.pl
        $input_ontology
        "$url" > $output
  </command>
  <inputs>
    <expand macro="input_ontology" />
    <param name="url" type="text" size="50" value="http://" label="URL" />
  </inputs>
  <outputs>
    <data format="rdf" name="output" />
  </outputs>
  <tests>
    <test>
      <param name="input" value="pre_cco_core.obo"/>
      <output name="out_file" file="pre_cco_core_tran.rdf"/>
    </test>
  </tests>

  <help>
This tool transforms an OBO formatted ontology (such as the Gene Ontology) to an RDF/XML one that has (full) transitive closure.

    </help>
    <expand macro="citations" />
</tool>

