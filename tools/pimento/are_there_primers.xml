<tool id="pimento_are_there_primers" name="PIMENTO Are there primers?" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" license="@LICENSE@">
    <description>Checks for the presence of primers in input reads</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="creators"/>
    <expand macro="requirements"/>
    <command detect_errors="aggressive"><![CDATA[
        pimento are_there_primers -i '$input' -o sample
    ]]></command>
    <inputs>
        <param name="input" type="data" label="Input reads" format="fastqsanger.gz"/>
    </inputs>    
    <outputs>
        <data name="output" label="${tool.name} on ${on_string}: Primer presence" format="txt" from_work_dir="sample_general_primer_out.txt"/>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="test.fastq.gz"/>
            <output name="output">
                <assert_contents>
                    <has_line line="1"/>
                    <has_n_lines n="2"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[ 
        Checks for the presence of primers in input reads. Using patterns of base-
        conservation values for input reads, this function makes a prediction for
        whether there are primers at either ends of reads. The flag for whether
        primers are detected (1 for yes, 0 for no) for both strands is saved to an
        output file.
    ]]></help>
    <expand macro="citations"/>
</tool>
