<tool id="mgnify_pipelines_toolkit_mapseq_to_asv_table" name="MGnify: MAPseq to ASV taxonomy table" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@" license="@LICENSE@">
    <description>Convert MAPseq taxonomic assignments into a structured ASV taxonomy table</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="creators"/>
    <expand macro="requirements"/>
    <command detect_errors="aggressive"><![CDATA[
        mapseq_to_asv_table -i '$input' -l '$label' -s sample
    ]]></command>
    <inputs>
        <param name="input" type="data" label="MAPseq classification output" format="tabular"/>
        <param argument="--label" type="select" label="Reference taxonomy rank schema" help="Taxonomy rank schema used by the reference database. Different databases use different rank hierarchies, for more information see help section below">
            <option value="DADA2-SILVA">SILVA</option>
            <option value="DADA2-PR2">PR2</option>
        </param>
    </inputs>    
    <outputs>
        <data name="silva_output" label="${tool.name} on ${on_string}: SILVA ASV taxonomy table" format="tsv" from_work_dir="sample_DADA2-SILVA_asv_taxa.tsv">
            <filter>label == "DADA2-SILVA"</filter>
        </data>
        <data name="pr2_output" label="${tool.name} on ${on_string}: PR2 ASV taxonomy table" format="tsv" from_work_dir="sample_DADA2-PR2_asv_taxa.tsv">
            <filter>label == "DADA2-PR2"</filter>
        </data>
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="input" value="test.mseq" ftype="tabular"/>
            <param name="label" value="DADA2-SILVA"/>
            <output name="silva_output" file="sample_DADA2-SILVA_asv_taxa.tsv" ftype="tsv"/>
        </test>
        <test expect_num_outputs="1">
            <param name="input" value="test.mseq" ftype="tabular"/>
            <param name="label" value="DADA2-PR2"/>
            <output name="pr2_output" file="sample_DADA2-PR2_asv_taxa.tsv" ftype="tsv"/>
        </test>
    </tests>
    <help>
<![CDATA[ 

**Convert MAPseq Output to ASV Taxonomy Table**
-----------------------------------------------

This tool converts **MAPseq taxonomic classification output** into a structured **ASV taxonomy table** suitable for downstream microbial community analysis.

**SILVA taxonomy schema**

Superkingdom → Kingdom → Phylum → Class → Order → Family → Genus → Species

**PR2 taxonomy schema**

Domain → Supergroup → Division → Subdivision → Class → Order → Family → Genus → Species
]]>
    </help>
    <expand macro="citations" />
</tool>
