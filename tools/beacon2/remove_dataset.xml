<tool id="beacon2_remove_dataset" name="Beacon2 remove dataset" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>remove records for one dataset from MongoDB (one collection or all)</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <expand macro="creators"/>
    <expand macro="stdio"/>
    <expand macro="version_command"/>

    <command detect_errors="exit_code"><![CDATA[
@BEACON2_RUNTIME_SETUP@ &&
python ./beacon2_runtime/remove_dataset.py \
    --datasetId '${dataset_id}' \
    > output.log 2>&1
    ]]></command>

    <!-- SINGLE <configfiles> SECTION -->
    <configfiles>
        <!-- Tool-specific conf.py – entry_type is now controlled by the user -->
        <configfile name="conf_py"><![CDATA[
@BEACON2_CONF_BASE_ADVANCED@

# Tool-specific settings for remove_dataset
# User can choose 'all' (delete from every collection) or a specific collection
record_type = 'genomicVariation'
collection_name = 'genomicVariations'
        ]]></configfile>

        <!-- Credentials from user preferences -->
        <configfile name="credentials"><![CDATA[
#set $db_auth_source = $__user__.extra_preferences.get('beacon2_account|db_auth_source', "")
#set $db_user = $__user__.extra_preferences.get('beacon2_account|db_user', "")
#set $db_password = $__user__.extra_preferences.get('beacon2_account|db_password', "")
#if $db_user == "" or $db_password == "" or $db_auth_source == "":
    #set $db_auth_source = "admin"
    #set $db_user = "root"
    #set $db_password = "example"
#end if
{
    "db_auth_source": "$db_auth_source",
    "db_user": "$db_user",
    "db_password": "$db_password"
}
        ]]></configfile>
    </configfiles>

    <inputs>
        <expand macro="dataset_id"/>
        <!-- Full advanced configuration – user can now select entry_type -->
        <expand macro="advanced_section"/>
    </inputs>

    <outputs>
        <expand macro="log_output"/>
    </outputs>

    <tests>
        <test>
            <param name="dataset_id" value="test_dataset_to_remove" />
            <!-- Default test uses the most common/safe option -->
            <param name="advanced|entry_type" value="all" />

            <output name="log" compare="contains">
                <assert_contents>
                    <has_text text="remove" />
                    <has_text text="dataset" />
                    <has_text text="completed" />
                    <has_text text="success" />
                </assert_contents>
            </output>
        </test>
    </tests>

    <help><![CDATA[
**Beacon2 remove dataset**

**⚠️ Destructive operation** — This tool permanently deletes all records belonging to the specified `datasetId`.

You can choose in the **Advanced section**:
- `all` → delete the dataset from **every collection** (individuals, biosamples, genomicVariations, cohorts, etc.)
- or a specific collection (e.g. `individuals`, `genomicVariations`, `cohorts`, …)

**Use with extreme caution** — this action cannot be undone.

**New features (Advanced section):**
- Choose which collection(s) to clean (`all` or specific type)
- Full custom `conf.py` support (highest priority)
- Verbose logging

MongoDB credentials are read automatically from your Galaxy user preferences.
    ]]></help>
    <expand macro="citations"/>
</tool>