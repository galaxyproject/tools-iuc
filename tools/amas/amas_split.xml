<tool id="amas_split" name="AMAS split" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="25.0">
    <description>split multiple alignments</description>
    
    <macros>
        <import>macros.xml</import>
    </macros>

    <xrefs>
        <xref type="bio.tools">amas</xref>
    </xrefs>

    <expand macro="requirements" />
    <expand macro="version_command" />

    <command detect_errors="exit_code"><![CDATA[
        #import re
        set -eu;

        rm -rf "run_dir/action";
        mkdir -p "run_dir/action";

        ## Create symlinks with original filename for consistent tests because
        ##  input filenames are used as str vars
        #set $safename_input = re.sub('[^\w\-_\.]', '_', $input_file.element_identifier)
        ln -s '${input_file}' "run_dir/action/${safename_input}";

        cd "run_dir/action";

        python -m amas.AMAS
        split
        --split-by '$split_by'
        $remove_empty
        --out-format $out_format
        --in-files $safename_input
        --in-format $in_format
        --data-type $data_type
        --cores "\${GALAXY_SLOTS:-1}"
        $check_align
        ]]></command>

    <inputs>
        <param name="input_file" type="data" format="fasta,phylip,nex" label="Sequence to split" multiple="false" help="Provide pre-aligned FASTA/PHYLIP/NEXUS files (DNA or protein); mixes of unaligned reads or contigs will produce meaningless results." />
        <expand macro="input_format" />
        <expand macro="output_format" label="Format of the split alignments output" />
        <param name="split_by" type="data" format="txt,nex" label="Partitions file for splitting" 
               help="A file defining how to split the concatenated alignment into separate gene/locus regions. Each line specifies a partition name and its position range (e.g., 'gene1 = 1-500' or 'DNA, gene1 = 1-500' for RAxML format)." />
        <param name="remove_empty" type="boolean" label="Remove taxa that are entirely missing within a partition" checked="false" truevalue="--remove-empty" falsevalue="" />
        <param name="data_type" type="select" label="Data type">
            <option value="aa">Protein alignments</option>
            <option value="dna">Nucleotide alignments</option>
        </param>
        <param name="check_align" type="boolean" label="Check if input sequences are aligned" checked="false" truevalue="--check-align" falsevalue="" />
    </inputs>

    <outputs>
       <expand macro="collection_outputs" name="split_alignments" label="Split alignment files"/>
    </outputs>

    <tests>
        <test expect_num_outputs="1">
            <param name="input_file" value="inputs/concat_result.phylip" />
            <param name="split_by" value="inputs/partitions_concat_unspecified.txt" />
            <param name="remove_empty" value="true" />
            <param name="out_format" value="fasta" />
            <param name="in_format" value="phylip" />
            <param name="data_type" value="dna" />
            <param name="check_align" value="false" />
            <output_collection name="split_alignments_fasta" type="list">
                <element name="concat_result_p1_concat_1-out.fas" file="outputs/expected_split_partition1.fas" />
                <element name="concat_result_p2_concat_2-out.fas" file="outputs/expected_split_partition2.fas" />
            </output_collection>
        </test>
    </tests>

    <help><![CDATA[
        AMAS (Alignment Manipulation And Summary) was written for modern phylogenomics workflows, where hundreds of taxa and thousands of loci are routinely analysed and summarised. It runs as a lean Python 3 program that depends only on the standard library yet still utilises multiprocessing to chew through very large amino-acid or nucleotide alignments. Drawing on Borowiec 2016 (PeerJ 4:e1660), AMAS combines format conversions, sequence manipulation, partition-aware slicing, replicate generation, and rich summary statistics (taxon counts, alignment length, matrix cells, missing data, AT/GC content, variable and parsimony-informative sites, plus alphabet frequencies). The wrapper surfaces the following sub-commands:

        * **split** — divide a concatenated alignment using a partitions file (format `unspecified`, `nexus`, or `raxml`) and optionally drop taxa that are entirely missing within a partition.

        **What is a partitions file?**

        A partitions file defines how a concatenated alignment should be split into separate regions (genes, loci, or other features). 

        **Format examples:**

        Unspecified format::

            gene1 = 1-500
            gene2 = 501-1200

        RAxML format::

            DNA, gene1 = 1-500
            DNA, gene2 = 501-1200

        NEXUS format::

            charset gene1 = 1-500;
            charset gene2 = 501-1200;

        An example for your data can be generated using the AMAS concat tool

        Each partition will be written as a separate output file.

        Common arguments:

        1. `--in-files` (multi-select parameter in Galaxy).
        2. `--in-format` — `fasta`, `phylip`, `nexus`, `phylip-int`, or `nexus-int`.
        3. `--data-type` — `dna` or `aa`.
        4. `--check-align` verifies that sequences are aligned (disabled by default for performance).

        AMAS source code and manual: https://github.com/marekborowiec/AMAS
    ]]></help>

    <expand macro="citations" />
</tool>