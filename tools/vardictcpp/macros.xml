<macros>
    <token name="@VERSION_SUFFIX@">0</token>
    <token name="@TOOL_VERSION@">0.1.0</token>
    <xml name="ref_select">
        <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Choose the source for the reference genome file">
                <option value="cached" selected="true">Use a built-in genome</option>
                <option value="history">Use a genome from the history</option>
            </param>
            <when value="cached">
                <param name="ref_file" type="select" label="Reference Genome File">
                    <options from_data_table="fasta_indexes"/>
                </param>
            </when>
            <when value="history">
                <param name="ref_file" format="fasta" type="data" label="Reference Genome File" />
            </when>
        </conditional>
    </xml>
    <xml name="input_default">
        <param name="tumor" type="data" format="bam" label="BAM file" />
    </xml>
    <xml name="regions_conditional">
        <conditional name="restrict">
            <param name="restrict_source" type="select" label="Regions to call variants in">
                <option value="genome" selected="true">Whole genome</option>
                <option value="chromosomes">Selected chromosomes (list)</option>
                <option value="region">A single region</option>
                <option value="bed">Regions from a BED file</option>
            </param>
            <when value="genome" />
            <when value="chromosomes">
                <param name="interval_file" type="data" format="txt" label="Chromosomes" help="Restrict calls to these chromosomes (one name per line)" />
            </when>
            <when value="region">
                <param name="region" type="text" label="Region" help="A single region as chr:start-end, e.g. chr7:55019017-55211628 (1-based, commas allowed)">
                    <validator type="regex" message="Enter a region as chr:start-end">^[^\s:]+:[0-9,]+-[0-9,]+$</validator>
                    <sanitizer invalid_char="">
                        <valid initial="string.printable">
                            <remove value="&apos;" />
                            <remove value="&quot;" />
                        </valid>
                    </sanitizer>
                </param>
            </when>
            <when value="bed">
                <param name="region_bed" type="data" format="bed" label="Regions BED file" help="Call variants in the regions (chrom, start, end) listed in this BED file" />
            </when>
        </conditional>
    </xml>
</macros>
