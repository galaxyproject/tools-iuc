<?xml version="1.0"?>
<tool id="pisces_reformatvcf" name="Pisces ReformatVcf" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="18.01">
    <description>to crushed VCF</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
## skipped --help/-h, --version/-v, --crush

## initialize
mkdir data &&
ln -s '${vcf}' 'data/variants.vcf' &&

## run
/home/stephan/Projects/tools/pisces/All_5.3.0.0/ReformatVcf ## TODO
## required
--vcf 'data/variants.vcf'
## common
--outfolder 'results' ## -o, --out
    ]]></command>
    <inputs>
        <expand macro="vcf"/>
        <section name="oo" title="Output options" expanded="true">
            <param name="out" type="select" multiple="true" optional="false" label="Select output file(s)">
                <option value="ao">Applied options</option>
                <option value="l">Log</option>
                <option value="u" selected="true">Uncrushed variants (VCF)</option>
            </param>
        </section>
    </inputs>
    <outputs>
        <expand macro="out_ao" tool="ReformatVcf"/>
        <expand macro="out_l" tool="ReformatVcf"/>
        <data name="out_u" format="txt" from_work_dir="data/variants.uncrushed.vcf" label="${tool.name} on ${on_string}: Uncrushed variants"> <!-- not created in results folder -->
            <filter>'u' in oo['out']</filter>
        </data>
    </outputs>
    <tests>
        <!-- #1 default -->
        <test expect_num_outputs="3">
            <param name="vcf" value="d.vcf"/>
            <section name="oo">
                <param name="out" value="ao,l,u"/>
            </section>
            <output name="out_ao">
                <assert_contents>
                    <has_size value="2444"/>
                </assert_contents>
            </output>
            <output name="out_l">
                <assert_contents>
                    <has_text_matching expression=".+Ending.+"/>
                </assert_contents>
            </output>
            <output name="out_u">
                <assert_contents>
                    <has_n_lines n="636"/>
                    <has_text_matching expression="chr12.+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

@WID@

Pisces ReformatVcf reformats a VCF from uncrushed to crushed.

**Input**

- Uncrushed variants (VCF)

**Output**

@HELP_AO@
- Crushed variants (VCF)
@HELP_L@

.. class:: infomark

**References**

@REFERENCES@
    ]]></help>
    <expand macro="citations"/>
</tool>