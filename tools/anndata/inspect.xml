<tool id="anndata_inspect" name="Inspect AnnData" version="@galaxy_version@">
    <description>object</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="exit_code"><![CDATA[
@CMD@
      ]]></command>
    <configfiles>
        <configfile name="script_file"><![CDATA[
@CMD_imports@

adata = ad.read('$input')

#if $inspect.info == 'whole'
with open('$general', 'w') as f:
    print(adata, file=f)
#else if $inspect.info == 'X'
adata.to_df().to_csv('$X', sep='\t')
#else if $inspect.info == 'obs'
adata.obs.to_csv('$obs', sep='\t')
#else if $inspect.info == 'var'
adata.var.to_csv('$var', sep='\t')
#else if $inspect.info == 'chunk_X'
    #if $inpect.chunk.info == 'random'
        #set $select = int($inspect.chunk.size)
    #else
        #set $select = ([int(x.strip()) for x in str(($inspect.chunk.list).split(',')])
    #end if
adata.chunk_X(select=$select, replace=True)
adata.to_df().to_csv('$X', sep='\t')
#end if
]]></configfile>
    </configfiles>
    <inputs>
        <param name="input" type="data" format="anndata" label="Annotated data matrix"/>
        <conditional name="inspect">
            <param name="info" type="select" label="What to inspect?">
                <option value="general">General information about the object</option>
                <option value="X">Data matrix</option>
                <option value="obs">Key-indexed observations annotation</option>
                <option value="var">Key-indexed annotation of variables/features</option>
                <!--<option value="uns">Unstructured annotation</option>!-->
                <option value="chunk_X">A chunk of the data matrix</option>
            </param>
            <when value="general"/>
            <when value="X"/>
            <when value="obs"/>
            <when value="var"/>
            <when value="chunk_X">
                <expand macro="params_chunk_X"/>
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data name="general" format="text" label="${tool.name} on ${on_string}: General information">
            <filter>inspect['info'] == 'general'</filter>
        </data>
        <data name="X" format="tabular" label="${tool.name} on ${on_string}: Key-indexed observations annotation">
            <filter>inspect['info'] == 'X' or inspect['chunk_X']</filter>
        </data>
        <data name="obs" format="tabular" label="${tool.name} on ${on_string}: Key-indexed observations annotation">
            <filter>inspect['info'] == 'obs'</filter>
        </data>
        <data name="var" format="tabular" label="${tool.name} on ${on_string}: Key-indexed annotation of variables/features">
            <filter>inspect['info'] == 'var'</filter>
        </data>
    </outputs>
    <tests>
        <test>
            <param name="input" value=""/>
            <conditional name="inspect">
                <param name="info" value="general"/>
            </conditional>
            <output name="general" value=""/>
        </test>
        <test>
            <param name="input" value=""/>
            <conditional name="inspect">
                <param name="info" value="X"/>
            </conditional>
            <output name="X" value=""/>
        </test>
        <test>
            <param name="input" value=""/>
            <conditional name="inspect">
                <param name="info" value="obs"/>
            </conditional>
            <output name="obs" value=""/>
        </test>
        <test>
            <param name="input" value=""/>
            <conditional name="inspect">
                <param name="info" value="var"/>
            </conditional>
            <output name="var" value=""/>
        </test>
        <test>
            <param name="input" value=""/>
            <conditional name="inspect">
                <param name="info" value="chunk_X"/>
                <conditional name="chunk">
                    <param name="info" value="specified"/>
                    <param name="list" value="3,5,8"/>
                </conditional>
            </conditional>
            <output name="var" value=""/>
        </test>
    </tests>
    <help><![CDATA[
@HELP@
    ]]></help>
    <expand macro="citations"/>
</tool>
