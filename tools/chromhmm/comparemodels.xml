<?xml version="1.0"?>
<tool id="chromhmm_comparemodels" name="chromHMM CompareModels" version="@TOOL_VERSION@+@WRAPPER_VERSION@">
    <description>of emission parameters</description>

    <macros>
        <import>macros.xml</import>
    </macros>

    <expand macro="requirements"/>

    <stdio></stdio>  

    <command detect_errors="exit_code"><![CDATA[
        ## initialize
        mkdir input &&
        ln -s '${referencemodel}' emissions_ref.txt &&
        #for $i, $file in enumerate($comparedir)
            ln -s '${file}' input/emissions_${i}.txt &&
        #end for

        ## run
        @RUN@
            CompareModels
            ## -color $oo.color ## todo bug, doesn't work
            emissions_ref.txt
            input
            comparison
        ]]></command>

    <inputs>
        <!-- excluded: -->
        <!-- imitaded: noimage -->
        <param name="referencemodel" type="data" format="txt" multiple="false" label="Select a reference of emission parameters." help="Usually a text file with the prefix 'emissions_"/>
        <param name="comparedir" type="data" format="txt" multiple="true" label="Select a set of emission parameters to compare." help="Usually text files with the prefix 'emissions_"/>
        <!-- advanced parameters -->        
        <section name="ap" title="Advanced parameters">
        </section>
        <!-- output options -->
        <section name="oo" title="Output options">
            <param argument="-color" type="text" value="0,0,255" label="Set heatmap color" help="Integer values between 0 and 255 separated by commas."/>
            <param name="comparison" type="select" multiple="true" label="Which files should be created?" help="">
                <option value="png">png</option>
                <option value="svg">svg</option>
            </param>
        </section>
    </inputs>

    <outputs>
        <data name="out_comparison_txt" format="txt" from_work_dir="comparison.txt" label="${tool.name} on $(on_string) Comparison, txt"/>
        <data name="out_comparison_png" format="png" from_work_dir="comparison.png" label="${tool.name} on $(on_string) Comparison, png">
            <filter>'png' in str(oo['comparison'])</filter>
        </data>
        <data name="out_comparison_svg" format="svg" from_work_dir="comparison.svg" label="${tool.name} on $(on_string) Comparison, svg">
            <filter>'svg' in str(oo['comparison'])</filter>
        </data>
    </outputs> 

    <tests> 
        <!-- #1 only required parameters, all outputs -->
        <test>
            <param name="referencevalue" value="comparemodels_emissions_ref.txt"/>
            <param name="comparedir" value="comparemodels_emissions_1.txt,comparemodels_emissions_2.txt,comparemodels_emissions_3.txt,comparemodels_emissions_4.txt"/>
            <section name="oo">
                <param name="comparison" value="png,svg"/>
            </section>
            <output name="out_comparison_png" file="comparemodels_test1.png"/>
            <output name="out_comparison_svg" file="comparemodels_test1.svg"/>
            <output name="out_comparison_txt">
                <assert_contents>
                    <has_n_lines n="4"/>
                    <has_line_matching expression="state&#009;emissions_0.txt&#009;.+"/>
                    <has_line_matching expression="3&#009;.+"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
.. class:: infomark

**What it does**

CompareModels @HELP_WID@

CompareModels allows the comparison of the emission parameters of a selected model to a set of models in terms of correlation.

**Input**

@HELP_FILETYPE_EMISSION@

**Output**

The output is a table in both heat map and text file formshowing the maximum correlationof each state in the selected model with its best matching state in each other model.

.. class:: infomark

**References**

@HELP_REFERENCES@
    ]]></help>
    <citations>
        <expand macro="citations"/>
    </citations>
</tool>