categories:
  - Sequence Analysis
description: |
  bamUtil is a repository that contains several programs that perform operations on SAM/BAM files.
long_description: |
  bamUtil is a collection of tools for manipulating SAM/BAM files compiled into a single executable
  called bam.  The collection includes tools to rewrite, modify or extract information from SAM/BAM files.
name: bamutil
owner: iuc
homepage_url: https://github.com/statgen/bamUtil
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/main/tools/bamutil
type: unrestricted
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "Wrapper for BamUtil: {{ tool_name }}"
suite:
  name: "suite_bamutil"
  description: |
    bamUtil is a repository that contains several programs that perform operations on SAM/BAM files.
  long_description: |
    bamUtil is a collection of tools for manipulating SAM/BAM files compiled into a single executable
    called bam.  The collection includes tools to rewrite, modify or extract information from SAM/BAM files.
