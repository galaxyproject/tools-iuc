<macros>
    <token name="@TOOL_VERSION@">3.6.4</token>
    <token name="@PYTHON_VERSION@">3.10</token>
    <token name="@VERSION_SUFFIX@">0</token>
    <token name="@PROFILE@">25.0</token>

    <xml name="requirements">
        <requirements>
            <requirement type="package" version="@PYTHON_VERSION@">python</requirement>
            <requirement type="package" version="@TOOL_VERSION@">biapy</requirement>
            <!-- Make cv2 come from conda-forge; pin to a recent 4.10 series -->
            <requirement type="package" version="4.10.*">opencv</requirement>
            <!-- Ensure OpenEXR ABI matches OpenCV’s build -->
            <requirement type="package" version="3.3.5">openexr</requirement>
            <!-- (usually pulled automatically, but harmless/robust to include) -->
            <requirement type="package" version="3.2.1">imath</requirement>
        </requirements>
    </xml>
    <xml name="creators">
        <creator>
            <person givenName="Daniel" familyName="Franco" url="https://danifranco.github.io/" identifier="https://orcid.org/0000-0002-1109-110X" />
        </creator>
    </xml>
    <xml name="citations">
        <citations>
            <citation type="doi">10.1038/s41592-025-02699-y</citation>
            <yield/>
        </citations>
    </xml>

    <!-- Q9 -->
    <xml name="train_raw_param">
        <param name="raw_train" type="data" format="tiff,tif,png,jpg,jpeg,h5,hdf5" multiple="true" label="Specify the training raw images" help="Select the training images; all must have the same number of channels, and each channel should contain the same type of information (see https://biapy.readthedocs.io/en/latest/get_started/how_it_works.html for details)."/>
    </xml>

    <xml name="train_raw_param_opt">
        <param name="raw_train" type="data" optional="true" format="tiff,tif,png,jpg,jpeg,h5,hdf5" multiple="true" label="Specify the training raw images" help="Select the training images; all must have the same number of channels, and each channel should contain the same type of information (see https://biapy.readthedocs.io/en/latest/get_started/how_it_works.html for details)."/>
    </xml>

    <!-- Q10 -->
    <xml name="train_gt_param">
        <param name="gt_train" type="data" format="tiff,tif,png,jpg,jpeg,h5,hdf5,csv,txt" multiple="true" label="Specify the training ground truth (target) images" help="Select the ground truth (target) data for training; the expected format depends on the workflow (e.g. masks for semantic/instance segmentation, centroid files for detection, high-res images for super-resolution, paired images for image-to-image); see https://biapy.readthedocs.io/en/latest/get_started/select_workflow.html for details."/>
    </xml>
    <xml name="train_gt_param_opt">
        <param name="gt_train" type="data"  optional="true" format="tiff,tif,png,jpg,jpeg,h5,hdf5,csv,txt" multiple="true" label="Specify the training ground truth (target) images" help="Select the ground truth (target) data for training; the expected format depends on the workflow (e.g. masks for semantic/instance segmentation, centroid files for detection, high-res images for super-resolution, paired images for image-to-image); see https://biapy.readthedocs.io/en/latest/get_started/select_workflow.html for details."/>
    </xml>

    <!-- Q11 -->
    <xml name="test_raw_param">
        <param name="raw_test" type="data" format="tiff,tif,png,jpg,jpeg,h5,hdf5" multiple="true" label="Specify the test raw images" help="Select the test images; all must have the same number of channels, and each channel should contain the same type of information to avoid confusing the model."/>
    </xml>
    <xml name="test_raw_param_opt">
        <param name="raw_test" type="data" format="tiff,tif,png,jpg,jpeg,h5,hdf5" optional="true" multiple="true" label="Specify the test raw images" help="Select the test images; all must have the same number of channels, and each channel should contain the same type of information to avoid confusing the model."/>
    </xml>

    <!-- Q13 -->
    <xml name="test_gt_param">
        <param name="test_gt"
            type="data"
            format="tiff,tif,png,jpg,jpeg,h5,hdf5,csv,txt"
            multiple="true"
            label="Specify the test ground truth/target images @LABEL_SUFFIX@"
            help="Select the ground truth (target) data for testing; the expected format depends on the workflow (e.g. masks for semantic/instance segmentation, centroid files for detection, high-res images for super-resolution, paired images for image-to-image); see https://biapy.readthedocs.io/en/latest/get_started/select_workflow.html for details."
            optional="@OPTIONAL@" />
    </xml>
</macros>
