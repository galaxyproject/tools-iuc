<tool id="cnvkit_antitarget" name="CNVkit Antitarget" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="21.05">
    <description>Given a “target” BED file that lists the chromosomal coordinates of the tiled regions used for targeted resequencing, derive a BED file off-target/”antitarget” regions.</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="xrefs"/>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[  
        ln -s '$targets_file' ./capture.bed &&
        #if $access
            ln -s '$access' ./access.bed &&
        #end if
        cnvkit.py antitarget
            ./capture.bed
            #if $access
                --access ./access.bed
            #end if
            --avg-size $avg_size
            --min-size $min_size
            --output '$output_setting.output'
    ]]></command>
        <configfiles>
        <configfile name="script_file"><![CDATA[
        ]]></configfile>
    </configfiles>
    <inputs>
        <param name="targets_file" type="data" format="bed" label="input BED or interval file" help="input BED or interval file listing the targeted regions." />
        <param name="access" type="data" optional="true" format="bed" label=" Regions of accessible sequence on chromosomes" help="" />
        <param name="avg_size" optional="true" type="integer" label=" Average size of antitarget bins." value="150000" help="only use integers, don't use fractures" />
        <param name="min_size" optional="true" type="integer" label="Minimum size of antitarget bins." value="25000" help="dropp smaller regions. only use integers, don't use fractures (Default: 1/16 avg size, calculated)" />
        <section name="output_setting" title="Change output file name" expanded="false">
            <param name="output" optional="true" type="text" label="Output file name" value="capture.antitarget.bed" help="File_name.bed" />
        </section>
    </inputs>
    <outputs>
        <data name="out_capture_antitarget" format="bed" label="${tool.name} on ${on_string}: capture antitarget regions" from_work_dir="capture.antitarget.bed" />
    </outputs>
    <tests>
        <test expect_num_outputs="1">
            <param name="targets_file" ftype="bed" value="capture.bed" />
            <param name="output" value="capture.antitarget.bed" />
            <output name="out_capture_antitarget" file="capture.antitarget.bed" /> 
        </test>
    </tests>
    <help><![CDATA[
    Given a “target” BED file that lists the chromosomal coordinates of the tiled regions used for targeted resequencing, derive a BED file off-target/”antitarget” regions.
    ]]></help>
    <citations>
        <citation type="doi">10.1371/journal.pcbi.1004873</citation>
    </citations>
</tool>
