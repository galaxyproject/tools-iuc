<?xml version="1.0"?>
<tool id="pyeuk_generate_sheet" name="PyEuk generate-sheet" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="26.0">
    <description>build a haplotype sheet from BLAST calls or assembled contigs, PyEuk generate-sheet</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="edam"/>
    <expand macro="requirements"/>
    <expand macro="version_command"/>
    <command detect_errors="aggressive"><![CDATA[
#if $source.kind == "specimen_zip"
pyeuk generate-sheet
    --specimen-dir '$source.specimen_zip'
    #if $source.background_zip
    --background-dir '$source.background_zip'
    #end if
    #if $source.de_novo
    --de-novo
    #end if
    --output '$sheet'
#else
pyeuk generate-sheet
    --assembled-fasta '$source.assembled_fasta'
    #if $source.de_novo_fa
    --de-novo
    #end if
    --output '$sheet'
#end if
    ]]></command>
    <inputs>
        <conditional name="source">
            <param name="kind" type="select" label="Input type">
                <option value="specimen_zip" selected="true">Specimen genotype BLAST files (.zip)</option>
                <option value="assembled_fasta">Externally assembled haplotype contigs (FASTA)</option>
            </param>
            <when value="specimen_zip">
                <param name="specimen_zip" type="data" format="zip" label="Specimen genotype files (.zip)"
                       help="A .zip of per-specimen genotype BLAST calls."/>
                <param name="background_zip" type="data" format="zip" optional="true" label="Background reference genotypes (.zip, optional)"/>
                <param name="de_novo" type="boolean" checked="false" truevalue="true" falsevalue="" label="De novo (no reference database)"/>
            </when>
            <when value="assembled_fasta">
                <param name="assembled_fasta" type="data" format="fasta" label="Assembled haplotype contigs (FASTA)"/>
                <param name="de_novo_fa" type="boolean" checked="true" truevalue="true" falsevalue="" label="De novo (no reference database)"/>
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data name="sheet" format="tabular" label="${tool.name} on ${on_string}: haplotype sheet"/>
    </outputs>
    <help><![CDATA[
**Build a presence/absence sheet from calls or contigs**

The non-amplicon path into PyEuk. Use it when haplotypes were already called elsewhere -- as
genotype BLAST files, or as externally assembled contigs -- rather than read off amplicon
windows. For the amplicon read-level path use **Build haplotype sheet** instead.

The output is a specimen x haplotype sheet, the same format the distance and cluster tools
consume.
@HELP_FOOTER@
    ]]></help>
    <expand macro="citations"/>
</tool>
