name: das_tool
owner: iuc
description: "DAS Tool for genome resolved metagenomics"
homepage_url: https://github.com/cmks/DAS_Tool
long_description: |
    DAS Tool is an automated method that integrates the results of a flexible number of binning algorithms to calculate an optimized, non-redundant set of bins from a single assembly.
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/tools/das_tool
type: unrestricted
categories:
- Metagenomics
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "Wrapper for the DAS tool suite: {{ tool_name }}"
suite:
  name: "suite_das_tool"
  description: "DAS Tool for genome resolved metagenomics"
  long_description: |
    DAS Tool is an automated method that integrates the results of a flexible number of binning algorithms to calculate an optimized, non-redundant set of bins from a single assembly.
