name: "polypolish"
owner: "iuc"
long_description: |
  "Polypolish is a tool for polishing genome assemblies with short reads.
  Polypolish uses SAM files where each read has been aligned to all possible locations (not just a single best location).
  This allows it to repair errors in repeat regions that other alignment-based polishers cannot fix."
categories:
- Sequence Analysis
remote_repository_url: "https://github.com/mesocentre-clermont-auvergne/galaxy-tools/tree/master/tools/polypolish"
homepage_url: "https://github.com/rrwick/Polypolish"
type: unrestricted
auto_tool_repositories:
  name_template: "{{ tool_id }}"
  description_template: "{{ tool_name }} tool for metagenomics analysis and visualization"
