<tool id="vpt_convert" name="Vizgen Post-processing Tool - Convert" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Convert geometry boundries to parquet or MEROSCOPE tiff to OME</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[

    ]]></command>
    <configfiles>
        <configfile name="vpt_config">

        </configfile>
    </configfiles>
    <inputs>

    </inputs>
    <outputs>

    </outputs>
    <tests>

    </tests>
    <help>

    </help>
    <expand macro="citations" />
</tool>