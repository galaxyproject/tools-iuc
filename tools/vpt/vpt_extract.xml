<tool id="vpt_extract" name="Vizgen Post-processing Tool - Extract" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Extract segmentation results and image patches</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements" />
    <command detect_errors="exit_code"><![CDATA[

    ]]></command>
    <configfiles>
        <configfile name="vpt_config">

        </configfile>
    </configfiles>
    <inputs>

    </inputs>
    <outputs>

    </outputs>
    <tests>

    </tests>
    <help>

    </help>
    <expand macro="citations" />
</tool>