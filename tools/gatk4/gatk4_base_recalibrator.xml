<tool id="gatk4_base_recalibrator" name="GATK4 BaseRecalibrator" version="@WRAPPER_VERSION@" profile="@PROFILE@">
    <description>Generates recalibration table for Base Quality Score Recalibration (BQSR)</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <expand macro="version_cmd"/>
    
    <command detect_errors="exit_code"><![CDATA[
    ## Create a temp directory
    mkdir -p tmp &&

    ## 1. Link Reference Genome
    #if $reference_source.reference_source_selector == "history":
        ln -s '$reference_source.reference_sequence' reference.fa &&
        samtools faidx reference.fa &&
    #else:
        ln -s '$reference_source.reference_sequence.fields.path' reference.fa &&
        ln -s '${reference_source.reference_sequence.fields.path}.fai' reference.fa.fai &&
    #end if
    
    ## Ensure dictionary exists
    gatk CreateSequenceDictionary -R reference.fa &&

    ## 2. Link Input BAM and Index
    ln -s '$input_bam' input.bam &&
    ln -s '${input_bam.metadata.bam_index}' input.bai &&

    ## 3. Link Known Sites
    ## Loop handles multiple input files (e.g., dbSNP and Indels)
    #for $i, $site in enumerate($known_sites):
        ln -s '$site' known_sites_${i}.vcf.gz &&
        ln -s '${site.metadata.tabix_index}' known_sites_${i}.vcf.gz.tbi &&
    #end for

    ## 4. Link Interval List (if provided)
    #if $ival_type.ival_type_sel == 'ival_file':
        ln -s '$ival_type.intervals' intervals.interval_list &&
    #end if

    ## 5. Run BaseRecalibrator
    gatk --java-options "-Xmx\${GALAXY_MEMORY_MB:-4096}m -XX:-UsePerfData" BaseRecalibrator
        -R reference.fa
        -I input.bam
        -O recal_data.table
        
        ## Add all known sites from the list
        #for $i, $site in enumerate($known_sites):
            --known-sites known_sites_${i}.vcf.gz
        #end for
        
        ## Use current working directory for temp files
        --tmp-dir tmp

        ## Use the original qualities (OQ tag) if requested by the user
        $use_original_qualities

        #if $ival_type.ival_type_sel == 'ival_file':
            -L intervals.interval_list
        #end if
    ]]></command>

    <inputs>
        <conditional name="reference_source">
            <param name="reference_source_selector" type="select" label="Choose the source for the reference genome">
                <option value="cached">Locally cached</option>
                <option value="history" selected="true">History</option>
            </param>
            <when value="cached">
                <param name="reference_sequence" type="select" label="Reference" help="Select reference genome">
                    <options from_data_table="fasta_indexes" />
                </param>
            </when>
            <when value="history">
                <param name="reference_sequence" type="data" format="fasta" label="Reference" help="Select reference genome from history" />
            </when>
        </conditional>
        
        <param name="input_bam" argument="-I" type="data" format="bam" label="Input BAM" help="Aligned and indexed BAM file." />
        
        <param name="known_sites" argument="--known-sites" type="data" format="vcf_bgzip" multiple="true" label="Known Sites" help="VCF file(s) (e.g., dbSNP, Mills) masking known sites. Must be bgzipped and indexed." />

        <param argument="--use-original-qualities" type="boolean" truevalue="--use-original-qualities" falsevalue="" checked="true" label="Use Original Qualities" help="Use the OQ tag if present (--use-original-qualities)." />

        <conditional name="ival_type">
            <param name="ival_type_sel" type="select" label="Restrict to Intervals?">
                <option value="no_ival" selected="true">No, process whole BAM</option>
                <option value="ival_file">Yes, use an Interval List</option>
            </param>
            <when value="no_ival"/>
            <when value="ival_file">
                <param name="intervals" argument="-L" type="data" format="picard_interval_list" label="Interval List" />
            </when>
        </conditional>
    </inputs>

    <outputs>
        <data name="output_recal" format="txt" from_work_dir="recal_data.table" label="${tool.name} on ${on_string}: Recalibration Table" />
    </outputs>

    <tests>
        <test>
            <conditional name="reference_source">
                <param name="reference_source_selector" value="history" />
                <param name="reference_sequence" value="test_ref.fasta" />
            </conditional>
            <param name="input_bam" value="bqsr_input.bam" />
            <param name="known_sites" value="test_dbsnp.vcf.gz,test_mills.vcf.gz" />
            <param name="use_original_qualities" value="true" />
            <conditional name="ival_type">
                <param name="ival_type_sel" value="ival_file" />
                <param name="intervals" value="bqsr_interval.interval_list" />
            </conditional>
            <output name="output_recal" file="bqsr_expected.table" lines_diff="2" />
        </test>

        <test>
            <conditional name="reference_source">
                <param name="reference_source_selector" value="cached" />
                <param name="reference_sequence" value="test_ref" />
            </conditional>
            <param name="input_bam" value="bqsr_input.bam" />
            <param name="known_sites" value="test_dbsnp.vcf.gz,test_mills.vcf.gz" />
            <param name="use_original_qualities" value="true" />
            <conditional name="ival_type">
                <param name="ival_type_sel" value="ival_file" />
                <param name="intervals" value="bqsr_interval.interval_list" />
            </conditional>
            <output name="output_recal" file="bqsr_expected.table" lines_diff="2" />
        </test>
    </tests>

    <help><![CDATA[
        **BaseRecalibrator**

        Generates a recalibration table for Base Quality Score Recalibration (BQSR).

        **Input Requirements**

        * **Input BAM:** Must be aligned and indexed.
        * **Known Sites:** One or more VCF files (bgzipped/indexed) to mask real variants.

        **More Information**
        Visit `GATK Documentation <https://gatk.broadinstitute.org/hc/en-us/articles/360037593511-BaseRecalibrator>`_.
    ]]></help>

    <citations>
        <expand macro="citations" />
    </citations>
</tool>