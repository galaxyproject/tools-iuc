<tool id="mtnucratio" name="Mt/Nuc Ratio Calculator" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE@">
    <description>Calculates Mitochondrial to Nuclear read ratio</description>
    <macros>
        <token name="@TOOL_VERSION@">0.7.1</token>
        <token name="@VERSION_SUFFIX@">0</token>
        <token name="@PROFILE@">25.1</token>
    </macros>

    <requirements>
        <requirement type="package" version="0.7.1">mtnucratio</requirement>
    </requirements>

    <command detect_errors="exit_code"><![CDATA[
        ## 1. Create symlinks for BAM and its index
        ln -s '$input_bam' input.bam &&
        if [ -f '$input_bam.metadata.bam_index' ]; then 
            ln -s '$input_bam.metadata.bam_index' input.bam.bai; 
        fi &&

        ## 2. Run the tool
        mtnucratio input.bam '$mt_chrom_name' &&
        mv input*.mtnucratio '$output_report' &&
        mv input*.json '$output_json'
    ]]></command>

    <inputs>
        <param name="input_bam" type="data" format="bam" label="Input BAM file" help="BAM file must be coordinate-sorted and indexed." />
        <param name="mt_chrom_name" type="text" value="MT" label="Mitochondrial Chromosome Name" help="Exact name of the mitochondrial contig (e.g., MT, chrM, rCRS)." />
    </inputs>

    <outputs>
        <data name="output_report" format="txt" label="${tool.name} on ${on_string}: Report" />
        <data name="output_json" format="json" label="${tool.name} on ${on_string}: JSON" />
    </outputs>

    <tests>
        <test expect_num_outputs="2">
            <param name="input_bam" value="test.bam" ftype="bam" />
            <param name="mt_chrom_name" value="MT" />
            <output name="output_report">
                <assert_contents>
                    <has_text text="mitochondrium" />
                    <has_text text="nuclear" />
                </assert_contents>
            </output>
            <output name="output_json">
                <assert_contents>
                    <has_text text="mtnuccalculator" />
                    <has_text text="mtreads" />
                    <has_text text="nucreads" />
                </assert_contents>
            </output>
        </test>
    </tests>

    <help><![CDATA[
**Mt/Nuc Ratio Calculator**
 
It calculates the ratio of reads mapping to the mitochondrial genome versus the nuclear genome.

**Inputs**

* **BAM File:** A coordinate-sorted and indexed BAM file.
* **Mitochondrial Chromosome Name:** The identifier used in the FASTA/BAM header for the mitochondrial genome (e.g., `MT`, `chrM`).

**Outputs**

* **Report:** A text file containing the counts and the calculated ratio.
* **MultiQC JSON:** A JSON file properly formatted to be recognized by MultiQC for plotting.
    ]]></help>

    <citations>
        <citation type="doi">10.5281/zenodo.13739840</citation>
    </citations>
    <creator>
        <person givenName="Ali Mert" familyName="Aydin" url="https://github.com/mertydn"/>
        <organization name="Galaxy Europe" url="https://galaxyproject.eu/" />
    </creator>
</tool>
