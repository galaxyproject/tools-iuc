categories:
- Tool Dependency Packages
description: Contains a tool dependency definition that downloads and compiles version
  3.2.1 of the R package.
long_description: |
  This package is configured with the following options:

  ./configure --with-tcltk \
  --with-blas \
  --with-lapack \
  --with-readline \
  --with-cairo \
  --with-libpng \
  --without-x \
  --enable-R-shlib \
  --disable-R-framework \
  --libdir=$INSTALL_DIR/lib \
  --prefix=$INSTALL_DIR
name: package_r_3_2_1
owner: iuc
remote_repository_url: https://github.com/galaxyproject/tools-iuc/tree/master/packages/package_r_3_2_1
type: tool_dependency_definition
